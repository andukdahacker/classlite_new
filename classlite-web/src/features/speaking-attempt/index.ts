/**
 * speaking-attempt feature barrel — Story 5.4 (TS-7). Only the route entry is
 * public; everything else is deep-imported within the feature. Consumes the shared
 * `@/features/attempts` spine (5.2d) + the presigned-upload spine (1.2e/4.4a).
 */
export { SpeakingAttemptPage } from './SpeakingAttemptPage'
