/**
 * writing-attempt feature barrel (TS-7) — Story 5.3. The student writing attempt
 * surface (desktop s34 / mobile s78), a consumer of the shared `attempts` spine
 * (5.2d). Cross-feature imports go through this barrel only.
 */
export { WritingAttemptPage } from './WritingAttemptPage'
