VITE_API_URL=http://localhost:8080
# Empty by default; set to a real DSN in `.env.local` to enable Sentry init
# locally. Story 1-7b's `initSentry()` no-ops when this is blank.
VITE_SENTRY_DSN=
# Set by CI to the deploy commit SHA; falls back to `dev` for local builds.
VITE_RELEASE_SHA=dev
