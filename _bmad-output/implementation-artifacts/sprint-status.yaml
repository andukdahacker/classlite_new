# generated: 2026-05-28
# last_updated: 2026-06-25 (Story 1-8-auth-ui-registration-and-login-screens transitioned review → done after /bmad-code-review pass (Amelia, different LLM than implementer). 3 parallel reviewers (Blind Hunter / Edge Case Hunter / Acceptance Auditor) over the 4772-line diff surfaced 80 raw findings → 3 decision-needed + 12 patch + 14 defer + 24 dismissed. All 3 decisions resolved + 13 patches applied (P8 + P13 reclassified to dismiss after verification). **Decisions:** D1 PasswordStrengthBar token mapping — amended spec to make shadcn-semantic (`bg-destructive` / `bg-primary`) + `bg-amber-500` (pragmatic warning stand-in) + `bg-[color:var(--cl-status-success)]` the canonical mapping; original spec referenced `--cl-status-danger` / `--cl-accent-2-btn` tokens that never existed in tokens.css. Deferred-work tracks `1-8-followup-warning-token-bridge` for the `--cl-status-warning` migration. D2 `useAuth.isLoading` hardcoded `false` — wired to boot-probe state now via new `getBootProbeInFlight() / subscribeBootProbe() / runBootProbe()` exports from auth-refresh.ts + second `useSyncExternalStore` subscription in useAuth.ts; App.tsx calls `runBootProbe()` instead of `refreshAccessToken()` so future Story 2.6 route guards can wait for the probe to resolve before deciding the user is logged out (closes the Winston #4 regression vector pre-emptively for the gated case). D3 OAuth-error transient — replaced `toast.error(...)` with an inline `<div role="alert">` in the form-level error slot; pinned test now asserts (1) alert text matches `t('auth.login.error.oauthGeneric')` and (2) the `?error=` param is cleared via a `UrlProbe` component reading MemoryRouter's `useSearchParams` + added negative test for clean `/login` landing. **Patches:** P1 useRegister navigate gains `encodeURIComponent(verifyPollId)` + `{ replace: true }`; P2 RegisterPage 422 handler tracks `applied` count and falls back to generic alert when zero `setError` calls fired (closes silent-submit-failure for non-allowlisted field names); P3 PasswordStrengthBar empty announcement gets `sr-only` class when score === 0 so "Password strength: none" / "Độ mạnh mật khẩu: chưa nhập" doesn't render as visible body text on first paint; P5 mutation keys split into `authKeys.loginMutation()` / `authKeys.registerMutation()` (cache-write `session()` key stays shared); P6 `if (isPending) return` guards at top of both onSubmit handlers (Enter-while-pending no longer double-submits); P7 AuthLayout MobileLanguageToggle adds `touchstart` listener alongside `mousedown` + `event.target instanceof Node` narrowing (iOS Safari tap-to-collapse now works); P9 registerSchema `.regex(/\S/)` rejects all-whitespace passwords; P10 localized `passwordMax` + `fullNameMax` + `passwordNotBlank` messages added to en+vi (3 new keys); P11 App.tsx boot-probe guard tightened to `if (session === undefined)` only (future-logout `null` sentinel no longer triggers doomed refresh); P12 LoginPage OAuth effect refactored to a `useState` lazy initializer (reads searchParams at mount) + `useRef` latch for the side-effect to avoid the `react-hooks/set-state-in-effect` cascade lint rule. **Dismissed after verification:** P8 (React only updates the `<p>` text node when score changes — `aria-live` doesn't spam per-keystroke); P13 (form.tsx Slot uses cloneElement + PasswordInput spreads `...rest` to inner Input — aria-describedby chain intact). **24 dismissed as noise** + **14 deferred** appended to `deferred-work.md` under new "Deferred from: code review of story 1-8-auth-ui-registration-and-login-screens (2026-06-25)" heading. **CI matrix after patch wave:** Vitest 340/340 across 43 files (was 336 baseline, +4 new tests: D2 isLoading transition + D3 hardened-pinned + D3 negative-pin + P5 mutation-key-distinct), tsc -b clean, ESLint clean (the cascade lint surfaced one issue during patch application; refactored to lazy-initializer pattern), lint:css clean, i18n-parity 275 keys clean (was 272 + 3 new validation keys), npm run build clean (LoginPage chunk 11.19 KB / RegisterPage chunk 5.88 KB / AuthLayout chunk 2.41 KB). 3 ★ REVIEWER-MANDATORY Vietnamese error keys (accountLocked/rateLimited/generic) + new oauthGeneric still need VN-fluent reviewer pass before merge — flagged in PR description per AC2 contract. **Net file touches:** auth-refresh.ts (D2 boot-probe state + reset extension), useAuth.ts (D2 isLoading subscription), App.tsx (D2 runBootProbe + P11 guard tighten), LoginPage.tsx (D3 inline alert + P6 isPending guard + P12 lazy initializer), RegisterPage.tsx (P2 applied-count fallback + P6 isPending guard), AuthLayout.tsx (P7 touchstart listener), PasswordStrengthBar.tsx (P3 sr-only + D1 JSDoc), registerSchema.ts (P9 not-blank + P10 localized max messages), authKeys.ts (P5 mutation key factories), login.ts/register.ts (P5 mutationKey wiring + P1 navigate fix), en.json/vi.json (P10 3 new validation keys), login.test.tsx (P5 mutationKey assertions), authKeys.test.ts (P5 distinctness tests), LoginPage.test.tsx (D3 pinned-test hardening + UrlProbe), useAuth.test.tsx (D2 isLoading transition test), i18n-parity-coverage.test.ts (P10 STORY_1_8_KEYS extension), story spec (AC1 PasswordStrengthBar canonical mapping amendment + Review Findings subsection), deferred-work.md (1-8 follow-ups + W1-W14 catalog). Prior context preserved below.) (Story 1-8 e2e pass — Playwright design-system 27/27 + cross-subdomain 6/6 + setup green. Includes 4 new Story 1-8 bilingual-smoke scenarios (/login form labels en+vi + /register H1 en+vi). One trivial in-session spec fix: getByLabel(passwordLabel) switched to {exact: true} so the eye-toggle's aria-label doesn't collide with the FormLabel under Playwright strict mode. DoD line 14.4 flipped to [x]. Dev-server boot-probe ECONNREFUSED noise (visible when Go API isn't running) tracked as non-blocking enhancement candidate in completion-notes — probe still functions as designed per Task 15. Prior context preserved below.) (Story 1-8-auth-ui-registration-and-login-screens transitioned in-progress → review. All 8 ACs + 14 tasks + Task 15 boot-probe discharged in a single session against baseline acf99f2. Net adds: 32 i18n keys (en+vi parity-clean, 4 ★ REVIEWER-MANDATORY); 5 shared auth components (AuthCard / GoogleOAuthButton / CollapsibleEmailForm / PasswordInput / PasswordStrengthBar) + co-located stories with `// storybook-rule: no-three-state` opt-out per pure-presentational nature; passwordStrength pure-fn library (categorical scoring per UX-DR8); LoginPage replaces deleted LoginPagePlaceholder (route + bundle-boundary regex updated in lockstep per Amelia #2); RegisterPage with 409 force-expand + 422 per-field setError + 429 + generic + emailDelivery:failed toast branches; AuthLayout enriched with wordmark + responsive LanguageToggle (mobile collapses to 32×32 icon-only that expands on tap per Sally amendment); useLogin + useRegister mutations populate authKeys.session() cache key; useAuth graduated from no-session stub to useSyncExternalStore-subscribed cache reader (chosen over useQuery({enabled: false}) after the latter failed the Murat #5 cross-component subscription contract); auth-refresh.ts body-parse refactor per Winston #1 (performNetworkRefresh parses EnvelopeLoginResult, RefreshResult carries data, literal ['auth','session'] cache key avoids the lib/→features/ import cycle, BroadcastChannel payload carries data so sibling tabs hydrate via setQueryData NOT invalidate, malformed-body stays ok:true); apiFetch gains ApiError.retryAfterSeconds sibling readonly property (NOT spread into details per Winston #3) + new surfaceAuthError option (LoginPage 401 INVALID_CREDENTIALS needs the original ApiError, not the AuthExpiredError translation); MSW handler catalog renamed 1-5.md→auth.md with /register section appended per Murat #4; vitest-setup.ts gains afterEach(queryClient.clear()) safety net with dynamic-import workaround for the @sentry/react mock-hoist ordering trap; App.tsx boot-time refresh probe per Task 15 with useRef latch + 3 pinned tests. Test matrix: Vitest 336/336 across 43 files (was 251 baseline; +85 new tests), tsc -b clean, ESLint clean (Google brand colors carry per-line eslint-disable with trademark-spec justification), lint:css clean, i18n-parity 272 keys (270 claimed; only legacy app.name/app.welcome un-claimed which is correct — not under any covered namespace), npm run build clean (LoginPage chunk 10.90 KB / RegisterPage chunk 5.63 KB / AuthLayout chunk 2.29 KB), npm run storybook:build clean, storybook test-runner 60 suites / 303 tests axe-zero in ~20s. Three documented deviations from the spec letter resolved pragmatically: (1) useSyncExternalStore over useQuery for useAuth (subscription reliability); (2) new surfaceAuthError option (skipAuthRefresh discards the 401 code); (3) inline <div role='alert'> for form-level errors (no Alert primitive in components/ui/ — 1d-2 shipped 38 primitives but Alert wasn't one). All three documented inline + in completion-notes Debug Log + escalated in story Change Log entry for code-review attention. 4 ★ REVIEWER-MANDATORY Vietnamese error keys (accountLocked/rateLimited/generic/oauthGeneric) still need a VN-fluent reviewer pass before merge — flagged in PR description per AC2. Playwright bilingual-smoke + cross-subdomain pass NOT run in this session — recommended as the first step of code-review pickup. Sibling completion-notes file authored per bmad-story-conventions.md (second story under the split convention after 1d-4). Hand-off to /code-review (recommend a different LLM than the one that implemented). Prior context preserved below.) (Story 1-8-auth-ui-registration-and-login-screens transitioned ready-for-dev → in-progress; Amelia picked up /bmad-dev-story 1-8 against baseline acf99f2 (1d-4 done; story file YAML frontmatter already carried baseline_commit so preserved per dev-story workflow rule — current HEAD matches at acf99f2). 14-task ordering plus Task 15 boot-probe to execute; no risk score ≥6 owned, WF-8 ATDD not required (R38 inheritance via per-story describe block extends i18n-parity-coverage.test.ts). Prior context preserved below.) (Story 1-8-auth-ui-registration-and-login-screens received a 4-agent party-mode review pass (Winston/Sally/Murat/Amelia) on the initial ready-for-dev scaffold; 10 findings surfaced — 6 load-bearing contract conflicts with on-disk state. 18 spec amendments encoded inline (NOT a re-scaffold): (1) AC5 auth-refresh.ts patch rewritten from two-line hand-wave to full body-parse refactor — performNetworkRefresh parses EnvelopeLoginResult, RefreshResult carries data, cache write uses literal ['auth','session'] key inside lock body avoiding third edge on the existing query-client↔api-fetch import cycle, BroadcastChannel payload carries data so sibling tabs hydrate via setQueryData (NOT invalidate which would clobber to null), malformed body stays ok:true to avoid downgrading flaky-gateway to logout (Winston #1 + Amelia #1). (2) AC5 Session contract: isAuthenticated derived from user.emailVerified NOT accessToken — closes registered-but-unverified footgun for 1.9a (Winston #5). (3) AC3+AC4 Zod schemas switched to in-component useMemo(t) builder hooks per Form.stories.tsx:52-72 canonical pattern — module-load i18n.t() froze validation messages to bootup locale (Murat #2 + Amelia #3). (4) Dev Notes Retry-After patch corrected: retryAfterSeconds is sibling readonly property on ApiError NOT spread into details (spreading would have corrupted 422 VALIDATION_ERROR's [{field,message}] array via {...arr} and silently no-op'd the setError loop) (Winston #3). (5) Task 15 added: boot-time refresh probe useEffect in App.tsx — LIFTED from Out of Scope per Ducdo's decision after Winston #4 walk-through proved 'first 401 hydrates' deferral didn't work (user reloading /dashboard with valid refresh cookie was getting bounced to /login without firing silent-refresh path; double-login regression). (6) QueryClient cache leak fixed: AC5 mandates createTestQueryClient() wrapper per auth test + Task 2.3 adds afterEach(queryClient.clear()) to vitest-setup.ts (Murat #1, single highest-leverage gap). (7) Task 11.4 amended: deleting LoginPagePlaceholder.tsx also updates e2e/route-bundle-boundaries.spec.ts:36-46 regex LoginPagePlaceholder → LoginPage (Amelia #2 — original deletion would have left a dead chunk-absence assertion vacuously passing). (8) AC6 dot-grid requirement DELETED — bg-dot-grid already globally applied via body in index.css:121; AuthLayout doesn't add it (spec was wrong on both class name AND necessity). (9) AC6 LanguageToggle mobile placement locked: 32×32 icon-only collapsed top-right, expands on tap (Sally — was hand-waved 'designer's call'). (10) AC2 vi key seeds pinned to AUTH-01/03 mockup literals (Sally #2): signUpLink='Chưa có tài khoản? Đăng ký' NOT 'Mới đến? Tạo tài khoản'; emailPlaceholder='email@example.com' NOT 'tên@truong.edu'; passwordPlaceholder='Ít nhất 8 ký tự' NOT 'Tối thiểu 8 ký tự'; new loginPasswordPlaceholder='Nhập mật khẩu' (no length hint per SEC-1); new passwordRequired error key replaces login schema's misuse of passwordMin. 4 keys flagged ★ REVIEWER-MANDATORY (accountLocked/rateLimited/generic + new oauthGeneric) — merge blocked until VN-fluent reviewer sign-off, not 'machine-translated flag-in-PR'. (11) AC1 AuthCard contract resolved: ship as plain <section role='region'> NOT a shadcn Card composition (would override 3 of 4 visual props anyway) (Winston minor). (12) AC1 GoogleOAuthButton gains aria-busy=true + visually-pressed for ~80-200ms top-level nav teardown on click (Sally — prevents flaky-network double-click). (13) AC4 LoginPage gains searchParams.get('error') transient bridge: oauthGeneric Alert on /login?error=... landings between 1-8 and 1.9d (Sally #7). (14) AC4 LoginPage rememberMe default false documented as deviation from AUTH-03 mockup (security-first for shared-phone VN students). (15) Task 2.4 added: append /register section to MSW catalog AND rename msw-handler-catalog-1-5.md → msw-handler-catalog-auth.md for broader scope (Murat #4). (16) DoD --project=bilingual-smoke filter dropped — no such Playwright project exists (Amelia #5); bilingual scenarios live in design-system project's testDir per playwright.config.ts:45-143. (17) Mutation three-state contract enumerated by name in AC3/AC4 pinned tests (Murat #3); useAuth cross-component subscription test added (Murat #5). (18) Dev Notes adds explicit thumb-zone exception documentation (Sally #3 — Google-first dominance outranks thumb-zone per § 10.3). Net effect: story grew 590 → ~720 lines (still under bmad-story-conventions.md spec-discipline target by inheritance — dev-record material continues to land in sibling completion-notes file at first pickup). 4 product decisions surfaced; Ducdo locked all 4 in the same session (boot-probe in scope; cache reset via both per-test wrapper + global clear; isAuthenticated from emailVerified; mockup copy authoritative over machine translations). 1 follow-up tracked: ESLint no-restricted-imports config carve-out for hooks/useAuth.ts → features/auth/api/authKeys may need explicit addition. Story REMAINS ready-for-dev — Amelia's 14-task ordering survives with Task 15 appended; reviewers converged on substance, only on who-fixes-it (Ducdo chose 'PM amends inline before dev pickup'). Hand-off to Amelia (dev) for /bmad-dev-story 1-8 unchanged — every contract bug surfaced by the panel is now resolved in the spec, not in dev's notebook. Prior context preserved below.) (Story 1-8-auth-ui-registration-and-login-screens scaffolded backlog → ready-for-dev. John's pre-dev context engine pass against baseline acf99f2 (1d-4 done). 8 ACs map verbatim to epic 1C Story 1.8 with implementation contracts pinned: 5 shared components (AuthCard, GoogleOAuthButton, CollapsibleEmailForm, PasswordInput, PasswordStrengthBar) at src/features/auth/components/ per FW-7 + storybook-conventions § 2; LoginPage replaces LoginPagePlaceholder + adds /register route under existing AuthLayout boundary; useLogin + useRegister mutations populate authKeys.session() cache key via setQueryData; useAuth graduates from no-session stub to cache-subscribing useQuery (enabled:false, queryFn:()=>null) so sibling components re-render on cache writes; auth-refresh.ts patched so silent-refresh-on-401 also hydrates the session cache; AuthLayout enriched with dot-grid + ClassLite wordmark + LanguageToggle slot; bilingual smoke gains /register H1 + /login form-label scenarios; MSW handlers landed verbatim from _bmad-output/test-artifacts/msw-handler-catalog-1-5.md into new src/test/mocks/handlers.ts (Story 1-8 is the authoritative consumer per the catalog frontmatter). ~25 new i18n keys (en+vi) with per-story describe('Story 1-8 i18n parity (R38)', ...) block extending i18n-parity-coverage.test.ts (matches 1d-2/1d-3/1d-4 inheritance pattern — no separate ATDD ceremony). Risk-score ≥6 check: NONE owned (R38 discharged at 1-7c, R4/R5/R6/R7/R13 owned by backend stories 1.5/1.6 with contracts already locked in api.yaml). WF-8 ATDD not required. Out of scope (explicit deferrals each owned by a specific later story): VerificationPending+useVerificationPoller → 1.9a (redirect target /verify-email?pollId= until 1.9a ships); ForgotPassword+ResetPassword → 1.9b (link target only in 1-8); InviteCard+useInviteToken → 1.9c (searchParams prop on GoogleOAuthButton ships in type signature but is never passed); Lockout countdown+OAuth mismatch+Google-Workspace-blocked+SessionExpired → 1.9d (1-8 surfaces 429 ACCOUNT_LOCKED as plain inline alert with Retry-After minutes interpolation, no polished recovery screen); role-aware post-login redirect → Story 2.6; onboarding redirect after first login → Epic 2 Story 2.1; standalone boot-time refresh probe → tracked follow-up (silent-refresh-on-first-401 path is enough for now and auth-refresh.ts patch in 1-8 hydrates the cache on that path); /api/auth/me endpoint NOT added (doesn't exist; session bootstraps from login/register/refresh response bodies); useLogout NOT shipped (1.9d/Epic 2 consumer). Scaffold reality acknowledged: 1d-2 already shipped all 38 shadcn primitives so Button/Input/Label/Form/Checkbox/Collapsible/Alert/Separator/Card are all available — no `npx shadcn add` runs in 1-8; LoginPagePlaceholder.tsx deleted in same commit as LoginPage.tsx lands; LoginPage preserves the t('auth.login.title') H1 contract from 1-7c so the existing bilingual smoke spec stays green. Story file authored under bmad-story-conventions.md split convention (sibling completion-notes.md authored at first dev pickup, not at story creation — 1-8 is the second story under this convention after 1d-4). Story file holds at ~500 lines (well under 600-line convention cap). Pinned Retry-After capture extension to apiFetch documented in Dev Notes (two-line patch surfacing retryAfterSeconds on ApiError.details when code is ACCOUNT_LOCKED or RATE_LIMIT_EXCEEDED). Hand-off to Amelia (dev) for /bmad-dev-story 1-8 — 14 ordered tasks, 8 ACs with pinned test contracts, DoD with 14 checkboxes covering CI matrix + LoginPagePlaceholder deletion + useAuth graduation + MSW handler landing + axe-zero + bilingual smoke green. Prior context preserved below.) (Story 1d-4-phase4-visual-bridge transitioned review → done after /bmad-code-review (Amelia). Three parallel reviewers (Blind Hunter / Edge Case Hunter / Acceptance Auditor) surfaced 7 decision-needed + 22 patch + 12 defer + 13 dismiss. All 7 decisions resolved against the pragmatic-interpretation rule: D1 essayHtml became branded `SafeHtml` (new `src/lib/safe-html.ts` with `asSafeHtml(...)` brand assertion + JSDoc contract), stories rewired to wrap fixture HTML via `asSafeHtml`; D2 dropped `onSubmitReply` from `AnchoredQuestionCardProps` entirely — Epic 7 Story 7.4 wires the real submit handler when behavior lands; D3 amended AC1 spec line 34 to keep `Toggle`-only toolbar (Toggle is the semantically correct primitive for B/I/H/L pressed-state, `Button` redundancy dropped); D4 amended AC6 Props block to match shipped `InboxListShellProps` (filters required, `onRowPrimaryAction` / `onRowArchive` optional) with rationale; D5 amended AC6 line 253 to document the bespoke multi-select toggle-chip pattern (`ToggleGroup` exclusive-select + `Badge.Removable` tag-dismiss were semantically wrong for inbox filters); D6 added JSDoc on `ScopeBar.onDateRangeChange` documenting Epic 8 Story 8.2 wiring; D7 swapped `InboxRow` unread `bg-[color:var(--cl-tint-blue)]/40` → `bg-muted/40` matching the CommentCard remediation pattern. Patch wave (29 patches total = 22 original + 7 decision-resolved): P1 raw-ISO timestamps across 4 components fixed via `*Label` companion props (`savedAtLabel`, `teacherReply.timestampLabel`, `row.occurredAtLabel` — matching the existing `askedAtLabel` precedent); P2 `MobileWritingSurface.SAVE_TONE.saving` swap `text-muted-foreground` → `text-foreground` (axe contrast regression class of bug Debug Log had fixed elsewhere in 1d-4); P3 `InboxListShell` filter chip gains `aria-pressed={active}` for consistency with ScopeBar; P4 the inner `<X>` becomes `aria-hidden="true"` (no double-announcement); P5 `activeFilters` filtered against `filters` (orphan-key defensive guard); P6 filter Badge guarded with `count > 0`; P7 `ScopeBar.selectedClassId || undefined` value coercion; P8 empty `classOptions` renders disabled SelectItem with new `scopeBar.classPicker.noOptions` key; P9 `SpeakingGradingSurface` axis hidden when `durationSec <= 0`, waveform aria-label switches to new `speakingGrading.waveform.noRecording` key; P10 `durationSec` NaN/Infinity defensive guard; P11 `CommentCard` body render guarded against empty string; P12 `ROLE_BADGE_VARIANT[asker.role] ?? 'outline'` runtime fallback; P13 `AnchoredQuestionCard` inner `data-testid`s suffixed with `question.id` (multi-instance collision fixed); P14 `formatBand` returns `'—'` on `!Number.isFinite(v) || v < 0` in both grading surfaces; P15 `MobileWritingSurface` truncated title gains `aria-label={resolvedTitle}` (UX-2 + AT reveal matching SidebarNavItem precedent); P16 dropped `contentEditable={false}` on body div (read-only fixture; no edit affordance to advertise); P17 `InboxRow` archive aria-label interpolates row context (`${archive}: ${mainText}`); P18 ICU plural `wordCount_one`/`wordCount_other` for both `writeDocSurface.footer` and `mobileWriting.footer` namespaces (en+vi); P19 `inboxRow.admin.billing.main` and `inboxRow.admin.integration.main` route status/action through i18next nested `$t(inboxRow.billingStatus.{{status}})` / `$t(inboxRow.integrationAction.{{action}})` with new `inboxRow.billingStatus.succeeded`/`failed` + `inboxRow.integrationAction.connected` keys (en+vi) — closes mixed-language string leak; P20 AC1 `WriteDocSurface` Default story swapped to Vietnamese sample text + `savedAtLabel: 'Đã lưu 2 phút trước'` (matches spec line 60 literal "realistic Vietnamese sample text"); P21 `deriveInitials` adds `.filter(Boolean)` + empty-parts fallback to `'?'`; P22 added `1d-followup-tokenizer-migration: backlog` follow-up entry to sprint-status. WriteDocSurface JSDoc updated to drop the misleading "Intl.DateTimeFormat at the i18n layer" claim — replaced with the actual `savedAtLabel` consumer-formatter contract. New keys added to en.json + vi.json (10 new): writeDocSurface.footer.wordCount_one/_other, mobileWriting.footer.wordCount_one/_other, speakingGrading.waveform.noRecording, scopeBar.classPicker.noOptions, inboxRow.billingStatus.succeeded/failed, inboxRow.integrationAction.connected — STORY_1D_4_KEYS extended accordingly, i18n-parity 240 keys parity-clean + namespace-coverage clean. New file: classlite-web/src/lib/safe-html.ts (12 LOC; `SafeHtml` brand + `asSafeHtml(...)` assertion — lives at the lib boundary so `WritingGradingSurface.tsx` stays react-refresh/only-export-components compliant). 12 defer items appended to deferred-work.md under new "Deferred from: code review of 1d-4-phase4-visual-bridge (2026-06-24)" heading. 13 findings dismissed (carry-over noise already documented in completion notes, false-positive tripwires — Button `xs`/`icon-xs` variants verified present in 1d-2 base-nova bundled button.tsx:25/29, etc.). Test matrix: Vitest 251/251 across 30 files, tsc -b clean, ESLint clean, i18n-parity 240 keys clean. Storybook + Playwright suites not re-run in code-review pass — recommend a fresh storybook test-runner pass post-merge against the renamed AnchoredQuestionCard inner test ids before Epic 7 starts consuming. Prior context preserved below.) (Story 1d-4-phase4-visual-bridge transitioned in-progress → review. All 8 ACs + 8 tasks discharged in one session. 10 new domain components ship (one more than AC count — CommentCard sub-component reused across AC2+AC3): WriteDocSurface, WritingGradingSurface (consumes CommentCard), SpeakingGradingSurface (consumes CommentCard), AnchoredQuestionCard, MobileWritingSurface, InboxRow, InboxListShell, ScopeBar, AnalyticsHomeShell. Each story file exports Default + Loading + Empty + Error per 1d-1's three-state lint, plus spec-listed variants (Saving / Offline / LocaleEn / LocaleVi / role-views per UX-3 / etc.). 122 new i18n keys (61 en + 61 vi) across 11 new namespaces; STORY_1D_4_KEYS block added to i18n-parity-coverage.test.ts. AC8 audit: zero `new Date()` calls in 1d-4 files (only JSDoc references), FW-7 placement clean (all components under src/components/domain/), every interactive element carries `data-testid`. New `.cl-anchor-*` highlight classes in src/index.css for AC2 fixture HTML use existing `--cl-tint-*` tokens + underline reinforcement for color-blind WCAG 1.4.1. Two carry-over fixes that ship with 1d-4: (1) scripts/lib/strip-comments-and-strings.mjs pass-order reorder (BLOCK → LINE → TEMPLATE → DOUBLE_QUOTE → SINGLE_QUOTE) — third occurrence of the regex-strip class of bug after apostrophes in JSDoc + 1d-3 follow-up #16; templates with `I'm`/`author's`/`can't` (AnchoredQuestionCard LongQuestion) and JSX double-quoted attributes with `couldn't` (InboxListShell Empty/Error) were false-opening single-quoted strings and eating export declarations; 2 regression tests added; tokenizer migration this util's docstring promises now has its tripwire fired but the targeted reorder ships with 1d-4 to unblock — acorn migration tracked separately. (2) SidebarNavItem.LongVietnameseLabel play test updated to match 1d-3 code-review's drop of the native `title` attribute (P23) — the storybook test-runner was not re-run during 1d-3 code-review close-out so the regression sat dormant; corrected in lockstep. Axe-core sweep applied four targeted fixes: (a) MobileWritingSurface app-bar `<header>` → `<div>` (banner landmark cannot nest in role=region); (b) WriteDocSurface `<time>` dropped opacity-70 (under AA contrast at text-xs); (c) text-muted-foreground → text-foreground + font-semibold on small-caps labels across CommentCard, WritingGradingSurface, SpeakingGradingSurface, AnchoredQuestionCard, InboxRow, WriteDocSurface footer (axe failed on 6.7-ratio at text-xs in CI even though AA paper-math passes — pragmatic upgrade rather than tag suppression); (d) CommentCard resolved state replaced card-wide opacity-60 with bg-muted/40 + body-only line-through (opacity dropped text contrast to 4.34 < 4.5); (e) AnalyticsHomeShell stories AnalyticsCardSkeleton h3 → h2 (PageHead h1 → h3 was skipping h2); Empty / Error stories switched to custom render bypassing the shell so EmptyStatePlaceholder's own h3 isn't preceded by PageHead's h1. Final test matrix: Vitest 251/251 across 30 files (was 248 baseline + 1 STORY_1D_4_KEYS test + 2 strip-utility regression tests); storybook test-runner 283/283 axe-clean across 55 suites in ~16s; tsc / lint / lint:css / build / storybook:build / i18n-parity 233 keys (122 new) all clean. Story file holds spec + Change Log (5 most recent cap); implementation record lives in new sibling file _bmad-output/implementation-artifacts/1d-4-phase4-visual-bridge-completion-notes.md per docs/bmad-story-conventions.md (first sibling under that convention — 1d-1/1d-2/1d-3 explicitly not migrated). One DoD item open: designer-notification non-code follow-up assigned post-merge. ScopeBar date-range picker ships as a static label button rather than mounting Calendar primitive — Calendar Range integration deferred to Epic 8 consumer because mounting would require a Date object in render (TS-6 + AC8 no-new-Date audit). Hand-off to /code-review (recommend a different LLM than the one that implemented). Prior context preserved below.) (Story 1d-4-phase4-visual-bridge transitioned backlog → in-progress; Amelia picked up /bmad-dev-story 1d-4 against baseline a900107 (story-file frontmatter; preserved per dev-story workflow rule — current HEAD is 9da21b7 with 1d-3 done). 1d-3 hard dependency satisfied. No risk-score ≥6 ACs in this story per spec; ATDD not required per WF-8. Prior context preserved below.) (Story 1d-3-app-shell-stack transitioned review → done after code-review patch wave (Amelia, post-merge, baseline c390282..4b88072). Three parallel reviewers (Blind Hunter, Edge Case Hunter, Acceptance Auditor) surfaced 24 patches + 7 decision-needed + 7 deferred + 16 dismissed. All 7 decisions resolved as "ship the spec compliance now": D1 wired a hamburger toggle into TopbarShell via a new `collapseToggle?: ReactNode` slot (AppLayout owns the useUIStore subscription per single-subscription discipline); D2 shipped the AC8 mobile pattern — single primary row with responsive eyebrow styling + optional mobileTitle row beneath; D3 replaced BreadcrumbBar's decorative ellipsis with an interactive DropdownMenu of skipped middle Link items; D4 moved focus management inside MobileTabBar via useEffect on location.pathname (focuses #main-content post-nav — onTabActivate stays as optional side-effect callback); D5 dropped aria-live="polite" from PageHead.<h1> (focus move is the single SR announcement source); D6 added matchLongestHrefPrefix helper consumed by both AppLayout (sidebar) and MobileTabBar (internal) — deep /classes/123 highlights /classes, /classes-archived does NOT collide; D7 shipped src/__tests__/token-bridge.test.ts asserting every required bridge variable in :root maps to var(--cl-*) — comment-only governance upgraded to enforced contract. Real bugs fixed: P1 sidebar+mobile activeHref no longer hardcoded to /dashboard (useLocation.pathname + longest-prefix); P2 user.name retains role-label placeholder with explicit TODO(1-8) marker for grep; P3 SidebarNavItem disabled state renders an inert <span> so keyboard Enter can't navigate; P4 MobileTab uses new mobileTab.unreadAria key (no cross-namespace bleed) AND announces the capped "9+" string matching the visual; P5 orphan keys deleted (appShell.root, sidebar.collapseToggle, sidebar.section.workspace, sidebar.section.resources — pre-claimed to defeat namespace-coverage, exactly the loophole party-mode #13 was meant to close); P6 MobileTabBar JSDoc no longer cites the nonexistent UX-DR29 lint; P7 AppShell pb-24 conditional on mobileTabBar presence (guest shell no longer wastes 96px); P8 MobileTabBar safe-area inset for iOS home indicator; P11 once-per-session warn extracted to AppLayout-warn-tracking.ts (sibling module so AppLayout.tsx stays react-refresh/only-export-components compliant). Script discipline: P9 i18n-parity extractClaimedKeys now matches single AND double-quoted strings + escaped quotes; P10 findOrphans scans union of locale keys (vi-only orphans no longer slip past); P19 distinct exit code 4 for missing coverage test file. Test discipline: P12 app-shell-mobile-viewport.spec collapses both branches into a single expect(box === null || box.width === 0).toBe(true); P13 sidebar-nav-item-focus.spec replaces 5-attempt Tab loop with await link.focus() + await expect(link).toBeFocused(); P20 dropped className-regex tautologies from AppShell.stories + MobileTabBar.stories (the new storybook-a11y Playwright project does runtime measurement). A11y polish: P14+P15 PageHead count uses Intl.NumberFormat(i18n.language) + Number.isFinite guard; P16+P17 UserPill deriveInitials trims whitespace, handles surrogate pairs, falls back to "?" + empty-string avatarUrl collapses to null; P21 SidebarShell renders plain <div> when group has no labelKey (no unnamed section landmarks); P22+P23 SearchPill drops aria-label (visible text is the accessible name) + SidebarNavItem drops native title attribute (Tooltip owns hover/focus reveal); P24 MobileTabBar DEV-warns on unreadByTab keys not in the active tab set. P18 dismissed — http-server@14.1.1 was already at package.json:76. Test deltas: +33 tests (token-bridge 32, longest-prefix 3, hamburger toggle 1 — minus dropped tautologies). Vitest 248/248 pass (was 215). New files: src/lib/match-route.ts, src/__tests__/token-bridge.test.ts, src/components/shared/AppLayout-warn-tracking.ts. CI surface: tsc -b / lint / lint:css / i18n-parity (122 keys, 120 claimed, namespace clean) / build / storybook:build all clean. Storybook + Playwright suites untouched on this pass — D1/D2/D3 introduce new mobile pattern + hamburger UI surface that warrants a follow-up Playwright sweep before broader Epic 2 consumption (added to deferred-work.md). 7 items deferred to follow-ups (spec contract drift amendments, DoD #22/#28 non-code follow-ups, sub-375px viewport scope, Cmd/Ctrl+K Mac glyph polish, Windows CI path separator, computed STORY_KEYS, regex-literal-with-// limitation). Prior context preserved below.) (Story 1d-3-app-shell-stack transitioned review-blocked → review. All 4 block-merge + 2 polish items from the party-mode review landed (tasks #10–#15). Highlights: (#10) AppLayout no longer defaults to role='owner' — reads from useRole(), null role renders a guest shell (topbar only, no sidebar/MobileTabBar) with a dev-only console.warn; tests cover both paths. (#11) New Playwright project `storybook-a11y` with spec asserting Tab+focus opens the Vietnamese-truncation tooltip via [data-slot="tooltip-content"] (base-ui Popup doesn't carry role=tooltip). (#12) Same project, spec asserts SidebarShell boundingBox null + every MobileTab >=44×44 at viewport 375×667 — replaces className-regex tautology with runtime browser measurement. (#13) UX-DR29 lint decision: amend story to "documented contract, not enforced lint"; lint deferred until a real role-as-permission misuse appears. (#14) SidebarShell Center settings group wrapped in `<section aria-labelledby><h3>` instead of plain `<div><p>` (an initial role=group on inner <ul> broke <li> list semantics per axe — corrected). (#15) Subscription-discipline JSDoc on SidebarShell `collapsed` prop. Test matrix all green: Vitest 202/202 (+2 guest-shell tests), storybook test-runner 220/220 axe-clean, Playwright 32/32 (+3 storybook-a11y), tsc/lint/lint:css/build/storybook:build/i18n-parity clean. Status: review. 6 follow-up tasks (#16–#21) tracked but not blocking this PR. Hand-off to /code-review (recommend a different LLM than the one that implemented). Prior context preserved below.) (Story 1d-3-app-shell-stack transitioned review → review-blocked after party-mode review (Winston/Sally/Murat/John + Amelia respondent). Four block-merge items identified before code-review pass: (1) shared/AppLayout.tsx DEV_SHELL_ROLE='owner' default is a privilege-direction footgun — replace with least-privilege sentinel + dev-warn (Winston); (2) SidebarNavItem.LongVietnameseLabel lost the keyboard-focus tooltip reveal verification when Amelia dropped userEvent.tab()+toHaveFocus() citing base-ui wrapper interference — add Playwright scenario asserting tooltip in a11y tree after Tab (Sally+Murat+John); (3) AppShell.Mobile + MobileTabBar.StudentView play functions assert className strings instead of runtime CSS — WCAG 2.5.5/2.5.8 compliance contract not verified, add Playwright scenario at 375×667 measuring real bounding boxes (Murat); (4) UX-DR29 lint referenced in AC7.a closure does not actually exist in the codebase — either amend the closure to "documented, lint deferred" or implement the rule now (John). Plus 2 polish items in same PR (SidebarShell group semantics upgrade — separator already exists, semantics need <h3>+role=group+aria-labelledby; subscription-discipline comment at SidebarShell prop site). 6 follow-ups deferred post-1d-3 (extract stripCommentsAndStrings util; Playwright waitFor comment; Sally Figma comment 48h deadline; Winston shadcn-base-nova tracking issue; CODEOWNERS infra story; completion-notes sibling file convention from 1d-4 onward). Tasks #10–#21 tracked. Status review-blocked is a project-specific extension to the canonical bmad lifecycle (backlog → ready-for-dev → in-progress → review → done) — re-promotes to review once block-merge items land. Prior context preserved below.) (Story 1d-3-app-shell-stack transitioned in-progress → review. All 8 tasks + 9 ACs discharged; 26 of 28 DoD items checked (CODEOWNERS rule + designer notification + Winston tracking issue deferred to PR description / follow-up). 10 domain components shipped to src/components/domain/ with co-located stories; shared/AppLayout refactored to compose them; shared/Sidebar/TopBar/UserPill placeholders deleted; 58 new i18n keys in en+vi with 10 migrated app.layout.* keys removed. Allowlist (Option A) for *Shell three-state lint shipped in required-exports.ts + documented in storybook-conventions.md § 3.1. Namespace-coverage assertion shipped in scripts/i18n-parity.mjs (Murat vacuity guard). Test matrix: Vitest 200/200 (was 194 baseline + 6 new MobileTabBar tests), storybook test-runner 220/220 axe-clean, Playwright 29/29 (bilingual-smoke /dashboard axe race fixed with explicit skip-link wait), tsc -b / lint / lint:css / build / storybook:build / i18n-parity (123 keys parity-clean + 121 namespace-claimed) all green. Bug fix surfaced during dev: required-exports.ts stripCommentsAndStrings previously matched apostrophes inside JSDoc as string-literal openers; restructured to apply patterns sequentially so JSDoc apostrophes can no longer false-trigger; same fix mirrored in scripts/i18n-parity.mjs's STORY_*_KEYS extractor. Hand-off to code-review next; recommend running with a different LLM than the one that implemented this story.) (Story 1d-3-app-shell-stack transitioned ready-for-dev → in-progress; Amelia picked up /bmad-dev-story 1d-3 against baseline c390282. Prior context preserved below.) (Story 1d-3-app-shell-stack: Ducdo closed all 4 open decisions surfaced by the party-mode review, applied recommended option each time. (1) MobileTabBar role-to-tabs → AC7.a (in-component role switch with documented UX-3 exception; UX-DR29 lint catches future misuse at story-author level); Sally's AC7.b alternative removed from story file + Dev Notes findings table updated. (2) *Shell three-state lint → Option A (predicate-gated closed-set allowlist {AppShell, SidebarShell, TopbarShell} + CODEOWNERS rule adds Murat as required reviewer on src/test/storybook-rules/required-exports.ts); Options B/C removed. (3) STORY_1D_3_KEYS vacuity guard → ship-now (scripts/i18n-parity.mjs namespace-coverage assertion ~30 LOC lands in 1d-3 PR; defer path rejected). (4) Owner+Admin mobile extrapolation → ship-with-flag (@status: extrapolated-pending-design-review comment block on stories; designer ratifies or amends in follow-up story post-merge). DoD now reflects each closure with "closed 2026-06-18 by Ducdo" provenance. Story file holds at ~691 lines; closures reduced ambiguity, didn't grow scope. Story remains ready-for-dev. Hand-off to Amelia for /bmad-dev-story 1d-3 — every load-bearing decision now closed, no pending alternative branches in the story file. Prior context preserved below.) (Story 1d-3-app-shell-stack received a 4-agent party-mode review pass (Winston/Sally/Murat/Amelia) of the 2026-06-17 refresh; 21 findings logged in the story file's new § "Party-mode review pass findings (2026-06-18) — applied amendments log" table. All applied in-place; 1 deferred to dev-pickup decision. Top 5 load-bearing amendments: (1) Token bridge integrity = single-point-of-failure both Winston (`--cl-*` vs `--sidebar` drift governance via comment block) and Amelia (Tailwind v4 `--color-sidebar-*` prefix prerequisite in `@theme inline`) flagged → Task 1 now opens with mandatory `src/index.css` audit BEFORE first domain component lands; if `--color-*` aliases missing, ship bridge + SidebarShell in same commit so reviewer sees the proof together. (2) Vietnamese 220px truncation WCAG 2.1.1 trap (Sally + Murat) → AC9 requires aria-label + hover-AND-focus tooltip + native `title` fallback recipe — NOT focus-only — closing the mouse-only-Vietnamese-user invisible-tooltip failure mode; new 1D-P1-094a axe scenario covers truncation a11y. (3) MobileTabBar route-activation focus management (Murat net-new) → AC7 adds `onTabActivate` JSDoc contract documenting that consumer (router) moves focus to `<h1>` + aria-live announces page title; new 1D-P0-019b story scenario. (4) STORY_1D_3_KEYS vacuity guard (Murat) → Task 6 ships namespace-coverage assertion in `scripts/i18n-parity.mjs` — orphan keys in 1d-3 namespaces fail CI; closes vacuous-pass loophole permanently. (5) *Shell allowlist must be predicate-gated closed set (Winston) → Dev Notes § amended with predicate (no useQuery / only slot+role+UI-state props / no user-data conditionals); CODEOWNERS rule adds Murat as required reviewer on `src/test/storybook-rules/required-exports.ts` blocking allowlist-only PRs. Other applied: Owner+Admin mobile `@status: extrapolated-pending-design-review` flag (Sally); `MobileTab.hasUnread: boolean | number` to preserve count on 44×44px tabs (Sally); 1D-P0-020 absence selector swapped from `queryByRole({name})` to `queryByTestId('sidebar-nav-primary')` to avoid i18n-coupled false-greens (Murat); PageHead three-state precision note (visual-shape-vs-consumer-fetch, Murat); Shadcn-base-nova primitive-quirk tracking issue added to DoD — Breadcrumb/Pagination/Dialog/Sheet aria quirks (Winston, decision owner); single `uiStore.sidebarCollapsed` subscription at AppShell (Winston) — `SidebarShell` receives `collapsed` as prop, not via hook re-subscription (avoids untestable double-renders); useRole() null guard for MobileTabBar tabs derivation (Amelia); TS-6 module-scope date audit (Amelia) — grep at module scope, not just render path; Avatar primitive ring-slot pre-check (Amelia); Student-tone namespace enforcement via story-author `play` function (Sally — drift-catches at story author level not just code review); BreadcrumbPage workaround documented as the second of a series → upstream-or-fork tracking issue (Winston). ONE open dev decision deferred to pickup: AC7.a (in-component role switch — John's current draft) vs AC7.b (per-role MobileTabBar components + orchestrator preserving UX-3 purity — Sally's counter, +30 LOC). Amelia + John choose at pickup with the choice documented in component header JSDoc + PR description. Task 0/7 commit-sequence discipline ratified by both Winston + Amelia: i18n atomic (en+vi same commit) → domain components green → AppLayout refactored → placeholders deleted. Story file grew to ~760 lines (from 604 post-refresh) — bulk additions in AC1/AC7/AC9 + Task 0/1/6/7 + Dev Notes § primitive quirks + Dev Notes § party-mode findings table + DoD (now 28 checkboxes). Hand-off to Amelia (dev) for /bmad-dev-story 1d-3 unchanged. Prior context preserved below.) (Story 1d-3-app-shell-stack transitioned backlog → ready-for-dev after John's pre-dev scaffold refresh. Story file rewritten in place against on-disk state at c390282 (1d-2 done). Three load-bearing corrections applied: (1) scaffold reality — src/components/shared/ already carries AppLayout.tsx + placeholder Sidebar.tsx + TopBar.tsx + UserPill.tsx from 1-7c; 1d-3 ships canonical role-aware components in components/domain/, refactors AppLayout to consume them, and retires the shared/ placeholders (Sidebar/TopBar/UserPill deleted; AppLayout refactored in place per FW-7 — app-wide layout stays in shared/). i18n key migration plan documented: app.layout.sidebar.* / topbar.* / userPill.roleLabel.* → sidebar.* / topbar.* / userPill.role.* with app.layout.skipToContent preserved. (2) Token plane is the shadcn-semantic bridge — index.css:64-97 ships bg-sidebar / text-sidebar-foreground / bg-sidebar-primary / bg-sidebar-accent / text-foreground / bg-card / border-border / ring-ring etc. 1d-3 domain components MUST use bridge utilities, NOT raw bg-[var(--cl-sidebar-bg)] arbitrary values the 1-7c placeholders used. Escape hatch bg-[color:var(--cl-green)] only for un-bridged success/warning semantic colors. Pattern 2 NOT extended (already at 5-file quota from 1d-2). (3) R38 i18n parity inherited via STORY_1D_3_KEYS extension to i18n-parity-coverage.test.ts (mirrors STORY_1D_2_KEYS pattern at lines 98-138) — NO new helper, NO new CI step, NO ATDD ceremony; conventions § 8 documents this. Carry-overs from 1d-2 close-out injected: BreadcrumbBar (AC1) overrides primitive English aria-label="More" + BreadcrumbPage ARIA shape per deferred-work.md line 5/8 — canonical solution to the leak; CommandPalette deferral (CommandDialog showCloseButton + InputGroupAddon fragility) NOT in 1d-3 scope (SearchPill is visual affordance only); portal-root carry-over (conventions § 4) is foundation only — 1d-3 shells do not portal; DropdownMenu Default play re-enable NOT in 1d-3 scope (SidebarShell does not consume DropdownMenu); pragmatic-i18n § 8.1 honored (Storybook structural demo copy MAY stay literal English). Load-bearing dev decision flagged: *Shell three-state lint tension — AppShell/SidebarShell/TopbarShell match *Shell pattern but are pure layout (per handoff line 162: PageHead is only data-rendering); story recommends Option A (amend conventions § 3 with pure-layout allowlist + src/test/storybook-rules amendment) over Option B (three-state stubs) or Option C (rename). WF-8 ATDD: NOT required for 1d-3 (no score ≥6 ACs; R38 discharged at 1-7c, R52 foundation-level at 1d-1, R51/R53 MONITOR). Test scenarios from test-design-qa.md "Epic 1D Refresh (2026-06-15)": 1D-P0-015..020 + 1D-P0-025 + 1D-P1-053..114 enumerated in story Dev Notes. Hand-off to Amelia (dev) for /bmad-dev-story 1d-3 — scaffold migration is Task 0 PRE-WORK; Task 7 is the REPLACEMENT step after domain components green in Storybook. Verify shared/ has no external consumers via grep before delete (today: only shared/AppLayout consumes shared/Sidebar etc.; no production route mounts AppLayout per 1-7c JSDoc). Prior context preserved below.) (Story 1d-2-shadcn-primitive-coverage transitioned review → done after code-review pass. 5 parallel review agents (BH-Stories, BH-Infra, EC-Stories, EC-Infra, AA) ran over the 9469-line diff; 6 decisions resolved (pragmatic AC8 i18n scope; portal-root carry-over to 1d-3; Progress font-mono deferral to consumer wrapper; drop Avatar SizeMd; dismiss Skeleton Text/Card FW-7 contradiction per AC4 literal; accept locale-blind `*.longViText` fixture convention) and 18 patches applied. Highlights: Form story rebuilt with inline-translated Zod schema + locale-pinned `WithRHFAndZodResolverEn` / `WithRHFAndZodResolverVi` stories asserting locale-correct validation copy via `i18n.t()`; ContextMenu/Command/NavigationMenu inherit DropdownMenu's Base UI #31 deferral comments (consistent AC3 escape hatch); Skeleton.ReducedMotion + Progress.IndeterminateReducedMotion gained `play` functions asserting `animationName === 'none'` so 1D-P1-049..052 no longer pass vacuously; Sonner `cn-toast` typo removed + `theme="light"` pinned; `next-themes` runtime dep removed (contradicted CL-THEME-SWAP rationale); App.tsx now mounts `<TooltipProvider>` so production tooltips have a provider; form.tsx Slot reworked to parent-wins for ARIA/id + cn() composition for className/style + chained handlers + dev-warn on non-element children + guard-before-hooks in `useFormField`; test-runner `preVisit` resets `reducedMotion` to `no-preference` between stories (Playwright emulation leaked); Input/Textarea stop reusing `auth.common.email` as a generic label + Textarea drops the placeholder-as-error antipattern (`storybook.textarea.errorTooLong` etc.); Calendar hoists `new Date()` to module scope + switches to `getUTCDay()` for TZ-stable weekend mask + `LocaleViWithEvents` routes hardcoded `buổi học hôm nay` via `t('storybook.calendar.eventToday')`; Popover/Tooltip route hardcoded Vietnamese `Trạng thái` trigger labels through `t()` so en-locale renders English; Avatar.WithImage drops external github.com asset for an inline data-URL; Avatar.SizeMd story dropped (was identical to Default); Slider default thumb-count is `[min]` (no accidental range mode); Form/Separator/ScrollArea/Sonner Default-as-alias exports removed; Dialog/Sheet/Drawer/AlertDialog plays use `screen.findByRole` (no document.querySelector); `date-fns` pin restored to exact `4.4.0`; `storybook-conventions.md` § 4 + 8 amended with portal-root carry-over note, pragmatic-i18n scope rule, and locale-blind fixture convention. 15 new i18n keys land in both en + vi (65 keys total parity-validated); STORY_1D_2_KEYS extended in `i18n-parity-coverage.test.ts`. 11 deferrals recorded in `deferred-work.md` (primitive-level English aria/sr-only labels, CommandDialog touch-close UX, shadcn-upstream nits — pagination role, BreadcrumbPage ARIA, PaginationLink href, AvatarBadge a11y, Calendar focus scroll, Calendar RTL chevron selector, AlertDialogCancel type surface, InputGroupAddon focus heuristic, DropdownMenu Default play). Post-patch test matrix: Vitest 189/189 across 27 files, ESLint clean, `tsc -b` clean, `npm run i18n-parity` zero (65 keys), AC7 audit greps all return zero, Pattern 2 file count unchanged at 5 (form / tooltip / skeleton / sonner / toggle-group). Hand-off: ready to commit + open PR for 1d-2 close-out, then proceed to 1d-3 app-shell-stack. Prior context preserved below.) (Story 1d-2-shadcn-primitive-coverage transitioned in-progress → review. All 8 ACs discharged in a single session (vs the planned 6-PR split, per user direction). 38 enumerated primitives installed via `npx shadcn@4.8.3 add` (+ `input-group` as a transitive of `command`); `form.tsx` manually authored per Pattern 2 because base-nova ships an empty schema. Five Pattern 2 file edits in `ui/` (`form` / `tooltip` / `skeleton` / `sonner` / `toggle-group`) — each carries `// CL-THEME-SWAP:` justification. All 5 AC7 audit greps return zero. `tokens.css` extended with `--cl-skeleton-pulse-*` motion tokens; canonical-set guard in `tokens-presence.test.ts` updated. `<Toaster />` mounts at `src/App.tsx` top-level; Storybook canvas mounts its own via `.storybook/preview.tsx` (wrapped with `<TooltipProvider delay={0}>` + `#storybook-portal-root` + local Toaster sibling). `.storybook/test-runner.ts` `preVisit` hook calls `page.emulateMedia({ reducedMotion })` so AC8 1D-P1-049..052 actually assert. 9 new `storybook.*` i18n keys in both en + vi (50 keys total parity-validated). Inline `describe('Story 1d-2 i18n parity (R38)', ...)` block in `i18n-parity-coverage.test.ts`. Test matrix: Vitest 189/189 across 27 files, tsc + ESLint + stylelint clean, `npm run i18n-parity` zero, `npm run build` clean, `npm run storybook:build` clean, `npm run storybook:test:ci` 185/185 axe-clean across 38 suites in ~12s. Three documented per-story axe suppressions (`Tabs` color-contrast, `Command` aria-required-children, `ToggleGroup` aria-allowed-attr) — all primitive-level Base UI / cmdk quirks with 1d-3 domain-wrapper remediation paths. DropdownMenu Default `play` deferred — Base UI production error #31 in the test-runner Chromium fires on `userEvent.click` (portal/Suspense edge case); tracked for 1d-3 re-enable when Base UI stabilizes test-runner interop. Side-correction: prior `last_updated` advertised ready-for-dev but the dict still read `backlog` — corrected in-place during the in-progress transition. Hand-off to code-review next (different LLM recommended).) (Story 1d-2-shadcn-primitive-coverage transitioned ready-for-dev → in-progress; Amelia picked up /bmad-dev-story 1d-2 against baseline 347d3a5. Side-correction: PM scaffold-refresh commit advertised ready-for-dev in the comment header but the dict line still read `backlog` — fixed in-place when transitioning to in-progress. Prior context preserved below.) (Story 1d-2-shadcn-primitive-coverage transitioned backlog → ready-for-dev after John's pre-dev scaffold refresh. Story file rewritten in place against on-disk state at 347d3a5 (1d-1 done). Three load-bearing corrections applied: (1) primitives are Base UI not Radix — components.json `style: "base-nova"` + `@base-ui/react@^1.5.0` is the installed engine, existing button.tsx imports `Button as ButtonPrimitive from "@base-ui/react/button"`; architecture.md "Radix" references are unrefreshed and out of scope; (2) Tailwind tokens flow through the shadcn-semantic bridge (`bg-background` / `bg-primary` / `bg-muted` / `bg-accent` / `bg-destructive` / `bg-card` / `bg-sidebar*` / `text-foreground` / `text-muted-foreground` / `border-border` / `ring-ring` / `font-sans` / `font-heading` / `font-mono` / `rounded-sm`..`rounded-2xl`) defined in src/index.css's `@theme inline` block — custom `bg-cl-paper` / `rounded-cl-control` / `border-cl-line` Tailwind utilities DO NOT EXIST and would have caused silent compile-pass with broken styling; AC7 expanded with full token-mapping table, three audit greps, and arbitrary-value escape `bg-[color:var(--cl-green)]` for un-bridged semantic colors (success/warning); (3) AC4 i18n parity infrastructure already inherited from Story 1-7c — no new helper authored; per-story discharge is a `describe('Story 1d-2 i18n parity (R38)', ...)` block extending the existing `src/lib/test/__tests__/i18n-parity-coverage.test.ts`, NOT separate ATDD ceremony. Primitive count corrected to 38 (was 32 in inventory / 34 in TEA matrix — accurate AC enumeration gives 12+7+4+5+7+3=38 including Breadcrumb added for 1d-3 BreadcrumbBar consumer). File-naming convention clarified: lowercase `button.tsx` / PascalCase `Button.stories.tsx` / story title `ui/<PascalName>`. New deps flagged: `react-day-picker` (via shadcn Calendar install) + `sonner` (via shadcn Sonner install). Dev notes loaded with full 1d-1 inheritance summary: Tier A held (R39 → MONITOR), `createTestQueryClient()` factory consumed via `useState(() => createTestQueryClient())` per decorator mount, role toolbar has `none` as default (production unauthenticated baseline), `EmptyStatePlaceholder`/`ErrorStatePlaceholder` for downstream — primitives don't consume, axe gated via `parameters.a11y.test = 'error'`, branch protection requires BOTH `test` AND `storybook` checks. Pre-existing flake at `src/test/lint-fixtures/integration-rules-active.test.ts` (needs --testTimeout 60000) and `src/routes.tsx:34` eslint-disable-next-line carried forward — both out of scope for 1d-2. WF-8 ATDD: NOT required (no ≥6 risk ACs in 1d-2; R51/R52/R53 all ≤4; R38 discharged at 1-7c; R39 mitigated at 1d-1). Mock seams: TEST-FE-1 inheritance — Form story uses vi.fn() fake mutation (not MSW, not real useMutation); Sonner triggers via story controls; vi.mock('@tanstack/react-query') is code-review reject. Designer review touchpoint: download storybook-static artifact from CI after merge; token tweaks land in tokens.css via 1.7a, not per-primitive ui/ edits. Hand-off to Amelia (dev) for /bmad-dev-story 1d-2 — pre-existing flake timeout known, all CI gates from 1d-1 will exercise the work.)
# previous_update_0: 2026-06-15 (Story 1d-1-storybook-foundation transitioned backlog → ready-for-dev after Murat's pre-dev TD gate refresh closed. /bmad-tea TD surgical refresh layered Epic 1D onto the 2026-06-04 system-level baseline (in-place, no fork) — test-design-architecture.md + test-design-qa.md + classlite_new-handoff.md all carry new "Epic 1D Refresh (2026-06-15)" sections. Risk decisions: R38 (i18n parity, score 6) discharge-mapped to 1d-1 AC4 (assertI18nParity helper + i18n:parity CI step + WF-8 ATDD red phase MANDATORY before AC4 implementation begins — pinned test: assertI18nParity.test.ts::TestAssertI18nParity_RaisesOnViOnlyKey); R39 (Vite/Rolldown plugin) promoted MONITOR (4-5) → MITIGATE (6) for Epic 1D scope — mitigation = three-tier ladder per 1d-1 AC1 (Tier A Rolldown preferred / Tier B dual-builder fallback / Tier C deferral), Tier C kill-switch requires PM (John) + user (Ducdo) re-scope approval; R45 (CF cache wrong origin) confirmed unchanged at MONITOR (3). Three new Epic-1D-only risks added: R51 (axe baseline drift across 34 primitives + 18 domain components, MONITOR 4) mitigated by storybook:test gate + axe.allowlist.json governance carried from 1-7c; R52 (three-state variant explosion across ~200 stories, MITIGATE 6) mitigated by 1d-1 AC3 @storybook/test-runner postRender hook with missing-empty-export.stories.tsx negative-fixture proof — error on merge from day 1; R53 (designer token churn, MONITOR 4) mitigated by Pattern 1 CSS-variable token discipline in 1d-2 AC7 + // CL-THEME-SWAP: comment governance. Coverage matrix: 25 P0 + 114 P1 + 32 P2 + 10 P3 Epic 1D scenarios across 52 component files / 24 matrix rows (1d-2 grouped by AC category × 6; 1d-3 individually × 10; 1d-4 individually × 8; 1d-1 foundation rows × 5). CI storybook job has 8-min soft cap (shard-by-pattern documented if runtime trends > cap after 100 stories). WF-8 ATDD applicability summary: 1d-1 AC4 is the ONLY ≥6-risk AC across Epic 1D — 1d-2/1d-3/1d-4 have no per-story red-phase ceremony, coverage enforced mechanically via 1d-1's CI gates. TEST-FE-1 mock-seam inheritance (MSW at HTTP boundary, NEVER mock useQuery — Empty stories driven by HttpResponse.json({data:[]})) propagated to all 1d-N stories via storybook-conventions.md § 6 reference. epic-1d transitioned backlog → in-progress (first story in epic 1D promoted per create-story workflow convention). 1d-1 pre-dev-gate paragraph in the story file updated CLOSED 2026-06-15 with the gate outcome summary. Hand-off to Amelia (dev) for /bmad-dev-story 1d-1 — Tier A spike first (1 working day timebox), Tier B if needed (1 day), escalate to Tier C only with explicit John+Ducdo approval. POST-TRANSITION AMENDMENT 2026-06-15 (Murat — /bmad-tea AT pre-flight discovery): AC4 amended in-place after AT pre-flight found that Story 1-7c (shipped 2026-06-12) already delivered the R38 four-layer mitigation (assertI18nParity Vitest helper at src/lib/test/i18n-parity.ts + helper tests + ATDD red specimen at i18n-parity-coverage.test.ts + npm run i18n-parity CI step at ci-web.yml:69-77). AC4 re-scoped from "generate new helper + CI step + failing-fixture infrastructure" to inheritance contract pointing at the existing 1-7c artifacts — no new code from 1d-1. R38 discharge mapping in test-design-architecture.md updated; WF-8 ATDD applicability table corrected (no Epic 1D story requires a separate ATDD red phase, since R38 was discharged at 1-7c and R52 is foundation-CI-lint mitigation that needs no ATDD); test-design-qa.md 1D-P0-001/002/003 reframed from "generate" to "verify 1-7c artifacts still green"; classlite_new-handoff.md story 1d-1 AC pattern + R38 risk-to-story row corrected. Net effect: Amelia's 1d-1 implementation is materially smaller than the original story implied — AC4 collapses from a multi-file build to a doc-inheritance contract. Per-story 1d-2/1d-3/1d-4 dev work picks up a tiny inline `describe('Story 1d-N i18n parity (R38)', ...)` block in i18n-parity-coverage.test.ts alongside their component tests, not a separate red-phase ceremony.)
# previous_update_-1: 2026-06-12 (Story 1-7c-shared-layout-components-and-i18n transitioned review → done after code review: 5 decisions resolved (D1 tertiary CTA shipped + 2 new locale keys, D2 sidebar nav got distinct aria-label + 1 new key, D3 mobile hamburger toggle wired in TopBar, D4 BroadcastChannel cross-tab language sync added, D5 root `errorElement` added to routes.tsx); 19 patches applied across en+vi locales (+4 keys → 41 total), PermissionDenied (tertiary CTA + no-op JSDoc), Sidebar (class dedup + distinct nav aria), TopBar (hamburger + i18n breadcrumb), LanguageToggle (fieldset → role=group), useLanguageInit (BroadcastChannel + Sentry-catch on changeLanguage), usePolling (intervalMs guard + Sentry-catch on rejection), routes.tsx (root errorElement + corrected ranking comment), i18n-parity.mjs (empty-value check), ci-web.yml (i18n-parity ahead of Test for fast-fail), vitest-setup.ts (explicit toHaveNoViolations named import), sprint-status comment renumber, and tests (i18n.t resolution everywhere, TEST-FE-3 `.reset()` usage, Tab-focus contract on AppLayout, BroadcastChannel sync test on useLanguageInit). Vitest 25/25 files, 153/153 tests green (lint-fixtures suite needs higher testTimeout; 4/4 pass at 30s timeout). 7 deferred items (sectionName prop → 2-6, Sidebar placeholder nav text → 1d-3, signOut key → 1-8, cookie migration duplicate, UserPill empty-displayName guard, vitest-setup cookie reset, Playwright clearCookies catch). 7 dismissed (Sidebar collapse-on-desktop is intentional per AC2, routes.tsx inline Component matches spec literal, parity-coverage app.errorFallback-removed guard intentional, App.tsx LSP false positive, usePolling lint suppression justified, Test 5 covered in LanguageToggle.test, bilingual smoke regex matches spec literal). Story closes Epic 1C foundation triple: 1-7a (design system) + 1-7b (app shell) + 1-7c (layout chrome + i18n) all done.)
# previous_update_-2: 2026-06-12 (Story 1-7c-shared-layout-components-and-i18n transitioned in-progress → review. All 11 tasks + 50 subtasks complete. Final test matrix: Vitest 149/149 across 25 files (1-7b baseline 100/100, +49 new); Playwright design-system 23/23 (+10 bilingual smoke); cross-subdomain 6/6 (+2 dashboard-boots-in-vi). `tsc -b`, `lint`, `lint:css`, `i18n-parity` (37 keys both en+vi), `build` (7 lazy chunks: AuthLayout, LoginPagePlaceholder, StudentDashboard, TeacherDashboard, AppLayout, PermissionDenied, NotFound) all clean. Dev-route grep gates exit 1. R38 (i18n parity score 6) mitigated via 4-layer guard: parity-coverage Vitest test, `assertI18nParity` in every component test, CI `npm run i18n-parity` step gating merge, Playwright bilingual smoke walking /login + /permission-denied + catch-all + /dashboard in en+vi. Out-of-spec micro-additions documented: (a) LoginPagePlaceholder H1 swap from app.welcome → auth.login.title, (b) routes.tsx wraps /dashboard + /student in <AppLayout> via pathless lazy layout route so skip-link is reachable on /dashboard. Tech findings flagged for review: vitest-axe 0.1.0 ships empty extend-expect.js (worked around with manual expect.extend + custom TS augmentation); RTL auto-cleanup doesn't fire with Vitest globals:false (added explicit afterEach(cleanup)); usePolling refactored to assign fnRef in separate useEffect (react-hooks/refs lint). Vietnamese translation review for app.errorBoundary.* / app.permissionDenied.* / app.notFound.* flagged in PR description. Task 11.12 manual DevTools cross-domain observation deferred to reviewer — Playwright covers headlessly.)
# previous_update_-3: 2026-06-11 (Story 1-7c-shared-layout-components-and-i18n scaffolded backlog → ready-for-dev: comprehensive context engine closing the Epic 1C foundation triple. OWNS R38 (i18n parity, score 6) — WF-8 ATDD red phase MANDATORY via four-layer mitigation (assertI18nParity helper in every component test + i18n-parity-coverage.test.ts as the ATDD red specimen against ~35 new keys + `npm run i18n-parity` CI step wired into `.github/workflows/ci-web.yml` + Playwright bilingual-smoke spec walking /login /permission-denied /not-found /dashboard in BOTH locales with @axe-core/playwright full-page audits). Scope: AppLayout + Sidebar + TopBar + UserPill placeholders in components/shared/ (non-role-variant — Epic 1D Story 1d-3 will refactor); polished ErrorBoundary REPLACING 1-7b's minimal RootErrorBoundary with Sentry event ID + retry CTA + UX-DR16 three-part recovery; PermissionDenied with two body-copy variants (Owner+Admin / Owner-only) per UX-3 + UX spec §6.4; NotFound + `path: '*'` catch-all closing 1-7b's W1 deferral; LanguageToggle + `lib/language-cookie.ts` + `hooks/useLanguageInit.ts` cookie-driven UX-DR17 cross-subdomain handoff (dashboard half only — landing half ships with Story 1.10); ~35 new i18n keys with parity-enforced en + vi; vitest-axe + axe-core + @axe-core/playwright devDeps + axe.allowlist.json governance stub; four stub hooks (useAuth/useCurrentCenter/useRole stubs returning no-session shapes + usePolling real implementation for Story 1-9a verify poller). Scaffold reality: 1-7b RootErrorBoundary + `app.errorFallback` key DELETED; App.tsx rewires wrapper + adds useLanguageInit(); routes.tsx extends with /permission-denied + path:* catch-all; lib/i18n.ts rewires `lng` seed from cookie. Risk-score ≥6 check: ONE owned (R38). Out of scope (explicit deferrals): role-variant Sidebar / TopBar / UserPill / SearchPalette / Mobile Tab Bar → 1d-3; real useAuth / useCurrentCenter / useRole behavior → Stories 1-8 / 2-2 / 2-6; Astro landing + full classlite.app → my.classlite.app E2E navigation → Story 1.10; EmptyState → Epic 10 Story 10-3/10-4 (Path B); additional shadcn primitives → 1d-2.)
# previous_update_-4: 2026-06-10 (Story 1-7b transitioned review → done: code-review applied 16 patches across api-fetch / auth-refresh / query-client / sentry / location-stub / multi-tab bait + extended useQuery test coverage. Patch highlights: isAuthError used in QueryCache/MutationCache (was AuthExpiredError-only); double-401-after-refresh now throws AuthExpiredError instead of escaping as ApiError(401); onAuthFailure latched against double-fire; next= param compares pathname only (no self-redirect loop); parseEnvelope JSON.parse wrapped (captive portal HTML 200 now surfaces INVALID_RESPONSE); localStorage read/write guarded for QuotaExceededError + private mode; BroadcastChannel listener detach+reattach on test reset; navigator.locks.request rejection falls back to in-process refresh; BroadcastChannel message type guard; Sentry httpClientIntegration dropped (was double-emitting breadcrumbs alongside apiFetch); stubLocation returns restore() handle; route-bundle dev-route audit FAILS instead of skips when dist/ missing; MultiTabTestPage safe error handling; subtasks 11.9/11.10 unchecked with deferred-to-reviewer annotation; project-context TEST-FE-3 updated to sanction reset() action pattern. Vitest 100/100 (added useQuery → QueryCache.onError test), Playwright design-system 13/13, lint + build + grep gates all clean. One defer: NotFound catch-all route → Story 1-7c.)
# previous_update_-5: 2026-06-09 (Story 1-7b transitioned in-progress → review)
# previous_update_-6: 2026-06-09 (Story 1-7b transitioned ready-for-dev → in-progress)
# previous_update_-7: 2026-06-09 (Story 1-7b-app-shell-routing-and-state-management scaffolded backlog → ready-for-dev: comprehensive context engine covering React Router v7 library mode with three explicit lazy chunk boundaries (auth/student/teacher) + TanStack Query v5 cache-level onError 401 silent-refresh contract + UX-DR19 multi-tab navigator.locks + BroadcastChannel coordinator with `lastRefreshedAt` debounce inside lock callback (prevents refresh-token-reuse race that the lock alone does NOT close) + `apiFetch` envelope-unwrap helper with request_id propagation + @sentry/react init with request_id breadcrumbs + minimal `RootErrorBoundary` top-level class component + three Zustand stores (UI/editor/language) with exported initialState per TEST-FE-3 + ESLint guard banning raw fetch/axios in features/hooks; explicit migration of 1-7a `/__theme-resolution` dev route into router as DEV-gated lazy child preserving the existing Playwright design-system project; CQ-1 cleanup of Google Fonts CDN block in index.html. Risk-score ≥6 check: NONE owned (R38→1-7c, R39→1d-1 with 1-7a-style early-signal escalation, R45→1-7c, R46→DevOps); ATDD red phase skipped per WF-8 with inline Vitest+Playwright executable contracts covering AC2-AC8. Scope deferrals explicit: AppLayout/Sidebar/TopBar/PermissionDenied/NotFound/polished ErrorBoundary/language toggle+cookie/assertI18nParity helper/axe-core CI/cross-domain Playwright project — all 1-7c or later.)
# previous_update_-1: 2026-06-08 (Story 1-7a transitioned in-progress → review: design system + shadcn theme bridge + Fraunces/Geist Mono fontsource + tokens.css sync + stylelint/ESLint raw-hex enforcement + components/ui governance complete. Vitest 67/67 green, Playwright 8/8 green on design-system project, lint + tsc + build clean on both surfaces.)
# previous_update_0: 2026-06-08 (Story 1-7a transitioned ready-for-dev → in-progress)
# previous_update_1: 2026-06-08 (Story 1-7a-design-system-and-component-library scaffolded ready-for-dev + party-mode revision pass: Sally/Winston/Amelia/Murat reviewed, John ruled — AC3 corrected to Tailwind v4 import order with full :root mapping including all *-foreground pairings + .dark block retained per Winston's CQ-1 reread + Playwright 4-primitive theme-resolution matrix (Button/Input/Card/Dialog) replacing jsdom; AC2 swapped SHA-256 for sync + git diff --exit-code + automated counter-fixture; AC4 prescriptive @fontsource-variable Fraunces both surfaces + Vietnamese subset + typography-resolution spec + R39 early-signal note; AC5 integration-level lint-script sandbox test + 3-layer fixture exclusion; AC6 trip-wire escalation + [shadcn] commit-tag allowlist + components/ui/README role/state taxonomy with UX-DR2/UX-DR22 anti-patterns; AC7 --cl-ink-dot token retiring stylelint-disable + docs/lint-exceptions.md governance stub; epic-1c transitioned backlog → in-progress)
# previous_update_2: 2026-06-07 (Epic 1D re-scoped to Path B after party-mode review: 1d-1 foundation rewritten with 3-tier Rolldown kill-switch + preview-side deps + i18n parity CI step (R38 mitigation) + error-on-merge three-state lint; 1d-2 adds Toggle/ToggleGroup primitives + shape-semantic skeleton split; 1d-3 adds full TS Props interfaces; 1d-4 RENAMED from visual-status to phase4-visual-bridge (Sally+Mary synthesis — static visual shells of WriteDocSurface/WritingGradingSurface/SpeakingGradingSurface/AnchoredQuestionCard/MobileWritingSurface/InboxListShell/AnalyticsHomeShell, behavior defers to feature epics); 1d-5/1d-6/1d-7/1d-8 deferred to feature epics 2/3/8/9/10; pre-1d-1 gate: /bmad-tea TD re-run required per Murat)
# previous_update_3: 2026-06-07 (Epic 1D Component Library Buildout added: 8 stories 1d-1..1d-8 covering Storybook foundation + shadcn primitives + domain components for Phases 1-3 from 93-screen mockup set; Phase 4 behavior-heavy components deferred to feature epics 5/6/7; designer parallelization initiative — launch slip accepted; all 8 stories status=backlog)
# previous_update_4: 2026-06-07 (story 1-6 → review: Google OAuth init/callback + invite acceptance + force-logout implemented; all ATDD red-phase tests green; full regression + migrate round-trip clean)
# project: classlite_new
# project_key: NOKEY
# tracking_system: file-system
# story_location: "{project-root}/_bmad-output/implementation-artifacts"

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - review-blocked: Implementation shipped but a party-mode / peer review surfaced
#     block-merge items that must land before a code-review pass. Project-specific
#     extension introduced 2026-06-21 (Story 1d-3 party-mode review). Re-promotes
#     to `review` once block-merge tasks are merged.
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)
#
# RESTRUCTURING NOTE (2026-05-29):
# =================================
# Epic 1 split into 1A/1B/1C. Stories 1.2, 1.7, 1.9, 2.3 decomposed into sub-stories.
# New gap stories added: 1.2d-f, 1.3b, 2.7, 4.5, 8.5.
# Epic files now in _bmad-output/planning-artifacts/epics/ (sharded).
# See epics/index.md for full structure.

generated: 2026-05-28
project: classlite_new
project_key: NOKEY
tracking_system: file-system
story_location: "{project-root}/_bmad-output/implementation-artifacts"

development_status:
  # Epic 1A: Project Foundation
  epic-1a: in-progress
  1-1-monorepo-scaffold-and-infrastructure-setup: done
  1-2a-go-api-skeleton-and-middleware-chain: done
  1-2b-database-connection-health-endpoint-and-tenant-context: done
  1-2c-error-handling-and-config-system: done
  1-2d-email-service-abstraction: done
  1-2e-presigned-upload-infrastructure: done
  1-2f-event-tracking-foundation: done
  1-3-auth-database-schema-rls-and-security-testing: done
  1-3b-audit-logging-infrastructure: done
  epic-1a-retrospective: optional

  # Epic 1B: Authentication
  epic-1b: in-progress
  1-4-email-password-registration-and-email-verification-api: done
  1-5-login-session-management-and-password-reset-api: done
  1-6-google-oauth-and-invite-acceptance-api: done
  epic-1b-retrospective: optional

  # Epic 1C: Frontend Foundation & Landing Page
  epic-1c: in-progress
  1-7a-design-system-and-component-library: done
  1-7b-app-shell-routing-and-state-management: done
  1-7c-shared-layout-components-and-i18n: done
  1-8-auth-ui-registration-and-login-screens: done
  1-9a-email-verification-ui: backlog
  1-9b-password-reset-ui: backlog
  1-9c-invite-acceptance-ui: backlog
  1-9d-auth-error-and-recovery-states: backlog
  1-10-astro-landing-page: backlog
  epic-1c-retrospective: optional

  # Epic 1D: Component Library Buildout (Path B — trim + Phase 4 visual bridge)
  # Pre-dev gate: CLOSED 2026-06-15 (Murat — TD surgical refresh complete; R38 → 1d-1 AC4 discharge mapping confirmed in handoff; R39 promoted MONITOR → MITIGATE for Epic 1D scope; 3 new risks added R51/R52/R53; coverage matrix appended in-place to test-design-qa.md as "Epic 1D Refresh (2026-06-15)")
  epic-1d: in-progress
  1d-1-storybook-foundation: done
  1d-2-shadcn-primitive-coverage: done
  1d-3-app-shell-stack: done
  1d-4-phase4-visual-bridge: done
  # Story 1d-3 follow-ups (party-mode review 2026-06-21):
  1d-followup-codeowners-and-shell-allowlist-rule: backlog
  # Story 1d-4 follow-ups (code review 2026-06-24):
  1d-followup-tokenizer-migration: backlog # → replace classlite-web/scripts/lib/strip-comments-and-strings.mjs regex stripper with acorn or @babel/parser. Third regex-strip class of bug ate export declarations during 1d-4 (templates with `I'm`/`author's`, JSX attrs with `couldn't`); pass-order reorder is an interim fix.
  # Legacy stories (deferred to feature epics per Path B — files retained as input artifacts):
  1d-4-visual-status-domain: deferred-to-feature-epic # → epic-8 (dashboards/analytics) + epic-9 (billing)
  1d-5-shells-and-states: deferred-to-feature-epic # → epic-2 (onboarding shells) + epic-10 (empty/error states)
  1d-6-data-list-table: deferred-to-feature-epic # → epic-3 story 3-1 (first consumer) + epic-6 story 6-1 (GradingQueueShell)
  1d-7-drawers-modals-forms: deferred-to-feature-epic # → epic-2 story 2-3a (canonical RHF wrapper)
  1d-8-tabs-shell-and-calendar: deferred-to-feature-epic # → epic-3 stories 3-2/3-4/3-5 (with widened 2-day calendar spike per Winston/Murat)
  epic-1d-retrospective: optional

  # Epic 2: Onboarding, Center Setup & Roles
  epic-2: backlog
  2-1-onboarding-api-persona-selection-center-setup-and-save-resume: backlog
  2-2-class-template-and-spawning-api: backlog
  2-3a-onboarding-ui-persona-selection-and-center-setup: backlog
  2-3b-onboarding-ui-template-selection-and-class-spawning: backlog
  2-3c-onboarding-ui-completion-and-resume: backlog
  2-4-post-onboarding-checklist-and-first-ai-grade-card: backlog
  2-5-center-settings-and-google-meet-integration: backlog
  2-6-roles-permissions-and-authorization-enforcement: backlog
  2-7-bulk-student-import: backlog
  epic-2-retrospective: optional

  # Epic 3: Class Management & Scheduling
  epic-3: backlog
  3-1-class-crud-lifecycle-and-creation-ui: backlog
  3-2-class-detail-view-with-tabs: backlog
  3-3-class-templates-management: backlog
  3-4-schedule-workspace-and-session-management: backlog
  3-5-session-detail-and-attendance-recording: backlog
  epic-3-retrospective: optional

  # Epic 4: Exercise Authoring, AI Content Generation & Knowledge Hub
  epic-4: backlog
  4-1-exercise-library-and-crud-api: backlog
  4-2-exercise-editor-structure-questions-and-settings: backlog
  4-3-ai-content-generation-pipeline: backlog
  4-4-knowledge-hub-and-file-management: backlog
  4-5-jsonb-schema-migration-strategy: backlog
  epic-4-retrospective: optional

  # Epic 5: Assignments, Student Attempts & Submissions
  epic-5: backlog
  5-1-assignment-creation-and-submission-lifecycle-api: backlog
  5-2-quiz-attempt-interface-reading-listening-vocabulary: backlog
  5-3-writing-attempt-interface: backlog
  5-4-speaking-attempt-interface: backlog
  5-5-submission-result-view: backlog
  epic-5-retrospective: optional

  # Epic 6: Grading & AI-Assisted Grading
  epic-6: backlog
  6-1-writing-grading-with-anchored-comments: backlog
  6-2-ai-assisted-writing-grading: backlog
  6-3-speaking-grading-and-ai-assisted-speaking-grading: backlog
  6-4-auto-grading-reading-listening-vocabulary: backlog
  epic-6-retrospective: optional

  # Epic 7: People Management, Enrollment & Anchored Q&A
  epic-7: backlog
  7-1-staff-management-and-invitation: backlog
  7-2-student-lists-and-student-detail-view: backlog
  7-3-enrollment-management: backlog
  7-4-anchored-qa-system: backlog
  epic-7-retrospective: optional

  # Epic 8: Analytics, Dashboards & Search
  epic-8: backlog
  8-1-role-specific-dashboards: backlog
  8-2-analytics-home-and-class-performance: backlog
  8-3-student-performance-views: backlog
  8-4-global-search-cmd-k: backlog
  8-5-analytics-seed-data-script: backlog
  epic-8-retrospective: optional

  # Epic 9: Billing, Plans & Account Management
  epic-9: backlog
  9-1-plan-tiers-and-limit-enforcement: backlog
  9-2-upgrade-downgrade-and-ai-credit-add-ons: backlog
  9-3-payment-failure-grace-period-and-invoices: backlog
  9-4-user-profile-management: backlog
  epic-9-retrospective: optional

  # Epic 10: Inbox, Notifications, Archive & Polish
  epic-10: backlog
  10-1-role-scoped-inbox-and-notifications: backlog
  10-2-archive: backlog
  10-3-empty-states: backlog
  10-4-error-states: backlog
  epic-10-retrospective: optional
