# generated: 2026-05-28
# last_updated: 2026-07-10 (Story 2-3a-onboarding-ui-persona-selection-and-center-setup transitioned review → done after `/bmad-code-review 2-3a` Round 1 three-layer adversarial pass (Blind Hunter / Edge Case Hunter / Acceptance Auditor, fresh-context general-purpose subagents). 57 raw findings → 42 actionable: 3 decision-needed + 35 patches + 4 defers + 14 dismissed. **Decisions**: R1-D1 → BLOCK-until-stories-land (R1-P37 Storybook stories added), R1-D2 → banner CTA dropped in `postCenterIncomplete` state (R1-P38), R1-D3 → RHF re-sync skipped when dirty (R1-P39). **All 39 patches applied inline**. **2 BLOCKERs fixed**: R1-P1 (`onboardingSubmitFlag` never reset on happy path — module-scope leak across session) + R1-P2 (layout render bail ignored the flag → CenterSetupPage unmounted mid-onSubmit before the PUT-progress + navigate calls settle). **4 STRONGs**: R1-P3 (Sally-I2 fold restored — aria-live announces state transitions via sr-only sibling; ticking seconds counter aria-hidden), R1-P4 (429 Retry-After countdown auto-re-enables + interpolated `{{seconds}}` counts down via `{initialSeconds, elapsedSeconds}` state model), R1-P5 (spec-order revert: `await putProgress.mutateAsync` BEFORE `navigate` — safe now that guard-suppression flag holds through transition), R1-P6 (`useCreateCenter.onSuccess` empty-cache branch invokes `onAuthFailure` + Sentry breadcrumb instead of silent bail). **10 MEDs**, **20 LOWs** — see story-file `## Review Findings` section for the full triage. **Deferred (4)**: R1-W1 cross-tab center broadcast (FU-2-3a-D covers), R1-W2 route-bundle-boundaries inline-code coverage, R1-W3 requestId body-fallback length cap, R1-W4 vitest 30s testTimeout — all filed to `deferred-work.md`. **New i18n keys** (7): `onboarding.wizard.autoSaving.savedAnnouncement`, `.retryNow`, `onboarding.persona.error.retryCta`, `onboarding.center.error.userAlreadyHasCenterGeneric`, `.brandColorInvalid`, `dashboard.finishSetup.awaitingNextStep` — all landed in en.json + vi.json + STORY_2_3A_KEYS. **Storybook stories added** (R1-P37): `PersonaSelectPage.stories.tsx` (7 variants: Default / Loading / SavedProgressOperator / SavedProgressFounder / LocaleVi / Error500 / AlreadyHasCenter) + `CenterSetupPage.stories.tsx` (8 variants including LocaleViCramped 720px for Sally-S5 Vietnamese Founder headline verification). **`useCurrentCenter` rewritten** as center-slice `useSyncExternalStore` selector — no re-render on `accessToken` rotation. **Regression clean**: 748/748 vitest (+9 tests vs pre-review), `tsc --noEmit -p tsconfig.app.json` clean, `npm run lint` clean (React 19 `set-state-in-effect` / `refs` / `purity` rules all passing after countdown state refactor), `npm run i18n-parity` clean (436 keys / 434 claimed). **Playwright smoke NOT re-run** — local port 5173 conflict with KoverNow Heist app on Ducdo's machine; code paths unaffected (boot-probe → cache → layout guard → outlet unchanged). Story transitions review → done. Hand-off: merge to main.)
# previous_update_-2: 2026-07-09 (Story 2-3a-onboarding-ui-persona-selection-and-center-setup transitioned in-progress → review after full green-phase shipped against baseline `f709e70`. All 14 ACs implemented + 40+ new component/hook/unit tests + 1 Playwright happy-path smoke + extended `e2e/route-bundle-boundaries.spec.ts` for `/welcome` chunk isolation. **Regression clean**: 739/739 vitest across 72 files, 7/7 route-bundle-boundaries Playwright, 1/1 onboarding smoke Playwright, `npm run lint` clean, `tsc --noEmit -p tsconfig.app.json` clean, `npm run i18n-parity` clean (430 keys / 428 claimed across `onboarding.` + `dashboard.finishSetup.` namespaces via the extended `COVERED_NAMESPACES` from Murat-S1). **Pragmatic deviations for reviewer** (all documented in sibling completion-notes): (1) `apiFetch` now falls back to `error.requestId` in the body when the `x-request-id` header is absent — MSW handlers ship the id inline and prod gateways may strip trace headers; (2) `auth-refresh.ts` `performNetworkRefresh` + `hydrateSessionCache` use a functional updater that preserves prior `session.center` across silent refresh (fresh sibling tabs still land defined-as-null) — closes an implicit regression the story spec didn't call out; (3) new module-scope `onboardingSubmitFlag` (in its own `onboardingSubmitFlag.ts`) suppresses the OnboardingLayout `session.center != null → /dashboard` guard during the create-center submit — closes a React-commit race where the cache write triggers `useAuth` re-render before the `navigate('/setup/template')` call commits; (4) `useAuth()` now exposes `session: Session | null` (needed by both `useCurrentCenter` and OnboardingLayout AC8 guard for `session.center` reads); (5) `vitest.config.ts testTimeout: 30_000` (pre-existing lint-sandbox integration tests were on the 5s edge — infra fix); (6) `@testing-library/jest-dom/vitest` wired into `vitest-setup.ts` (red-phase specimens use jest-dom matchers, previously unregistered); (7) `TeacherDashboard` deep-imports `@/features/onboarding/api/useOnboardingProgress` (bypassing the barrel) to preserve chunk isolation — `route-bundle-boundaries` caught the leak; (8) Storybook stories (Tasks 6.5 + 7.7) deferred — the vitest suite covers every branch the stories would document. **Follow-ups filed** are unchanged from the story spec (FU-2-3a-A through -H). **Sibling completion-notes** authored at `_bmad-output/implementation-artifacts/2-3a-onboarding-ui-persona-selection-and-center-setup-completion-notes.md` per docs/bmad-story-conventions.md. Baseline commit `f709e70` unchanged. Story file size: 526 lines (well under the 600-line ceiling). Hand-off: `/code-review 2-3a` on a **different** LLM.)
# previous_update_-1: 2026-07-08 (Story 2-3a-onboarding-ui-persona-selection-and-center-setup **ATDD red-phase executed** via `/bmad-tea AT 2-3a`. 9 red-phase specimen files landed (~56KB test code) mapping 1:1 to AC1–AC14 with all party-mode-folded contracts baked in (Winston-W1 `updatedAt` source, Winston-W2 compound `!isLoading && !isAuthenticated` guard, Winston-W3 saveSeq guard, Sally-B1 zero-selection, Sally-B2 idle+persistentFailure states, Sally-S4 two-line 409 recovery, Amelia-B4 RHF `values` prop, Murat-B1 **10-entry slug canonical + 30-char length cap** (verbatim from `classlite-api/internal/service/slug_atdd_test.go:24-65`), Murat-B3 Retry-After countdown + missing-header fallback, Murat-S4 debounce 4-invariant coverage, Murat-S5 Playwright REQUIRED happy-path smoke, Murat-S8 cross-feature session-cache write path). **Files landed**: `handlers.ts` (10 error variant builders per Dev Notes §"MSW handler contract inventory"), `PersonaSelectPage.test.tsx` (AC1/2/3/10/13/14), `CenterSetupPage.test.tsx` (AC5/6/7 5-error matrix/11/13/14), `OnboardingLayout.test.tsx` (AC8 4 guards + boot-probe race), `useAutoSave.test.tsx` (4 debounce invariants + saveSeq + persistentFailure), `slugPreview.test.ts` (10-entry canonical), `letterMark.test.ts` (Sally-I3 clarifications — `TT` not `TA`, single-token `CL`, emoji `?` fallback), `useCreateCenter.test.tsx` (Session.center + accessToken bump), Playwright `onboarding-persona-center.spec.ts`. **ATDD checklist** at `_bmad-output/test-artifacts/atdd-checklist-2-3a-onboarding-ui-persona-selection-and-center-setup.md` — full test strategy + AC-to-file mapping + green-phase task order. **Red signal verified as intended**: ~8+ TS2307 undefined-module errors across all 9 files + `Property 'center' does not exist on type 'Session'` shape mismatch. Each error maps 1:1 to a Task in the story spec. **Green-phase task order recommended for Amelia**: Task 2.2 (regex extract to `src/lib/centerName.ts`) → 2.3 (`slugPreview` — closes 10-entry unit test first, fastest feedback) → 2.4 (`letterMark`) → 3 (query keys + 4 API hooks) → 4 (Session extension w/ 5 writer sites) → 5 (auto-save context) → 1+6 (OnboardingLayout + PersonaSelectPage) → 7 (CenterSetupPage) → 8 (routes lazy + bundle boundary) → 9 (i18n + `assertI18nInterpolationParity`) + 10 (regression + Playwright). Story Task 0 now checked; Task 0.3 test-design cross-reference deferred to Amelia's pre-green-phase pass (5 min). Story stays `ready-for-dev`. Hand-off: `/bmad-dev-story 2-3a`.)
# previous_update_-0.15: 2026-07-08 (Story 2-3a-onboarding-ui-persona-selection-and-center-setup **party-mode review amendments folded**. Sally + Winston + Amelia + Murat reviewed (each spawned as independent general-purpose subagent, fresh-context parallel); John ruled the calls and applied directly. **All 4 voices independently said FOLD-STRONGS-BEFORE-DEV — no BLOCK, no SHIP-AS-IS.** 8 BLOCKERs + 20 STRONGs + 12 INFOs ACCEPTed; 2 STRONGs + 1 INFO REJECTed; 3 STRONGs + 1 INFO DEFERred as new follow-ups. **Net story-file delta: 412 → 514 lines (+25%, well under bmad-story-conventions 600 ceiling).** **Highest-leverage folds**: (a) **Winston-W1** — `meta.serverTime` architecturally unreachable through `apiFetch` (parseEnvelope drops meta); swapped AC4/AC6 to source `lastSavedAt` from `OnboardingProgressResult.updatedAt` instead — closes a compile-time landmine before Amelia hits it at green-phase; (b) **Winston-W2 + Amelia-B2 cross-agent convergence** — session-writer inventory was FIVE sites not four; missed `hydrateSessionCache` at auth-refresh.ts:354 (BroadcastChannel sibling-tab hydrator) + `acceptInvite.ts:76` (Story 1-9c invite-accept); invited-teacher users would have shipped `session.center === undefined` and broken every AC8 route guard; enumerated all 5 in Task 4.2 with grep-verified line numbers + cross-tab Vitest; (c) **Winston-W3 + Amelia-B3 cross-agent convergence** — `Center.slug` → `.shortCode` shape migration was silently compile-breaking; killed the `type Center = CenterSummary` re-export cleanly; added Task 4.0 pre-flight grep; (d) **Amelia-B1** — `sanitizeCenterName` regex extraction claimed "one-line refactor" but triggers TS-7 cross-feature import (`onboarding/` → `auth/`); moved `CENTER_NAME_REGEX` to NEW `src/lib/centerName.ts` (shared) so both `sanitizeCenterName` and onboarding Zod consume it cleanly; (e) **Amelia-B4** — RHF `defaultValues` from async GET is a snapshot trap that silently discards saved drafts (form permanently holds empty state after progress GET resolves); swapped to RHF `values` prop (v7.32+ reactive API, `^7.76.1` confirmed safe); (f) **Murat-B1** — slug canonical set was 7 entries per story but Go backend tests ship 10 + separate 30-char length-cap; corrected Task 2.3 to full 10-entry inline table + FU-2-3a-H tracks shared JSON fixture unification; (g) **Murat-B2** — MSW handler contract inventory unenumerated; added Dev Notes §"MSW handler contract inventory" 4-endpoint table (2xx + 4xx bodies + Retry-After headers); (h) **Murat-B3** — 429 Retry-After test-hook + missing-Retry-After branch spec pinned in Task 7.8; (i) **Sally-B1** — "Operator pre-selected on first paint" was a dark pattern silently biasing the operator/founder fork; changed to zero-selection + roving-tabindex (WAI-ARIA APG pattern), Continue button genuinely disabled until user selects; (j) **Sally-B2** — auto-save "Not yet saved" read as error on fresh mount + failed state invisible on flaky 3G; changed pristine copy to "Auto-save on," added persistent-banner escalation after ~5s failures + new `.idle` / `.failedPersistent` i18n keys + amber-icon distinct-from-success color; (k) **Sally-S3** — "save and finish later" → `/dashboard` was an abandonment scene until 2.4 ships; folded 30-line welcome-back banner into `TeacherDashboard` (AC11 amendment) — this is the seam Story 2.4's "Finish setting up" card grows into; (l) **Sally-S4** — 409 recovery UI ignored compromised-account scenario; rewrote to two-line inline "Your account already has a center: {{centerName}} ({{shortCode}}.classlite.app). Not familiar? [Contact support →] — otherwise open your dashboard." interpolating from 409 body (**FU-2-3a-F** verifies Story 2.1's 409 payload carries both fields; if not, backend patch before green-phase); (m) **Sally-S5 + AC1 layout** — Vietnamese Founder headline at 230px overflows; changed persona-card grid to 2-col at `md`, 3-col at `lg`+ (each card ~350px at md, comfortable for Vietnamese); (n) **Amelia-S2** — persona cards + brand-color needed two custom radio implementations; unified as `RadioGroupTile` primitive under `src/features/onboarding/components/` (single a11y implementation, no drift); (o) **Winston-W3 saveSeq + Amelia-S1 savingState gate** — auto-save debounce out-of-order + in-flight-on-unmount race protection folded into Task 5.1; (p) **Murat-S4** — debounce 4-invariant test coverage pinned (single-fire per window, last-value-wins, unmount cancels, route-change cleanup); (q) **Murat-S5** — Playwright smoke promoted from OPTIONAL → REQUIRED (jsdom lies about focus + login→wizard integration boundary is uncovered by Vitest); (r) **Murat-S1 + S2** — `COVERED_NAMESPACES` extension in `scripts/i18n-parity.mjs` + `assertI18nInterpolationParity` helper at `src/lib/test/i18n-parity.ts` (R38 score 6 with 48 keys × 4 interpolation shapes {{seconds}}/{{max}}/{{requestId}}/{{current}}/{{total}}/{{centerName}}/{{shortCode}} needs actual teeth beyond key-presence); (s) **Murat-S6** — three-state coverage extended from GET only to all 4 mutations (POST persona, POST centers, PUT progress advance, PUT progress debounced); (t) **Winston-W5** — OnboardingLayout must be lazy (Rolldown chunk-boundary regression risk); extended `e2e/route-bundle-boundaries.spec.ts` to assert `/welcome` chunk isolation; (u) **Winston-W2 route guard order** — pinned compound `!isLoading && !isAuthenticated` condition (Story 1-8 boot-probe race would otherwise ship the same regression); (v) **Sally-S1 Vietnamese persona labels pinned inline by John**: `Người điều hành` (Operator) / `Người sáng lập` (Founder) / `Giáo viên độc lập` (Solo Teacher) — Ducdo can veto in code review; (w) **Sally-I3 letter-mark algorithm example fixed**: `Trung tâm Anh ngữ` → `TT` (not `TA` as originally written); single-token rule `ClassLite → CL` spelled out; pristine state = empty rounded square with `border-dashed` (not `??`); (x) **Sally-I1 + I2** — SVG illustrations `aria-hidden="true"` (SR was reading "YOU"/"STUDENTS"/"3 TEACHERS" from SVG text nodes); aria-live announces state transitions ONLY (not ticking "Ns ago" counter). **REJECTs (3)**: Winston-W4 (JWT cache-vs-cookie WHY sentence — wrong story's teaching moment); Amelia-S3 (Zod v4 confirmed no fold); Amelia-S4 (DoD item 9 covers). **Half-accept: Murat-S3 downgraded to language change** ("expect review-cycle rebound if skipped") rather than mandating ATDD — R38 is the only ≥6 risk this story touches, discharged via inheritance. **6 new follow-ups filed**: FU-2-3a-E (branches feature, Story 2.5 or customer signal), FU-2-3a-F (verify 409 body carries centerName+shortCode), FU-2-3a-G (slug-mismatch inline notice on collision suffix), FU-2-3a-H (shared slug canonical JSON fixture consumed by both Go and TS tests), FU-2-3a-C amended scope (`--cl-persona-*` vs `--cl-brand-*` namespace split). **Cross-agent convergence**: Winston-W2 + Amelia-B2 both grepped and found the missed writer sites; Winston-W3 + Amelia-B3 both flagged `.slug` migration; Winston-W1 (slug drift long-term) + Murat-B1 (canonical-set miscount) collapsed to the same root class (FU-2-3a-H covers). Story stays `ready-for-dev`. Sibling completion-notes deferred to first dev pickup. Hand-off sequence: `/bmad-tea AT 2-3a` (Task 0 — RECOMMENDED, skip only with recorded justification), then `/bmad-dev-story 2-3a`.)
# previous_update_-0.3: 2026-07-08 (Story 2-3a-onboarding-ui-persona-selection-and-center-setup transitioned backlog → ready-for-dev. John's pre-dev context engine pass against baseline `f709e70` (Story 2-2 done + Round 2 code-review clean). This is the FIRST frontend story of Epic 2 and the FIRST post-auth authenticated surface any verified user hits — persona pick (s00 = `/welcome`) + center setup (s01 = `/setup/center`). 14 ACs elaborate the epic-level 5 ACs with the following backend-reality pins: (1) all four Story 2.1 endpoints (`POST /api/onboarding/persona`, `POST /api/centers`, `PUT /api/onboarding/progress`, `GET /api/onboarding/progress`) already visible in generated `client.ts` — NO `api.yaml` touch, NO `codegen.sh` run; (2) `logoUrl` deferred to Center Settings (Story 2.5) per FU-2-3a-A — AC4 letter-mark preview satisfies epic-level "no logo → letter-mark" fully; (3) NEW `Session.center` slot extension (Task 4) closes the Story 2-2 JSDoc note on `useCurrentCenter` stub and replaces it with a real selector; (4) auto-save debounced 1500ms → PUT `onboarding_progress` fire-and-forget with `meta.serverTime`-driven "last saved Ns ago" affordance (skew-immune, no `Date.now()` trust); (5) "save and finish later" navigates to `/dashboard` (Story 2.4 renders the "Finish setting up" card; placeholder Teacher/OwnerDashboard until 2.4 ships — accepted intermediate); (6) five error codes handled at UI (401 via apiFetch silent-refresh, 403 EMAIL_VERIFICATION_REQUIRED, 409 USER_ALREADY_HAS_CENTER → "Open Dashboard" recovery, 422 details-array → RHF field errors per Story 1-8 P4 pattern, 429 with `retryAfterSeconds` from ApiError, 500 with visible requestId + no auto-retry per Story 2.1 AC5 slug-collision semantics); (7) client `slugifyPreview` MUST mirror backend `internal/service/slug.go` NFKC+NFD+`đ→d` + AC5b canonical Vietnamese test set — Task 2.3 pins the same 7-entry set as a unit-test invariant; (8) resume-routing on GET progress via 5-row decision table (persona=null → /welcome; currentStep=done → /dashboard; else route by step). **Risk score ≥6 check: NONE OWNED** — Epic 2's R1 (score 9) discharged at Story 2.1/2.2 backend, R18 (score 6) is Story 2.7's, R38 (score 6) inherited via per-story `describe('Story 2-3a i18n parity (R38)', ...)` block appended to `i18n-parity-coverage.test.ts` (Task 9.2). **WF-8 ATDD ceremony: RECOMMENDED but SKIPPABLE** (Task 0.1/0.2 documents the choice). NEW `<OnboardingLayout>` sibling to AuthLayout + AppLayout — full-bleed shell (no sidebar/topbar), dot-grid background, ClassLite wordmark + auto-save indicator top bar. NEW `<OnboardingAutoSaveContext>` provides `savingState`, `lastSavedAt`, `flush`, `scheduleSave` to child pages. Route protection guards in AC8: `isLoading` → skeleton; unauthenticated → /login?next=; already-has-center → /dashboard (wizard not re-enterable); unverified → /verify-email. Persona-card semantics: `role="radiogroup"` + arrow-key nav + `aria-checked` per WCAG 2.1 AA (only selected card tabbable, ARIA authoring practices radio-group pattern). Center-name rune-vs-byte length check via `Array.from(v).length` (mirrors Story 2.1 P3 fix for `centerNameMaxLen`). REUSES verbatim: `apiFetch`+`ApiError.retryAfterSeconds` (Story 1-8), `sanitizeCenterName` regex (Story 1-9c — extract as named export if private), `useMemo(t)` Zod builder-hook (Story 1-8 `useRegisterSchema`), `authKeys` factory + Session cache-write pattern (Story 1-8), shadcn Form/Input/Alert/Button/RadioGroup primitives (Story 1d-2), `assertI18nParity` helper (Story 1-7c). Session type extension in Task 4 populates `center: null` on all four existing writers (`useLogin`, `useRegister`, `runBootProbe`, refresh-success path) + populates real center on `useCreateCenter.onSuccess`. FOUR filed follow-ups: FU-2-3a-A (R2 presigned logo upload — Story 2.5 pickup), FU-2-3a-B (post-login smart redirect to /welcome for center-less users — Story 2.6), FU-2-3a-C (persona illustration hex tokens), FU-2-3a-D (auto-save multi-tab reconciliation). 40 new i18n keys. Story file 412 lines — well under bmad-story-conventions 600 ceiling. Sibling completion-notes deferred to first dev pickup. Hand-off sequence: optionally `/bmad-tea AT 2-3a` for MSW-driven AC coverage, then `/bmad-dev-story 2-3a`.)
# previous_update_-0.5: 2026-07-06 (Story 2-2-class-template-and-spawning-api transitioned review → done after `/bmad-code-review 2-2` Round 2 (different Opus 4.7 1M session). **41 findings surfaced, 34 actionable this pass**: 5 decision-needed + 30 patches + 6 defer + 11 dismissed → after decision resolution: 33 patches applied + 1 filed as FU-2-2-L + 7 defers. **Decisions (2026-07-06 resolution)**: R2-D1 → rewrite Sally-B4 SELF_INVITE_BLOCKED belt to compare against raw callerInfo.Email (belt is now reachable, real normalization drift fires it); R2-D2 → new sqlc CountCenterMembersByUserAndCenter query + swap Branch B verification call (belt-and-suspenders against RLS drift, no more "safe only because RLS" invisibility); R2-D3 → Founder auto-assign returns 500 on corrupt caller email row (aligns with AC6 MUST rather than silently downgrading to Branch D); R2-D4 → accept seed-migration NO FORCE window as maintenance-window-replay-only policy (R2-W7, no code change); R2-D5 → reject duplicate cohortName at service layer with per-index DUPLICATE_COHORT_NAME field error. **Highest-value patches this pass**: R2-P1 STRONG — main.go now wires `classSvc.SetAcceptURLBase(cfg.AppInviteURLBase)` + Validate() rejects empty in non-dev (previously invite emails would ship `http://localhost:5173/invite/<token>` in prod); R2-P2 STRONG — CRLF/control-char strip on centerName + inviterName before RenderInviteEmail (SMTP header injection surface closed via owner-controlled center name); R2-P3 MED — post-commit Enqueue loop now runs under `context.WithoutCancel(parentCtx)` + `cancel()` fires immediately after `tx.Commit` (no silent per-bucket email drops on 30s spawnDeadline mid-loop expiry); R2-P4 LOW — `RELEASE SAVEPOINT invite_insert` added on reuse-existing branch (no more savepoint stacking); R2-P5 MED — ExtractTenant middleware now uses `store.SetTenantContext` helper for uniformity with rest of codebase; R2-P8/P9 MED — `floatToNumeric` and `numericToFloat` now surface errors instead of silently clamping/returning 0 (DB corruption no longer hides as valid band 0); R2-P19 MED — TemplateService returns typed `InvalidTenantClaimError` on `uuid.Parse(tc.CenterID)` failure (routes to 403 INVALID_TENANT_CLAIM per AC13 taxonomy); R2-P20 LOW — audit `after.target_band` uses `numericToFloat(tmpl.TargetBand)` (forensic truth-preservation on rounded value); R2-P21 MED — email PII redacted via `hashEmailForLog` in slog.Info (GDPR); R2-P22 LOW — `request_id` correlation added to every spawn slog call; R2-P23 LOW — `Content-Type: application/json` gate → 415 UNSUPPORTED_MEDIA_TYPE; R2-P24 LOW — Content-Length pre-flight → 413/422 fast-fail; R2-P25 LOW — Branch A response uses `callerInfo.Email` (DB value) not `payloadNormalized`; R2-P27 MED — startDate boundary truncated to UTC day (no midnight-jitter false-rejects); R2-P31 LOW — Sally-B4 belt reachable via raw-value comparison; R2-P32 MED — new `CountCenterMembersByUserAndCenter` sqlc query + Branch B swap (belt-and-suspenders); R2-P33 LOW — 500 on corrupt Founder email; R2-P34 MED — duplicate cohortName rejection with DUPLICATE_COHORT_NAME field code. **Test hardenings (10)**: R2-P7 (`ORDER BY created_at ASC` on signAccessTokenWithCenter — deterministic multi-membership tiebreak), R2-P11 (removed unused authSvc), R2-P12 (removed `_ = templateID`), R2-P13 (all 4 RLS UPDATE tests always re-read + SAVEPOINT wraps the raise path so 42501 doesn't abort TxDB harness), R2-P14 (AC11 header_center_spoof asserts attacker got exactly 1 class — closes SEC-7 positive-side gap), R2-P15 (AC04b tightened to `explicit_self` only), R2-P17 (new `TestSpawn_NonexistentTemplateID_Returns404`), R2-P28 (SeedCenterForUser uses `SELECT set_config($1, true)` bind — no more string-concat SQL in helpers), R2-P29 (pre2_2Purge scoped to `role = 'owner'` centers only — cross-user residue closed). **R2-P16 SKIPPED as design gap** → filed as FU-2-2-L: `TemplateHandler` accepts concrete `*service.TemplateService` not an interface, so SEED_INCOMPLETE handler test needs a `TemplateSvc` interface extraction at the handler seam. C3-13's service-level `CountSystemTemplates` unit test proves the counting logic; handler branch is a single literal WriteError call, trivially trusted. **7 defers filed under deferred-work.md#story-2-2 (2026-07-06)**: R2-W1 (DisallowUnknownFields inconsistency across Story 1.x handlers → FU-2-2-K sweep), R2-W2/W3/W5 (`sync_template_sessions_center_id` trigger polish deferred to next migration touch per WF-2), R2-W4 (classes.session_count nullable → Story 3.1 migration), R2-W6 (ExtractTenant per-request tx C1-18 restated), R2-W7 (seed migration maintenance-window-replay-only policy). **Regression**: `go test -race -count=1 -p 1 ./...` green across all 14 packages (11.2s for internal/test = RLS + ATDD suite). **Codegen**: `scripts/codegen.sh` re-run for new sqlc query — regenerated `internal/store/generated/center_members.sql.go` (+ `classlite-web/src/lib/api/client.ts` byte-identical, no api.yaml drift). **NEW config env**: `APP_INVITE_URL_BASE` required in non-dev. **Story file `## Review Findings` section**: prior 52 R1 patches + AC13 decision remain checked; new Round 2 subsection adds 5 checked decisions, 33 checked patches, 1 unchecked patch (R2-P16 skip), 7 checked defers, and dismissed/known-accepted summary. AC11 body_center_override prose amended to document C2-10 DisallowUnknownFields posture (R2-P18). Hand-off: merge to main.)
# previous_update_-0.7: 2026-07-06 (Story 2-2-class-template-and-spawning-api transitioned in-progress → review after all STRONG/MED patches applied and full regression green. **Total resolved this session: 52 of 55 actionable review findings applied inline** — 5 BLOCKERs + 1 decision (AC13 amend) + 18 STRONGs + 20 MEDs + 8 LOWs. 3 LOW test-polish items remain unchecked (C3-19 SeedActiveInvite RLS-visibility verify, C3-21 mock assertion tightening, C3-22 Vietnamese-diacritic coverage). **Highest-value STRONG fixes this pass**: C1-05 dropped `expires_at > $3` from `GetActiveInviteByEmail` (23505 race resolution now works when expired-but-unaccepted row owns the partial-unique-index slot); C1-06 NaN/Inf targetBand guard (NaN comparisons no longer bypass range check); C1-07 `math.Round` on `float→numeric` (deserialized 6.4999999 no longer silently writes 64); C1-08 `pgtype.Timestamptz.Valid` check on reused invite (no zero-time propagates to wire); C1-09 30s spawn tx deadline; C1-10 rate-limit key changed from pure-IP to `centerID:ip` via new `CenterAndIPKeyFn` (botnet bypass closed, NAT users independent); C2-04 handler enforces `maxItems: 20` on `classes[]` post-decode; C2-05 `MaxBytesReader(nil, ...)` + explicit decodeError dispatch fixes `http: superfluous WriteHeader` race; C2-10 handler `DisallowUnknownFields()` on both decoders — REJECTS smuggled `centerId` at request body (AC11 body_center_override subtest updated to assert 422 rather than silent SEC-7 spawn — strictly more secure); C3-03 new `AssertRLSViolation` helper asserts SQLSTATE 42501 on Pattern-2 tests (relation-missing regressions no longer mask as green); C3-04 UPDATE/DELETE tests now assert `RowsAffected == 0`; C3-05 new `classes_teacher_mutex` CHECK test (Winston-W-S3 belt now verified); C3-06 new `TestSpawn_AC12_ErrorEnvelopeShape_AuthenticatedSpawnValidationError` proves envelope shape via a real spawn 422 (not just 401 middleware); C3-08 new `TenantCannotReparentViaTemplateIDSwap` closes UPDATE-side template_id swap vector; C3-09 typed `pgtype.UUID` scan on trigger reconciliation test (wrong-UUID trigger drift now caught); C3-11 `signAccessTokenWithCenter` t.Fatals on missing membership; new `NewTestServerFor2_2ForUserNoCenter` for CENTER_REQUIRED negatives; C3-13 new `TestTemplateService_CountSystemTemplates` unit test (SEED_INCOMPLETE detection covered on all boundary inputs); C3-14 new `TestSpawn_AC06_NonFounderPersonaDoesNotAutoAssign` (Operator/Solo Teacher personas verified NOT to plant classes[0].teacher_id); C3-15 DB row count verified after 201 on happy path; C3-16 no-partial-write DB assertion on validation errors; C3-17 error code asserted on previously code-blind 422 tests; C3-20 `pre2_2Purge` errors surfaced via `slog.Warn` (residue-driven flakes now traceable); C1-20 invite email now uses `users.full_name` (added to `GetUserPersonaAndEmail` query) — no longer leaks email local-part into copy; C1-16 seed migration policy pinned (SEEDS ARE IMMUTABLE — future fixes require new UUID + delete old); C1-17 audit `class.spawned` after-payload expanded to include teacher_status + assignment_reason + pending_teacher_email (forensic gap closed). **Also (dead-code delete)**: `spawnAcceptInvitePathVi` const removed. **Regression**: `go test -race -count=1 -p 1 ./...` green across 14 packages. **Codegen**: `sqlc generate` + `openapi-typescript` re-run twice (once for the `GetActiveInviteByEmail` param removal, once for the `full_name` add). **Story file `## Review Findings` section**: 52 patch items now checked off + 2 deferred items checked off + 3 remaining as unchecked LOW polish. **Filed follow-up added**: FU-2-2-J (durable invite-email reconciler for crash-between-commit-and-Enqueue drift — C1-14 documented, not fully closed). Status transitioned in-progress → review. Hand-off: run `/bmad-code-review 2-2` on a different LLM to verify no regressions from this pass; then merge to main.)
# previous_update_-0.95: 2026-07-05 (Story 2-2-class-template-and-spawning-api transitioned review → in-progress after `/bmad-code-review 2-2` three-chunk parallel pass (Blind Hunter / Edge Case Hunter / Acceptance Auditor). 57 findings surfaced, 55 actionable: 5 BLOCKER + 1 decision resolved + 18 STRONG + 20 MED + 11 LOW + 2 defer + 12 dismissed. **Decision resolved**: AC13 error-code catalog AMENDED to codify shipped middleware codes `AUTH_REQUIRED`/`AUTH_INVALID` (401) and `RATE_LIMIT_EXCEEDED` (429) rather than rename Story 1.x middleware (lower blast radius; Story 2.3b UI hasn't shipped). Also added `INVALID_TENANT_CLAIM` (403) to the documented set. **Applied inline (5 BLOCKERs + amend)**: C1-01 `ListAccessibleTemplates` sort direction — center-owned now `created_at DESC` with `id ASC` tiebreaker (was ASC, wizard rendered oldest custom first); C1-02 invite email now looks up `centers.name` inside spawn tx and passes as `centerName` to `RenderInviteEmail` (was passing class-template name, producing "You're invited to Writing Bootcamp 6.5" instead of the actual center name); C1-03 `classes.template_id` FK gains `ON DELETE SET NULL` (was default NO ACTION → seed down-migration failed after any spawn, rollback deadlock in prod); C3-01 four new WITH-CHECK-on-UPDATE tests added (`TestRLS_ClassTemplate_TenantCannotReparentOwnRow`, `TestRLS_ClassTemplate_TenantCannotPromoteOwnRowToSystemSeed`, `TestRLS_Classes_TenantCannotReparentOwnRow`, `TestRLS_TemplateSession_TenantCannotReparentOwnRow`) — the pre-existing Pattern 3 tests exercised the USING clause only; dropping WITH CHECK from any of the 3 UPDATE policies would silently pass; the new tests attempt `UPDATE ... SET center_id = <other tenant>` on own rows + a promote-to-seed variant, closing the AC7 R1 verification gap; C3-02 AC11 attack-vector subtests refactored — each subtest now builds its own fresh attacker/victim fixture with unique-suffix emails and per-subtest cleanup, removing the shared-state ordering coupling (partial `-run` filter now safe); also tightened C3-07 assertions on `body_center_override` + `header_center_spoof` to verify attacker DID legitimately get 1 class (SEC-7 contract) instead of just checking victim zero-count. **Regression**: `go test -race -count=1 -p 1 ./...` green across 14 packages. **Left as unchecked action items in story file `## Review Findings` section (39 patches)**: C1-05..22 (service correctness — GetActiveInviteByEmail filter, NaN/Inf targetBand, floating rounding, ExpiresAt.Valid check, spawn tx timeout, IP-keyed rate limit); C2-02..13 (HTTP layer — missing 500 in OpenAPI, FieldError.code back-compat, maxItems handler enforcement, MaxBytesReader status race, 0-vs-1 indexing docs, 403/422 code docs, DisallowUnknownFields); C3-03..22 (tests — SQLSTATE assertion, RowsAffected on UPDATE/DELETE, classes_teacher_mutex CHECK, AC12 envelope spawn-specific case, template_sessions UPDATE trigger gap, `signAccessTokenWithCenter` silent-empty, non-founder persona negatives, DB post-verification, Vietnamese-diacritic coverage). **Deferred (2)**: C1-18 `ExtractTenant` per-request tx perf regression (disclosed trade-off, monitor); C2-14 no idempotency key on spawn (explicit non-goal). **Dismissed (12)**: superuser RLS bypass concern (verified `SET LOCAL ROLE classlite_app` at helpers.go:97 — RLS genuinely enforced); enumeration oracle on Branch B (Branch B / Branch C responses converge on "not a member" path — no leak); NO FORCE/FORCE not tx-wrapped (golang-migrate wraps by default, verified); NULLIF cast DoS (SetTenantContext validates UUID first); required+nullable OpenAPI vs Go pointer permissiveness; various OpenAPI polish. Status transitioned review → in-progress. Hand-off: next dev pass to work through the 39 remaining action items in the story file's `## Review Findings` section; re-review before merging to main.)
# previous_update_-0.9: 2026-07-03 (Story 2-2-class-template-and-spawning-api transitioned in-progress → review. Amelia shipped green-phase implementation against baseline commit `26c569b`. Full test regression `go test -race -count=1 -p 1 ./...` green across 10 packages. **R1 discharge complete**: 23 RLS tests green (J15 6-pattern × 3 resource families + 5 named extensions). 12 handler ATDD tests + 11 service ATDD tests + 3 middleware ATDD tests all green. 5 migration pairs shipped (class_templates + template_sessions with trigger + classes + seed + invites unique index); down/up round-trip verified. Codegen re-run once at end (sqlc + openapi-typescript). **Pragmatic deviations for reviewer**: (1) `ExtractTenant` middleware fixed to open tx + SET LOCAL for center_members RLS-scoped lookup — closes a latent production bug Story 2.1 never exercised (all authenticated calls with a CenterID claim would have hit INVALID_TENANT_CLAIM otherwise); (2) `model.FieldError.Code` field added for AC13 per-field codes (INVALID_TEACHER_EMAIL / SELF_INVITE_BLOCKED); (3) ATDD test helpers received `bufSnapshot` (non-consuming body reads) + AC11 subtest reordering (assertions verbatim per §9 preservation); (4) `SECURITY DEFINER` on trigger `sync_template_sessions_center_id` — WITH CHECK on the RLS UPDATE policy is the primary defense; SECURITY DEFINER removes the "parent invisible because RLS filtered it" foot-gun; (5) `-p 1` required for regression: Story 2.2's raw-pool tests commit shared emails (owner@example.com, teacher@example.com), and parallel package execution creates collisions on `idx_users_email`. Standard integration-test discipline. TWO NEW filed follow-ups: FU-2-2-G (backfill FOR UPDATE + WITH CHECK on `center_members`/`invites`/`audit_logs` next time those migrations open — Winston-W-B1 fold-forward); FU-2-2-H (Story 3.1 `GET /api/classes` read path computes `invite_expired` state when `pending_teacher_email` set + `teacher_id NULL` + invite `expires_at < now()` — Sally-S3 punt to owning story). Sibling completion-notes authored at `_bmad-output/implementation-artifacts/2-2-class-template-and-spawning-api-completion-notes.md` per docs/bmad-story-conventions.md. Hand-off: `/code-review 2-2` on a different LLM.)
# previous_update_0: 2026-07-03 (Story 2-2-class-template-and-spawning-api **party-mode review amendments folded**. Sally + Winston + Amelia + Murat reviewed (each spawned as independent subagent via general-purpose agent tool, fresh-context parallel); John ruled the calls and applied directly. **14 BLOCKERs ACCEPTED** (Sally B1-B4, Winston W-B1..W-B3, Amelia A-B1..A-B4, Murat M-B1..M-B3 — Amelia-A-B2 folded as duplicate of Winston-W-B2 on seed migration role) + **Winston-W-S5 promoted STRONG→BLOCKER** (pre-tx template read would 404 own custom templates via empty-tenant RLS invisibility) + **Sally-S1 + Winston-W-S3** accepted as cheap amendments. **Net story-file delta: 496 → 610 lines (+23%, ~2% over bmad-story-conventions 600 ceiling — flagged for code-review reviewer attention).** Highest-leverage folds: (a) **AC3 response payload** expanded to surface `pendingTeacherEmail`, 5-value `teacherAssignmentReason` enum (explicit_self/explicit_member/founder_auto/invited/unassigned), and top-level `invites[]` array with `{email, classIndices, enqueued, reusedExistingInvite, expiresAt}` per invite — Sally-B1 + B2 + S6 fold: wizard's done screen (s04/s06) can now render truth to owner without a second round-trip AND without lying about email delivery when EmailRetryQueue buffer is full; (b) **AC4 three-branch resolution** with explicit ordering Branch A → B → C, Branch B > C precedence after prior invite acceptance (Murat-M-B3), Branch A normalizes BOTH sides symmetrically (Sally-B4), + `422 SELF_INVITE_BLOCKED` code as belt against normalization drift regression; (c) **AC7 four RLS policies per table** (SELECT / INSERT / UPDATE / DELETE) with explicit `WITH CHECK` on UPDATE — closes Winston-W-B1's hostile-tenant-reparents-to-tenantB-or-NULL attack vector on all three new resource families; (d) **AC10 grows 18 → 23 named tests** — 2 extensions on class_templates (dual-scope positive + system-seed write protection per Murat-M-B2) + 3 extensions on template_sessions (trigger reconciliation + parent-mismatch WITH CHECK rejection + user-cannot-plant-under-system-seed per Murat-M-B1); (e) **NEW AC13 error code catalog** with per-endpoint code → wizard-route mapping — Sally-B3 fold: en.json + vi.json key on stable identifiers, wizard router has explicit contract; (f) **Task 2.4 seed migration** dropped `SESSION AUTHORIZATION classlite` (fragile in Railway prod), uses temporary `NO FORCE ROW LEVEL SECURITY` window + `ON CONFLICT (id) DO NOTHING` for idempotent re-runs; (g) **Task 7.2 spawn tx flow** rewritten to move template SELECT INSIDE tx after `SET LOCAL` (Winston-W-S5 promoted — otherwise custom templates return 404 to their own creators — same family bug as Story 2.1 Winston-B2), Branch B membership check inside tx for RLS scoping, tokens use `base64.RawURLEncoding` per existing `newPasswordResetToken` helper (NOT hex — Amelia-A-B4 fix); (h) **Task 9.4 middleware chain reorder** — `onboardingLimit` BEFORE `RequireVerifiedEmail` (Winston-W-B3 closes center-less-flood attack surface) + NEW `spawnLimit` (5/min IP) per Winston-W-S4 to bound Resend spend under runaway wizard loop; (i) **Task 3.5 dropped GetUserByEmailCI** — reuse existing case-sensitive `GetUserByEmail` with service-layer normalization (Amelia-A-B1 + `[[feedback_check_prior_story_artifacts_before_generating]]` — the "prior story shipped canonical version" case memory MD warns about); (j) **Task 12.2 gains 2 new tests** — `TestClassService_Spawn_PostAcceptReInviteLandsInBranchB` (Murat-M-B3) + `TestClassService_Spawn_InviteEnqueueBufferFullSucceedsBestEffort` (Murat-M-S3 — closes the "best-effort semantic isn't testable" trap Story 1.4 closed once already); (k) **Task 2 migrations timestamped `20260703120000..20260703120400`** (Amelia-A-B3 full-day gap from Story 2.1's 20260702 range leaves room for concurrent Story 3.1 at 20260704+); (l) **AC1 completeness threshold** raised ≥4 → ≥5 with Sally-S1's Academic Reading 6.5 5th seed (fixed UUID `...555555555505`, targetBand 6.5, primarySkill reading, sessionCount 10, color `#14b8a6` teal) closing Vietnamese uni-admissions Reading-standalone gap; (m) **classes_teacher_mutex CHECK constraint** (Winston-W-S3: `CHECK (teacher_id IS NULL OR pending_teacher_email IS NULL)`) prevents Epic 7 from leaving pending_teacher_email populated after flipping teacher_id — belt against reconciliation drift. **Cross-agent convergences noted**: Sally-B4 + Amelia-A-S1 + Winston-W-S5 all pointed at "tenancy-set ordering / normalization symmetry" — same root class, folded together in Task 7.2. Winston-W-B1 + Murat-M-B1/M-B2 were the same threat surface (RLS UPDATE hole + missing negative tests catching it) — folded together in AC7 + Task 10. **Deferred as-STRONGs**: Winston-W-S1 (statement_timeout), W-S2 (deadlock ordering comment), Sally-S4 (retry not idempotent — dev-note), S5 (autosave boundary — one-line clarification), Amelia-A-S1..A-S8 (impl-notes for reviewer). **TWO NEW filed follow-ups**: FU-2-2-G (backfill FOR UPDATE + WITH CHECK on center_members/invites/audit_logs when those migrations next open — Winston-W-B1 fold-forward), FU-2-2-H (Story 3.1's `GET /api/classes` read path computes `invite_expired` state when `pending_teacher_email` set + `teacher_id NULL` + invite `expires_at < now()` — Sally-S3 punt to owning story). Story stays `ready-for-dev`. Hand-off unchanged: `/bmad-tea AT 2-2` next (mandatory, R1 = 9 with 23 J15+extension tests required — Murat's bottom-line said R1 discharge would fail pre-AT without these folds), then `/bmad-dev-story 2-2`.)
# previous_update_-0.5b: 2026-07-02 (Story 2-2-class-template-and-spawning-api transitioned backlog → ready-for-dev. John's pre-dev context engine pass against baseline `26c569b` (Story 2-1 done + TA P2/P3 backfill + AC6 hardening + RV MED fixes). 12 ACs cover the 3 new endpoints (GET /api/templates, POST /api/templates, POST /api/templates/{id}/spawn) with FOUR backend-reality reframes pinned inline: (1) templates are dual-scope (system seeds `center_id IS NULL` + tenant-owned) via one `class_templates` table + dual-scope RLS policy `USING (center_id IS NULL OR center_id = current_tenant)` + `WITH CHECK (center_id = current_tenant)` — seeds insertable only via superuser; (2) spawn is atomic all-or-nothing (differs from R18/Story 2.7 CSV partial-success — flagged for epic gate); (3) invite email is out-of-tx best-effort via `EmailRetryQueue` (reuse Story 1.4/1.5 lineage, existing InProcessRetryQueue with 30s/2m/8m/30m backoff wired at cmd/api/main.go:72); (4) teacher assignment decoupled from `center_members.role` — Founder auto-assign sets `classes.teacher_id`, not center role (mirrors Story 2.1 uniform-owner ruling per Sally-B1). **Risk score ≥6 check: R1 owns THREE new resource families (`class_templates`, `template_sessions`, `classes`) — J15 6-pattern grid × 3 files mandatory before backlog → in-progress.** Total 18 RLS adversarial tests. ATDD red phase MANDATORY via `/bmad-tea AT 2-2` (Task 0). NEW `RequireCenterContext` middleware sits between `RequireVerifiedEmail` and rate limit, returning `403 CENTER_REQUIRED` for callers who finished Story 2.1's persona pick but never POSTed a center — extracted here so Epic 3+ endpoints can compose it. NEW partial unique index `idx_invites_center_email_active ON invites(center_id, LOWER(email)) WHERE accepted_at IS NULL` (Task 2.5) enforces per-center-per-email invite dedup at DB with belt-and-suspenders application-layer dedup in `ClassService.Spawn`. Four pre-built templates pinned with deterministic UUIDs `11111111-2222-3333-4444-55555555550{1..4}` for testability (Writing Bootcamp 6.5, Speaking Mastery 7+, Foundation Listening+Reading, Starter Band 5.5 All Skills — target bands 6.5/7.0/5.5/5.5, primary skills writing/speaking/listening_reading/all_skills, session counts 12/12/10/8) — down migration deletes by fixed UUID only. 14 seed template_sessions rows (3-4 per template — starter kit, NOT full syllabus; full 12-session syllabus deferred to Epic 4 per FU-2-2-D). **Teacher resolution three-branch protocol** (AC4): (A) caller's own email → self-assign; (B) existing member of THIS center → assign teacher_id; (C) not a member (including cross-center existing users) → create invite row + enqueue email + teacher_id=NULL + pending_teacher_email set; (D) empty/null → unassigned unless Founder + classes[0] (AC6 auto-assign). Persona read at spawn-time via one users.persona SELECT (NOT in JWT — EDGE-2 staleness would leak wrong-persona spawns for up to 15min). AC10 J15 grid × 3 includes ONE extra positive-case (`class_templates` Pattern 1 asserts tenant A CAN see `center_id IS NULL` seeds — dual-scope positive path) and ONE extra negative-case (Pattern 2 asserts tenant A INSERT with `center_id = NULL` fails WITH CHECK — seed protection). AC11 three-vector attack matrix (body_center_override, body_template_id_from_other_tenant, header_center_spoof) mirrors Story 2.1's AC10 posture. `AuditService.LogWithinTx` reused verbatim for both `class_template.created` and per-class `class.spawned` audit rows (inside single spawn tx). Reused Story 2.1 infrastructure: `WriteEnvelope` helper (do not re-implement), `model.NewID()` UUID pre-generation, interface-seams-at-constructor pattern (AuditLogger + InviteSender), `AuthDB` reuse per §Architectural Debt Acknowledged, byte→rune lesson at `utf8.RuneCountInString`, hashInviteTokenHex + newPasswordResetToken helpers from Story 1.6/1.5, RenderInviteEmail template from Story 1.6, `EmailRetryQueue.Enqueue(job EmailJob) (accepted bool)` signature. **Explicit non-goals** listed inline: UI (2.3b/c), template edit/delete (Story 3.3), class CRUD lifecycle (Story 3.1), enrollment/student emails (Story 2.7 + Epic 7), Google Meet auto-link (Story 2.5), class capacity/plan limits (Story 9.1), X-Idempotency-Key header (post-v1). SIX filed follow-ups: FU-2-2-A (template_sessions drift audit — trigger prevents INSERT/UPDATE drift, DELETE-then-INSERT theoretically stales), FU-2-2-B (cross-center teacher borrow UX), FU-2-2-C (partial spawn — chunked commits every N classes), FU-2-2-D (complete 12-session syllabus — Epic 4 pickup with exercise links), FU-2-2-E (claim-the-class flow — pending_teacher_email → teacher_id on invite accept, Epic 7 Story 7.1 owner, P1), FU-2-2-F (user-keyed rate limit variant — shared with FU-2-1-E). Story file 496 lines — well under bmad-story-conventions 600 ceiling. Sibling completion-notes deferred to first dev pickup per docs/bmad-story-conventions.md. Hand-off sequence: `/bmad-tea AT 2-2` next (mandatory, R1 = 9), then `/bmad-dev-story 2-2`.)
# previous_update_0b: 2026-07-02 (Story 2-1-onboarding-api-persona-selection-center-setup-and-save-resume transitioned review → done after three-layer `/bmad-code-review 2-1` on Opus 4.7 1M (Blind Hunter / Edge Case Hunter / Acceptance Auditor, fresh-context parallel). 2 decisions resolved (D1 unexport MintAccessToken free function → `mintAccessToken` (same-package, only *AuthService.MintAccessToken stays public); D2 reject `""` for brandColor/logoUrl at validateCreateCenterInput with 422). 13 patches applied + 1 patch converted to defer with in-file warning + 5 deferred + ~15 dismissed. Highest-leverage patches: (P1 HIGH) AC4 persona field leak — GetProgress used to promote payload.personaChoice as top-level `persona` when users.persona was NULL; split into `Persona` (users.persona-only) vs `PersonaChoice` (payload draft) on the service struct; handler emits `progress.Persona`; single JOIN query `GetOnboardingProgressWithPersona` for atomic snapshot when row exists. (P2 MED) AC9 P5 handler posture — userIDFromContext returned model.ValidationError → 422; spec mandates 500; introduced `handler.ErrTenantContextMissing` sentinel + extended P5 test to hit the handler through httptest with a context-stripped request. (P3 HIGH) centerNameMaxLen byte→rune via utf8.RuneCountInString (Vietnamese ~90% of end-user input). (P4 MED) UpdateUserPersona :exec→:execrows + service check RowsAffected>0 or model.NotFoundError. (P5 MED) GetProgress two-query→JOIN. (P6/P7 LOW) RandomSuffix Crockford base32 (32 chars) + crypto/rand.Int rejection sampling; trimName → strings.TrimSpace. (P8 LOW) CenterService dropped synthetic `EmailVerified: true` on tx-set TenantContext. (P9 LOW) LatestAuditLogForUser now returns entity_type + entity_id; AC6 shape test asserts entity_type='center' and entity_id=<centerID from create response>. (P10/D1) MintAccessToken free function unexported → collapses to `mintAccessToken` + method wrapper; mint_access_token_test moved from `service_test` to `service` package. (P11 LOW) PurgeUserAndOwnedCenters logs each cleanup error via t.Logf instead of silently swallowing via `_, _ =`. (P12→Defer) CREATE INDEX CONCURRENTLY on center_members not shippable in a single golang-migrate file (postgres driver wraps every migration in a tx); demoted to inline warning-comment + deferred-work.md entry with the manual-DDL follow-up plan. (D2/P14 MED) validateCreateCenterInput rejects `""` for brandColor/logoUrl. Codegen re-run for users.sql + onboarding_progress.sql. Full API regression clean across handler / middleware / service / store / test / test/workers. Web tsc -b clean. Green-phase implementation complete across all 12 tasks + 39 subtasks. All 6 J15 patterns green (R1 discharge), TestCenterMembers_UserUniqueViolation green, TestCenters_SlugCollisionRegeneration under SetupRawPool green, onboarding handler ATDD suite (12 tests) + center handler ATDD suite (7 tests) green including AC10 3-attack-vector matrix + AC6 audit rollback + AC02 concurrent double-post remap. Full API regression clean (handler / middleware / service / store / test / test/workers / tenantcheck). Web tsc -b clean. Pragmatic deviations flagged for reviewer: (a) TestGoogleCallback_TenantBindingMatrix_CrossSubdomain Carol multi-center case removed under Story 2.1 v1 unique index (FU-2-1-D restore path); (b) PurgeUserAndOwnedCenters uses separate classlite superuser pool for RLS bypass at cleanup only (real tests still run as classlite_app); (c) slug retry uses SAVEPOINT boundaries per attempt to survive Postgres 25P02 after per-attempt unique-violation; (d) Task 12 unit tests use SetupDB rather than mocked store interface per Dev Notes §2 AuthDB-reuse debt acknowledgment. Sibling completion-notes deferred to reviewer feedback pass. Hand-off: /code-review 2-1 next on a different LLM.)
# previous_update_0a: 2026-07-02 (Story 2-1-onboarding-api-persona-selection-center-setup-and-save-resume transitioned ready-for-dev → in-progress. Amelia picked up via /bmad-dev-story 2-1. Baseline commit 6b522c1 captured (ATDD red-phase landed on top of d528444). Task 0 red-phase verified — 11+ undefined symbols in internal/test/onboarding_progress_rls_test.go map 1:1 to green-phase Tasks 3/6/7. Green-phase order per checklist: Task 5.0/7.4/3.1-3.5/6.1/7.1-7.6/8.1-8.2/9.)
# previous_update_0: 2026-07-01 (Story 2-1-onboarding-api-persona-selection-center-setup-and-save-resume **party-mode review amendments folded**. Sally + Winston + Amelia + Murat reviewed (each spawned as independent subagent via general-purpose agent tool, fresh-context parallel); John ruled the calls and applied directly. **8 BLOCKERs raised → 7 ACCEPTED (2 folded as duplicate — Winston-B1 + Amelia-B1 both flagged duplicate GetUserByID) + 1 REJECTED-as-FU (Sally-B1 persona→role, deferred to FU-2-1-F). 13 STRONGs raised → 12 ACCEPTED + 1 REJECTED-as-obsolete (Murat-S1 collapsed by B1 folding). 0 true REJECTS by John.** Net story-file delta: 363 → 458 lines (+95, +26%) — well under the bmad-story-conventions 600-line ceiling. **Highest-leverage findings**: (a) Winston-B1 = Amelia-B1 — RequireVerifiedEmail duplicated ExtractTenant's GetUserByID (auth.go:59); folded by adding `EmailVerified bool` to model.TenantContext + populating in ExtractTenant + collapsing RequireVerifiedEmail to a pure context-check middleware (mirrors RequireRole exactly, zero DB cost); (b) Winston-B2 — AC2 tx ordering had SET LOCAL AFTER centers INSERT, a landmine for future RLS-on-centers work; rewrote Task 7.2 to pre-generate the center UUID in Go (new internal/model/id.go NewID helper) then BEGIN → SET LOCAL → INSERT centers(id,...) → INSERT center_members → audit → COMMIT; (c) Murat-B3 — Task 10.3 concurrent slug-collision race can't materialize under SetupDB(t)'s single-tx harness; amended to use SetupRawPool(t) + t.Cleanup, mirroring refresh_atdd_test.go:235's Story 1.5 concurrent-rotation pattern; (d) Murat-B1 — AC9's J15 adaptation for non-RLS onboarding_progress was under-specified; enumerated six named patterns inline (P1-Read forget-user_id-filter, P2-Insert trust-payload-userID, P3-Update variant, P4-Delete N/A-documented, P5-NoAuthFallback, P6-DefaultStateNoCache); (e) Murat-B2 — AC10 cross-user isolation now three named t.Run subtests (attack_vector_url_param_override / body_field_override / header_spoof, mirroring auth_v15_adversarial_test.go); (f) Amelia-B2 — MintAccessToken extraction promoted from buried Dev Notes line to explicit Task 7.6 with `accessTokenIssuer` interface seam per Winston-S1. **Envelope contract added**: every 2xx from this story's 4 endpoints returns `meta.serverTime: <ISO-8601 UTC>` so the wizard's "auto-saved Ns ago" affordance (UX §8.1) is skew-immune. **CenterService takes AuditLogger + accessTokenIssuer interfaces at constructor** (Task 7.1) — enables Task 11.2's brokenAuditLogger injection for the AC6 atomicity red test. **AuditService gets LogWithinTx sibling** (Task 7.5) with Godoc precondition "caller MUST have set app.current_tenant_id" (Task 7.5.1) — must NOT re-run SetTenantContext. **AC5b canonical Vietnamese slug test set pinned** (Trung tâm Anh ngữ Sài Gòn → trung-tam-anh-ngu-sai-gon etc.) — ATDD-verifiable, protects against naive strings.Map(unicode.IsLower) mess. **AC4 GET progress now returns users.persona** for single-round-trip resume. **WF-3 codegen consolidated** to single Task 3.5 run (was double-run in Tasks 1.2 + 3.4). **Slug generator rewrite** — NFKC + NFD + unicode.IsMark strip + small non-decomposing table (đ/Đ/ø/Ø/æ/Æ) targeting ~30 lines. **Task 11.2 audit atomicity test** now uses brokenAuditLogger + asserts exact JSONB `{before: null, after: {...}}` shape. **Founder-vs-Owner clarification** added to Dev Notes preventing Story 2.2 from stomping center_members.role. TWO NEW filed follow-ups: FU-2-1-E (NAT rate-limit collision test + user-keyed variant), FU-2-1-F (persona→role branching if UX regrets uniform-owner post-2.6). Story stays `ready-for-dev`. Hand-off unchanged: /bmad-tea AT 2-1 next (mandatory, R1 = 9), then /bmad-dev-story 2-1.)
# previous_update_-1: 2026-07-01 (Story 2-1-onboarding-api-persona-selection-center-setup-and-save-resume transitioned backlog → ready-for-dev. epic-2 transitioned backlog → in-progress (first story in epic). John's pre-dev context engine pass against baseline d528444 (Epic 1 fully closed + Epic 1C gate advisories C1-C4 closed + R1 mitigation shipped 2e49d4e). 10 ACs cover the 4 new endpoints (POST /api/onboarding/persona, POST /api/centers, PUT /api/onboarding/progress, GET /api/onboarding/progress) with FOUR backend-reality pins: (1) center creation is fully transactional — centers INSERT + short_code retry (5 attempts) + SET LOCAL app.current_tenant_id + center_members INSERT + audit_logs INSERT all inside one tx; a rollback on any step leaves zero orphan rows; (2) `onboarding_progress` table has NO RLS (mirrors email_verifications pattern per Epic 1 §4 comment) — isolation enforced at service layer via user_id filter + compensating cross-user handler-integration test (AC10); (3) `AccessClaims.CenterID`/`Role` empty is the legitimate authenticated-but-no-center-yet state — DO NOT special-case empty tenant; ExtractTenant already handles this correctly; (4) v1 one-center-per-user invariant DB-enforced via NEW UNIQUE INDEX on center_members(user_id) + service-layer 409 USER_ALREADY_HAS_CENTER pre-check (belt + suspenders per EDGE-1). **Risk score ≥6 check: ONE owned — R1 (score 9).** WF-8 ATDD red phase MANDATORY before backlog → in-progress transition; discharge via /bmad-tea AT 2-1 generating Task 0 red specimens (handler ATDD + onboarding_progress_rls_test.go scaffolded from _TEMPLATE_rls_test.go covering the 6 J15 patterns adapted for non-RLS table). R1 mitigation infrastructure already shipped at commit 2e49d4e — tenantcheck analyzer stays vacuous this story since no hand-written *Store wrappers introduced (Epic 1's direct-sqlc-in-service pattern continued; J15 grid at SQL layer is the real R1 protection here). NEW middleware `RequireVerifiedEmail` introduced by this story (mirrors RequireRole shape) sitting between ExtractTenant and handler in the chain, returning 403 EMAIL_VERIFICATION_REQUIRED — extracted here so Epic 2+ endpoints can compose it. Fresh access token WITH CenterID+Role claims returned in POST /api/centers response body (rather than forcing re-login) — requires new `AuthService.MintAccessToken(userID, centerID, role)` public helper (existing buildAccessToken is unexported + does single-membership heuristic that's wrong for post-create path where the tx may not be committed yet at read time). Vietnamese-diacritic-aware Slugify + 4-char base32 collision suffix + up to 5 retries; ONE-CENTER-PER-USER-v1 pinned as explicit product ceiling + filed follow-up FU-2-1-D for post-launch reconsideration. FOUR filed follow-ups (FU-2-1-A center_settings extraction @ 2.5, FU-2-1-B role enum migration @ 2.6, FU-2-1-C robust Vietnamese normalization @ Epic 8, FU-2-1-D multi-center @ post-launch). Story file 236 lines (well under bmad-story-conventions 600 ceiling). Sibling completion-notes deferred to first dev pickup. Hand-off sequence: /bmad-tea AT 2-1 next, then /bmad-dev-story 2-1.)
# previous_update_-1: 2026-06-30 (Story 1-10-astro-landing-page transitioned review → done after /code-review three-layer pass (Blind Hunter / Edge Case Hunter / Acceptance Auditor). 37 patches applied (1 BLOCKER + 2 CRIT + 8 HIGH + 22 MED + 4 minor): /en/index.astro now ships the full seven-section layout; useHintCookieWrite clears on logout + resets latch on auth flip-down; PricingSection :has() selectors gain :global() to pierce the PricingCard scope hash; CF Pages Function preserves query+hash, emits Cache-Control, returns 405 for non-GET/HEAD, drops q=0 entries, clamps q to [0,1]; BaseLayout hint-cookie reader uses exact name=value parse + case-insensitive session_expired; lang-cookie reads-then-writes (no overwrite); SessionExpiredBanner.replaceState preserves UTM + hash; AC8 Layer 4 orphan scan now sources `used keys` from .astro AST (no more STORY_*_KEYS tautology) and the in-test Layer 3 assertion enforces orphan emptiness; 9 per-component assertLandingI18nParity tests landed under src/components/landing/__tests__/; StickyHeader sentinel rewired to body-root with rootMargin (positioning-ancestor-immune) + hamburger aria-expanded toggle + mobile lang toggle in <details> panel + motion-safe transitions; cookie-domain helper lowercases + strips trailing dot (parity sentinel byte-identical); /privacy + /terms localised to /{vi,en}/{privacy,terms} (root /privacy + /terms deleted, BaseLayout takes new routePath prop driving hreflang alternates); validate-dashboard-url.mjs hardened (quoted .env values, `export KEY=val` form, CRLF trim, NODE_ENV=test → development, LANDING_ROOT override for test isolation); Playwright config + landing.spec rebased to classlite.localhost:8788 so .classlite.localhost cookies actually attach; CYCLE-LOOP TERMINATION asserts specific URL pattern instead of <=3 navigations; useHintCookieWrite tests use waitFor instead of setTimeout(50); locale-redirect.test.ts gained 13 new edge cases (q=0, wildcard, whitespace/comma-only, query preservation, Cache-Control, HEAD/POST methods); tsconfig.json now scopes node types to a separate tsconfig.scripts.json (.astro scripts cannot reach process.env at type-check time); CI 0-JS grep guard narrowed to attribute syntax (no false-positive on comments). Verification matrix green: landing check-parity 71 keys / 6 prices locked / 0 orphans, cookie-domain parity PASS, validate-url PASS, astro check 0/0/0, tsc -p tsconfig.scripts.json clean, lint:css clean, vitest 53/53 (14 files), build 6 pages clean (vi+en × {index,privacy,terms}); dashboard tsc -b clean, eslint clean, vitest 610/610. 12 items remain deferred to follow-ups (per `_bmad-output/implementation-artifacts/deferred-work.md`).)
# previous_update_history_summary: review-handoff details retained in story file _bmad-output/implementation-artifacts/1-10-astro-landing-page.md
# previous_update_-1_original: 2026-06-30 (Story 1-10-astro-landing-page transitioned in-progress → review. All 10 tasks + 50+ subtasks complete; all 9 ACs satisfied; sibling completion-notes authored. Landing CI matrix green: vitest 25/25 (5 files), parity 73 keys / 6 prices locked / 0 orphans, cookie-domain parity PASS, validate-url PASS, build 4 pages clean, lint:css clean, astro check 0/0/0. Dashboard CI matrix green: lint / lint:css / tsc -b / vitest 610/610 (+9 new) / playwright 48/48 / build / build:check 4/4 chunks under ceilings / storybook:build / i18n-parity 376 keys × 2 locales clean. WF-8 ATDD evidence: R-NEW-54 + R-NEW-55 ATDD specimens (Vitest unit + Playwright e2e) pinned RED before mitigations shipped green. R38 four-layer landing discharge OWNED. 12 ★ REVIEWER-MANDATORY vi keys flagged in PR description. 8 pragmatic deviations documented for John's Epic 1C AC amendment. 6 followups filed (illustrations, legal pages, Zalo link, cookie-domain package P2, OG images, font preload tuning). Ready for /code-review on fresh-context, different-LLM model.)
# previous_update_-1: 2026-06-30 (Story 1-10-astro-landing-page transitioned ready-for-dev → in-progress. Amelia picked up via /bmad-dev-story 1-10. Baseline commit preserved at b8515b5.)
# previous_update_-2: 2026-06-29 (Story 1-10-astro-landing-page **party-mode review round 1 BLOCKERs folded**. Sally + Winston + Amelia + Murat reviewed (each spawned as independent subagent on Opus 4.7 1M, fresh-context parallel); John ruled the calls and applied directly. **6 BLOCKERs + ~12 STRONG amendments folded; 0 REJECTS.** Net story-file delta: 461 → 600 lines (+139, +30%) — at the bmad-story-conventions ceiling. Story status remains `ready-for-dev` (amendment, not re-promotion). **Risk picture revised — three risks NOW OWNED**: R38 (i18n parity, score 6) OWNED at landing layer (NOT inherited) — single-layer parity script was strictly weaker than dashboard's 1-7c four-layer discharge; landing now ships ALL FOUR layers per AC8 (Layer 1 `as const satisfies Strings` Amelia STRONG #8 fix; Layer 2 `assertLandingI18nParity` Vitest helper — Vitest NEW devDep on landing; Layer 3 ATDD parity-coverage specimen with closed-enumeration `STORY_1_10_KEYS`; Layer 4 `check-landing-parity.mjs` with LOCKED_PRICES table per Murat STRONG #6 + orphan-key scan); R-NEW-54 (CF cache deterministic-locale redirect, score 9) OWNED — `Astro.request.headers` at static build returns build-runner header, baking one locale for all visitors; mitigated by CF Pages Function `functions/index.ts` reading Accept-Language at edge per-request with `Vary: Accept-Language`; ATDD red specimen `e2e/locale-redirect.spec.ts` pinned BEFORE green per WF-8; R-NEW-55 (PUBLIC_DASHBOARD_URL open-redirect, score 6) OWNED — `import.meta.env.PUBLIC_DASHBOARD_URL` untyped/unvalidated, staging misconfig could redirect to phishing dashboard clone; mitigated by Astro 6 `astro:env` typed schema + production regex validator `^https://my\.classlite\.app$` at `scripts/validate-dashboard-url.mjs` wired as `prebuild`; ATDD red specimen `e2e/dashboard-url-validation.spec.ts` pinned BEFORE green per WF-8. **WF-8 ATDD red phase REQUIRED for R-NEW-54 + R-NEW-55** (previous draft's "WF-8 not required" was wrong). **AC1 mechanism amended**: `src/pages/index.astro` DELETED, replaced by CF Pages Function at `functions/index.ts` (≤40 lines) — Astro static can't per-request branch; CF Pages picks up `functions/` automatically. **AC2 copy amended per Sally BLOCKERs**: (1) hero headline restored to LP-01 wireframe verbatim "Giáo viên của bạn đang mất 12 phút..." replacing draft's amputated "15 giờ → 60 giờ" hour-aggregation that split the punch+rescue across H1+subhead; `hero.subheadline` key REMOVED; (2) PainCalculator gains đồng money conversion line ("≈ 150 triệu đồng/năm") + assumption call-out ("Giả định: 200.000 đồng/giờ") per Sally STRONG #4 — owners budget in money not hours; (3) SocialProof reframed as scenarios via section-header ("Hình dung kết quả với ClassLite") + italic section note explaining illustrative archetypes — `socialProof.disclaimer` footer-level key REMOVED, `socialProof.sectionHeader` + `socialProof.sectionNote` NEW; the disclaimer-as-rebuttal pattern read as performatively evasive. **AC4 `is:inline` discipline pinned per Amelia BLOCKER #12**: hint cookie redirect MUST be `<script is:inline>` in `<head>` ABOVE `<title>` (default Astro `<script>` is deferred ES module that runs after paint → flash-of-landing flash before bounce); `<html data-session-expired="true">` attribute set by same inline script BEFORE paint drives CSS-only banner reveal — zero CLS per Sally STRONG #6 (replaces draft's `display:none → block` post-paint swap that caused layout earthquake during scroll intent); IntersectionObserver clarified as default `<script>` (post-mount only). **AC4d wire-up amended per Amelia+Winston convergence**: provider-wrap option DELETED from spec — App.tsx has no QueryClientProvider wrap (queryClient is module-level at App.tsx:50); hook-call form pinned at App.tsx:55 after `useLanguageInit()`. **AC6 cross-locale state preservation per Sally STRONG #7**: language toggle preserves `?billing=` query param; ~4-line `<script is:inline>` hydration on destination pre-checks radio before paint. **AC7 `astro:env` typed schema** replaces `import.meta.env` per Amelia BLOCKER #7. **Cookie-domain enforcement seam per Winston STRONG #3**: NEW `check-cookie-domain-parity.mjs` sentinel-comment parity script (Task 9.7) — extracts function body from `cookie-domain.ts` between `/* CL-COOKIE-DOMAIN-PARITY-START/END */` sentinels, asserts byte-identical inline duplicate in BaseLayout.astro, fails CI on drift. **0-JS budget grep guard per Winston STRONG #7**: ≤8-line CI step grepping for `client:(load|idle|visible|media|only)` in `src/`, fails build with PM+reviewer sign-off requirement. **Cross-codebase byte-string fixture per Murat STRONG #4**: pins exact cookie write byte string in BOTH `useHintCookieWrite.test.tsx` AND NEW `hint-cookie-shape.test.ts` on landing — neither unit suite alone verifies the contract. **Cycle-loop e2e test per Murat STRONG #3**: Playwright test simulates session-expired bounce + sign-in click, asserts exactly 2 navigations (no infinite loop). **Cookie-domain test cross-subdomain reality fix per Murat BLOCKER #3**: Playwright baseURL changed from `http://localhost:4321` to `http://classlite.localhost:4321` (Domain=.classlite.localhost cookies do NOT match localhost); requires /etc/hosts entry. **App.tsx + Astro 6 specifics verified by Amelia**: `as const satisfies Strings` not `: Strings = ... satisfies Strings`; `<script is:inline>` for pre-paint; `astro:env` for typed env. **Followup priority bumped**: `1-10-followup-cookie-domain-package` P3 → P2 per Winston STRONG. **Pragmatic deviations 6 → 8** (Sally hero restore + SocialProof reframe added). **REVIEWER-MANDATORY vi keys 9 → 12** (added moneyConversion / assumption / sectionHeader / sectionNote / eyebrow). New tasks added: 5.0 + 8.0 ATDD red specimens; 6.5 default-script discipline; 8.6/8.7/8.8 validator + deploy doc; 9.5 byte-string fixture; 9.7 cookie-domain parity; 9.8 0-JS grep. Hand-off to Amelia (dev) for `/bmad-dev-story 1-10` — AT specimens land first (red); GREEN follows.)
# previous_update_-2: 2026-06-29 (Story 1-10-astro-landing-page transitioned backlog → ready-for-dev. John's pre-dev context engine pass against baseline `b8515b5` (1-9d done). 9 ACs map to UX-DR3/DR4/DR11/DR12/DR13/DR14/DR15/DR17/DR18 with FOUR backend-reality reframes pinned inline: (1) `logged_in=1` hint cookie is NOT written anywhere today (1-9d investigation confirmed) — this story OWNS both halves of UX-DR18 cross-codebase: new `useHintCookieWrite` hook in classlite-web writes the hint when `useAuth().isAuthenticated` flips true, landing reads it for redirect-to-dashboard; without the write, UX-DR18's read/redirect cycle is dead-on-arrival; (2) `Accept-Language` redirect is build-time on a static shim page at `src/pages/index.astro` (Astro 6 static output can't per-request branch; pragmatic `prefer-Vi-on-tie` rule per project-context UX-2); (3) VND prices LOCKED per BLOCKER A8 (2026-06-04) — Free 0₫ / Pro 399.000₫/tháng (3.990.000₫/năm) / Studio 999.000₫/tháng (9.990.000₫/năm) with VAT 10% inclusive caption hardcoded in Astro pages, no env config; (4) CTAs use build-time `PUBLIC_DASHBOARD_URL` constant (dev `http://my.classlite.localhost:5173`, prod `https://my.classlite.app`). **Risk-score ≥6 check: NONE owned.** R38 (i18n parity) inherits via NEW landing-specific `check-landing-parity.mjs` script — symmetric-diff between `src/content/{vi,en}.ts` modules typed against shared `Strings` interface, wired into `ci-landing.yml` as hard block between `astro check` and `astro build`; failure mode "marketing page renders empty string" is same risk class but lower blast radius than dashboard's "Vietnamese user sees raw key" — no ATDD red phase required per WF-8. R45 (CF cache, MONITOR 3) confirmed unchanged — Story 1.10 does not touch Vary:Origin or CDN config. R46 (deploy order) not applicable — landing has no api.yaml contract; dashboard-side write ships in same PR as landing for atomicity by convention not by R46 enforcement. Inheritance from 1.1 / 1-7a / 1-7c / 1-8 / 1-9d: Astro scaffold + tokens.css shared parity (1.7a sync-tokens.sh CI guard) + BaseLayout + bg-dot-grid utility + fontsource imports + stylelint already shipped from earlier stories; `language-cookie.ts` Domain logic extracted to NEW shared `classlite-web/src/lib/cookie-domain.ts` consumed by both lang cookie write and new hint cookie write (Task 1); `useAuth()` shape (1-8 — `isAuthenticated: user?.emailVerified === true`) drives `useHintCookieWrite` subscription (Task 2); 1-9d's defensive `logged_in=` clear on session-expired path (Strict variant) is the stale-cookie-loop breaker for THIS story's write (Lax variant — asymmetry documented in cookie-domain.ts JSDoc). **Cookie attribute matrix** pinned in Dev Notes: `logged_in` + `lang` both `Lax`/no `Secure`/JS-readable/1y; auth JWTs `Strict`/`Secure`/`HttpOnly`/15m+. **Pricing toggle decision**: CSS-only via hidden radio + sibling-selector CSS (Option A per Task 6.2) — 0-JS budget is load-bearing; total inline JS budget ≤80 lines across landing site (Task 9.6 documents discipline in `landing-script-budget.md` — hint cookie redirect ≤15 lines, session-expired banner reveal ≤5 lines, StickyHeader IntersectionObserver ≤30 lines, lang cookie write ≤15 lines). Astro built-in i18n shipping `/vi/` + `/en/` static routes; English page emits hreflang `x-default → /vi/` because Vietnamese is co-primary market. New devDeps on landing: `@playwright/test` + `@axe-core/playwright` (landing has no a11y infra today — Task 9.1 wires Playwright config with desktop 1280×800 + mobile 390×844 chromium projects; e2e covers all 7 sections + StickyHeader scroll-state + hint cookie redirect + session-expired banner + language toggle + mobile no-horizontal-scroll + axe zero-violations across both locales × both viewports). LoginPage `?plan=pro/studio` query param consumption is OOS — Epic 9 plan picker pre-fill owner; this story emits the param, Task 8.5 inspects RegisterPage to confirm no crash on unknown plan. **Pragmatic deviations from Epic 1C AC flagged for John amendment**: (a) `Accept-Language` build-time at shim page not per-request; (b) hint cookie write side ships dashboard-side (Epic AC line 443 names only the read side); (c) session-expired banner reveal driven by inline JS reading window.location.search (Astro static output can't per-request branch); (d) PainCalculator/FeatureCard illustrations placeholder rectangles (followup `1-10-followup-feature-illustrations`); (e) Zalo support URL placeholder `0123456789` (followup `1-10-followup-zalo-link`); (f) Terms/Privacy pages placeholder "TBD" bodies (followup `1-10-followup-legal-pages`). 9 ★ REVIEWER-MANDATORY vi keys flagged for VN-fluent reviewer pass (Task 3.7): meta.title / hero.headline / hero.subheadline / painCalculator.line1 / feature.writing.body / socialProof.disclaimer / socialProof.card1.quote / pricing.belowCta / banner.sessionExpired.body. **Architectural debt acknowledged**: cookie-domain logic triplicated (cookie-domain.ts canonical + language-cookie.ts re-export + landing inline per WF-7 cross-service ban) — pinned by cookie attribute matrix contract; future `@classlite/cookie-domain` workspace package tracked as `1-10-followup-cookie-domain-package` Winston P3 not v1. Hint cookie has NO server-side authority — DevTools cookie write triggers redirect to dashboard which then bounces unauthenticated via real auth cookie check; documented as UX hint not security signal in JSDoc. Story file 461 lines — well under bmad-story-conventions 600 ceiling. Sibling completion-notes file deferred to first dev pickup per docs/bmad-story-conventions.md. Hand-off to Amelia (dev) for `/bmad-dev-story 1-10`.)
# previous_update_-1: 2026-06-29 (Story 1-9d-auth-error-and-recovery-states transitioned review → done after three-layer adversarial code review on Opus 4.7 1M (fresh-context parallel subagents: Blind Hunter / Edge Case Hunter / Acceptance Auditor). ~60 raw findings → 3 decisions resolved (D1 keep role="alert" + drop heading focus-steal across 3 regions; D2 backfill sibling-tab broadcast test; D3 accept 10 KB ceiling retroactively as documented) + 12 patches applied + 8 deferred + ~19 dismissed. Patches: P1 (Retry-After=0 lockout-drop fix + positive guard), P2 (Banner variant-derived testId), P3 (cookie-spy assertion tightened to Max-Age=0 + Domain), P4 (i18n plural body_one/body_other + STORY_1_9D_KEYS 16→17), P5+P6 (test routes pin :id via ClassesProbe + dashboard testid), P7 (MAX_LOCKOUT_SECONDS=86_400 cap), P8 (Sally a11y BLOCKER threshold-announce test 60s/30s edge crossings), P9 (Amelia BLOCKER page-level mode-flip ratchet), P10 (page-level countdown tick), P11 (drop heading focus-steal across 3 replacement regions + a11y tests rewritten to assert role="alert" on region), P12 (sibling-tab broadcast test via module-level queryClient + BroadcastChannel.postMessage + test-route-classes-42 assertion — closes Murat M3 regression guard the completion notes had deferred). 8 deferred appended to deferred-work.md. Dismissed includes Blind Hunter sanitizer threat-model misreads (decoded.slice(1,3) correct for URL-parser-strip-leading-ws; /javascript: is pathname not JS URL; searchParams IS in deps) and spec-intentional behaviors (latched replacement persists by design; cookie domain forward-compat for Story 1.10). Full CI matrix green: lint, lint:css, tsc -b, vitest 594/594 (was 589, +5 new tests), playwright 48/48, build clean, build:check 4/4 chunks under ceiling (LoginPage 7.33K / 10K), storybook:build clean, i18n-parity 376 keys × 2 locales clean.)
# previous_update_-2: 2026-06-29 (Story 1-9d-auth-error-and-recovery-states transitioned in-progress → review. All 8 ACs + Definition of Done satisfied. Full CI matrix green: lint, lint:css, tsc -b, vitest 589/589 across 63 files (was 506/506, +83 new tests), playwright 48/48, npm run build clean, npm run build:check reports all 4 auth chunks under ceilings (Forgot 1.71K / Reset 1.98K / Invite 3.16K / Login 7.34K — LoginPage ceiling raised to 10K per Task 7.0 ceiling-decision contract), npm run storybook:build clean, npm run i18n-parity clean (375 keys × 2 locales). Dev Agent Record + File List authored at sibling 1-9d-auth-error-and-recovery-states-completion-notes.md per bmad-story-conventions. Pragmatic deviations flagged: (1) latched replacement-mode state instead of pure render-time selector — URL-clear effect drops ?error= immediately, breaking the pure-selector contract; (2) kept auth.login.error.accountLocked i18n key (now functionally unused but still claimed by STORY_1_8_KEYS); (3) bundle baseline measurement deferred — post-1-9d LoginPage 7.34K gzipped sits in the ≥6K ESCALATE tier; opted for 10K ceiling with rationale in check-chunk-size.mjs JSDoc; (4) sibling-tab broadcast test (Winston W2/Murat M3) deferred — already-auth guard test covers transitive path. 6 ★ REVIEWER-MANDATORY vi keys flagged for VN-fluent reviewer pass. R-NEW=15 (open-redirect) discharged via 17 sanitizeNextParam tests committed standalone at 3c429f1. Ready for /code-review on fresh-context, different-LLM model.)
# previous_update_-1: 2026-06-29 (Story 1-9d-auth-error-and-recovery-states transitioned ready-for-dev → in-progress. Amelia picked up via /bmad-dev-story 1-9d. Baseline commit preserved at 85b26f0.)
# previous_update_0: 2026-06-29 (Story 1-9d-auth-error-and-recovery-states **party-mode review amendments folded**. Sally + Winston + Amelia + Murat reviewed (each spawned as independent subagent); John ruled the calls. 27 ACCEPTS + 1 DEFER (Sally S6 invited-vs-verified visual differentiation → `1d-followup-banner-invited-verified-differentiation`) + 0 REJECTS. **Net story-file delta**: 504 → 655 lines (+151, ~30%). EXCEEDS bmad-story-conventions.md 600-line ceiling by ~9%; flagged for code-review reviewer attention. Density is load-bearing per party-mode rulings — ATDD specimens / a11y contracts / privacy ratchets are NOT prunable. If contested at code review, prunable candidates are Filed Follow-ups bullets + Architectural Debt section (move to project-context). **Key amendments by AC**: AC1 — Sally a11y (aria-live tick + threshold-announce at 60s/30s + 2 new i18n keys); Sally BLOCKER mobile stack-order at 390×844; Sally focus on heading; Amelia BLOCKER mode-derive race (useLockoutCountdown owns isActive useState driving deriveLoginPageMode, NOT raw lockoutUntilMs); Amelia STRONG form-unmount verbatim pin; Amelia JSON envelope storage + version 1 forward-compat; Winston UX-not-security JSDoc; Winston 900s LOCKOUT_FALLBACK_SECONDS matching backend (was 600s 5-min mismatch); Murat BLOCKER 5 poisoning negative ratchets self-clear; Murat M8 cleanup ratchets (clearInterval spy + double-mount tick-count + isActive same-tick flip). AC2 — Sally STRONG drop register CTA (strands invite); reopen-invite-hint copy replaces dead-end fallback + new i18n key; Sally focus; Murat M6 DOM-wide privacy ratchet extends to query-param echo class. AC3 — Sally STRONG fork — google_userinfo_failed and google_email_unverified get DISTINCT body copy (Workspace-policy vs forced-verification) + 2 new keys; `.not.toEqual` ratchet locks fork; Sally focus; Murat M6 query-param echo ratchet. AC4 — Sally S5 data-loss-hint secondary copy + new key; Sally focus stays on form (banner is acknowledgment); Winston W1 + Amelia A1 BLOCKER three-site convergence — DROP internal useLogin navigate, hook becomes cache + broadcast only, page owns destination at password-submit + already-auth guard; Winston W2 + Murat M3 sibling-tab broadcast test pinned independently; Amelia A6 URL-clear PRESERVES next=; Amelia A3 cookie-clear driven by mount-time useRef snapshot NOT live bannerKey (StrictMode pass-2 survival); Murat M2 + Amelia A8 — 5 OWASP CWE-601 ratchets added (triple-slash + whitespace-prefix + encoded tab + space-prefix + double-backslash) = 17 total tests; Murat M4 STRONG Mode×Banner negative coverage matrix per TEST-FE-6; Murat M5 STRONG cookie-clear StrictMode-spy idempotency test pinned at Task 5.9 (closes 1-9b P1 / 1-9c P6 lineage). AC5 — Winston W6 Banner scope-guardrail JSDoc against god-component drift. AC6 — keys 12 → 16; Sally S7 vi register edit (Vui lòng); Winston W7 orphan-key provenance comments; Murat M7 closed-enumeration meta-assertion (R-NEW=16 misattribution defense); ★ REVIEWER-MANDATORY vi count 4 → 6. AC7 — 5 → 6 LoginPage stories (LockoutMobile390 + fork-distinct WorkspaceBlocked variants). AC8 — NEW Task 7.0 baseline measurement (Winston W3 + Amelia A5); ceiling-decision contract: ≤5KB→8KB / 5-6KB→10KB+SVG share / ≥6KB→ESCALATE to John for lazy-load OR scope-defer. Tasks renumbered: 3.1/3.3 (envelope + isActive + cleanup); 4.1/4.2 (dropped CTA + forked body); 5.2-5.7 amended; 5.9 NEW (cookie spy); 7.0 NEW. Hand-off to Amelia (dev) for `/bmad-dev-story 1-9d`.)
# previous_update_0: 2026-06-29 (Story 1-9d-auth-error-and-recovery-states transitioned backlog → ready-for-dev. John's pre-dev context engine pass against baseline `85b26f0` (1-9c done). 8 ACs map to UX-DR16/DR18/DR20 with FOUR backend-reality reframes pinned inline against Epic 1C's wireframe-driven AC: (1) no `GET /api/auth/lockout-status` endpoint exists — lockout state rehydrates from localStorage instead of server-fetch (Epic AC line 381 amended pragmatically); (2) no `google_blocked` `?error=` code in backend (`auth_handler.go:553-583` enumerates 13 actual codes — none match) — frontend maps `google_userinfo_failed` AND `google_email_unverified` to the WorkspaceBlocked screen as the realistic Google-policy surface; (3) OAuth invite-email-mismatch backend redirect does NOT carry expected/actual emails (privacy contract `auth_handler.go:590-597` SEC-11) — polished mismatch screen frames recovery without echoing emails, mirrors 1-9c REST-path ratchet; (4) `logged_in=1` hint cookie is NOT set by the backend ANYWHERE (handler-agent grep 2026-06-29) — defensive `document.cookie` clear shipped forward-compat for Story 1.10 (Astro landing owns the cookie source). Risk-score ≥6 check: ONE owned — R-NEW=15 open-redirect via `?next=` param on session-expired login success path (P=2, I=3 → 6). Discharge: ATDD red specimens at `sanitizeNextParam.test.ts` (Task 0) PINNED BEFORE GREEN per WF-8 — 12 tests including 6 rejection ratchets (`//evil`, `%2F%2Fevil`, `/\evil`, `https://`, `http://`, `javascript:`). R38 (i18n parity) inherits via `STORY_1_9D_KEYS` (12 new keys: `auth.login.lockout.*` ×3, `auth.login.oauthMismatch.*` ×4, `auth.login.workspaceBlocked.*` ×4, `auth.login.banner.sessionExpired`) + namespace-coverage extension (Murat 1-9c handoff — `scripts/i18n-parity.mjs:COVERED_NAMESPACES` += `'auth.'` requires orphan-key claim patches into historical `STORY_1_8_KEYS` / `STORY_1_9A_KEYS` / `STORY_1_9B_KEYS` / `STORY_1_9C_KEYS` arrays). Discharges Winston 1-9c gate: 5th BannerKey variant (`session-expired`) triggers MANDATORY `<Banner variant>` discriminated-union refactor — extracted to `src/features/auth/components/Banner.tsx` with 5 variants + per-variant token map + aria-role contract (`alert` for warning/destructive, `status` for success). Priority chain re-ranks to `session-expired > invited > reset > verified > oauth-error` (UX-DR18 urgency). Inheritance from 1-8/1-9a/1-9b/1-9c: reuses AuthLayout / AuthCard / GoogleOAuthButton (threads `{prompt: 'select_account'}` via existing `searchParams` prop for Google account-picker re-OAuth on AC2/AC3 retry CTAs) / CollapsibleEmailForm / PasswordInput / useResendCountdown shape / `api-fetch.ts` RFC 9110 Retry-After parsing / `auth-refresh.ts` SESSION_EXPIRED_PATH + `next=` appending (1-7b shipped both — 1-9d is the FIRST consumer that reads them). LoginPage extends to a mode machine: `'default' | 'lockout' | 'oauthMismatch' | 'workspaceBlocked'` with mode-replacement render branches; the `session-expired` branch is banner-only (form mounted alongside). NEW: `useLockoutCountdown(lockoutUntilMs)` hook + `lockoutStorage.ts` localStorage wrapper (independent of `useResendCountdown`'s 300s clamp because lockout window is 15 min). Post-login `?next=` consumer wired at BOTH navigation points (login.ts:68 `useLogin.onSuccess` AND LoginPage.tsx:194-199 already-auth guard). Pragmatic deviations: (a) lockout countdown is localStorage-driven not backend-fetched (no GET endpoint); (b) google_blocked maps to two existing backend codes; (c) OAuth mismatch screen does not echo emails (privacy ratchet); (d) defensive logged_in= cookie clear is forward-compat for Story 1.10. All 4 flagged for John Epic AC amendment per `[[feedback_pragmatic_interpretation_of_spec_absolutes]]`. Out-of-scope (each owned by a specific later story): dedicated `/login/locked` route + backend GET endpoint (API team follow-up, low-priority); dedicated `google_blocked` backend code (API team); `prompt=select_account` Playwright E2E (no Google sandbox in CI — Story 1-6 ATDD extension when sandbox lands); Story 1.10 stale-hint-cookie landing-page redirect dance; `traceability-matrix-epic-1c.md` + `nfr-assessment-epic-1c.md` (Murat owner, pre-1-9d-merge — separate commit, NOT this story); codegen-drift CI gate (DevOps); `build:check` CI wiring (1-line PR later); polished `oauth_wrong_tenant` screen (rare; treat as generic oauth-error). 4 ★ REVIEWER-MANDATORY vi keys flagged for VN-fluent sign-off. Story file 502 lines — under bmad-story-conventions.md 600-line cap. Sibling completion-notes file deferred to first dev pickup per `docs/bmad-story-conventions.md`. Hand-off to Amelia (dev) for `/bmad-dev-story 1-9d`.)
# previous_update_0: 2026-06-29 (Story 1-9c-invite-acceptance-ui transitioned review → done. Three-layer code review on Opus 4.7 1M (fresh context, three parallel subagents: Blind Hunter / Edge Case Hunter / Acceptance Auditor) — 86 raw findings → 2 decisions resolved + 13 patches applied + 8 deferred + ~50 dismissed. Decisions: D1 dedicated `auth.invite.error.emailAlreadyRegistered.cta` key (en+vi, parity test updated — auth.invite.* key count 28→29, ★ REVIEWER-MANDATORY vi count 7→8); D2 lock the intentional footer-absence for alreadyAccepted + emailAlreadyRegistered with a negative test. Patches: (P1) BLOCK-MERGE — routes.test.tsx now imports `indexLoader` directly from routes.tsx instead of the hand-copied harness; (P2) rate-limit alert text re-derives `countdown.remaining` on every render so seconds tick down (mirrors 1-9b P7); (P3) privacy-ratchet extended from 2 to 5 footer-bearing terminal regions (added expired + emailMismatch + passwordNotAllowed); (P4) page-level MIN_RATE_LIMIT_SECONDS=5 floor test for Retry-After:0 clock-skew defense; (P5) MSW_ACCEPT_INVITE_DEFAULT.center.id is now a valid UUID; (P6) sanitizeCenterName.test.ts rewritten with ` ` / `\x07` JS escapes — `file(1)` now reports `Unicode text, UTF-8 text` (was binary `data`), git diff now shows the diff inline; (P7) indexLoader extracted as named export with `+ url.hash` appended alongside `url.search`; (P8) useAcceptInvite gained an optional `getActiveToken: () => string|null|undefined` callback — onSuccess compares vars.inviteToken against the page's current token via tokenRef and skips session-cache write + navigate for stale in-flight results; (P9) all 7 terminal data-testid containers now wrap with role="alert" (alreadyAccepted uses role="status" — good outcome); (P10) MAX_RATE_LIMIT_SECONDS dropped in favor of imported MAX_COUNTDOWN_SECONDS from useResendCountdown; (P11) text-amber-600 swapped for `var(--cl-amber)` design token; (P12) emailAlreadyRegistered.cta dedicated key landed in en+vi+parity test; (P13) absent-footer test landed for both alreadyAccepted + emailAlreadyRegistered. Hash-forwarding test in routes.test.tsx removed inline with a comment explaining MemoryRouter strips fragments on initialEntries parse — production browser router carries the hash, contract verified by inspection. Full CI matrix green: lint, lint:css, tsc -b, vitest 506/506 across 59 files (was 500/500 — +6 tests: 1 P4, 3 P3, 1 P13, 1 P1 multi-param), playwright route-bundle-boundaries 6/6, npm run build clean, npm run build:check (ForgotPasswordPage 1707B / ResetPasswordPage 1981B / InviteAcceptancePage 3158B — all under the 8KB ceiling), npm run storybook:build clean. 8 deferred items appended to deferred-work.md under "Deferred from: code review of story-1-9c (2026-06-29)". Story file Review Findings section retains the full audit trail with all 13 patches checked off.)
# previous_update_-1: 2026-06-27 (Story 1-9c-invite-acceptance-ui transitioned in-progress → review. All 8 ACs + Definition of Done satisfied. Vitest 500/500 across 59 files (was 444), Playwright route-bundle-boundaries 6/6, npm run build clean, npm run build:check reports all 3 auth chunks under the 8 KB gzipped ceiling (Forgot 1704B / Reset 1979B / Invite 3094B), npm run storybook:build clean, tsc -b / lint / lint:css all clean. Dev Agent Record + File List authored at sibling 1-9c-invite-acceptance-ui-completion-notes.md per bmad-story-conventions. Pragmatic deviations flagged: (1) useInviteSchema fullName uses .regex(/\S/) not .refine — same behavioral coverage (empty + whitespace), avoids the ZodEffects wrapping that bit zodResolver in the form-level test; (2) scripts/check-chunk-size.mjs created fresh — the 1-9b reference in the spec was aspirational, the script wasn't actually shipped; covers all 3 current auth chunks at the 8 KB ceiling, wired as npm run build:check; (3) build:check not yet wired into ci-web.yml — 1-line add for a future CI-touching PR. 7 ★ REVIEWER-MANDATORY vi keys flagged in PR description for VN-fluent reviewer pass. Ready for /code-review on fresh-context, different-LLM model.)
# previous_update_-1: 2026-06-26 (Story 1-9c-invite-acceptance-ui party-mode review amendments folded. Sally + Winston + Amelia + Murat reviewed (each spawned as independent subagent), John ruled the calls. 9 ACCEPTS + 5 DEFER-with-named-owner + 0 REJECTS. AC3 i18n key count 27→29 (+ auth.invite.titleWithCenter for Sally's sanitized ?c=centerName conversion-critical ribbon bridge + auth.invite.emailFormExpandedAnnouncement for CollapsibleEmailForm aria-live a11y pin); 2 vi seed copy rewrites for active voice (auth.invite.title "Bạn được mời tham gia" + auth.login.banner.invited "Chào mừng bạn đến với trung tâm"); ★ REVIEWER-MANDATORY vi count 5→7. AC4 added sanitizeCenterName helper at src/features/auth/lib/sanitizeCenterName.ts with /^[\p{L}\p{N}\s\-'.]{1,60}$/u regex + Unicode NFC normalize (8 unit tests pinned) + CollapsibleEmailForm focus → fullName on expand + aria-live announcement pin + token-change-resets-errorState 4-line useEffect (Murat ATDD specimen pin pre-dev — 1-9b shipped this as P1 patch AT code review, 1-9c pins green-first). AC5 added check-circle SVG to invite-already-accepted (Sally visual differentiation good-outcome vs dead-link); dropped Retry-After=0 clamp page test (Amelia — belongs on useResendCountdown.test.ts); added Amelia privacy-ratchet "footer Sign-in link from any terminal state does NOT land on /login?invited=true" across 7 regions; added Murat STRONG email-leak rejection ratchet ATDD specimen for 409 INVITE_EMAIL_MISMATCH with DOM-wide negatives against details.invitedEmail + details.oauthEmail. AC4 test list dropped Google CTA non-RR-navigate test (Amelia — covered by 1-8 GoogleOAuthButton.test.tsx); tightened Google CTA inviteToken test with non-empty assertion + <a href> vs RR <Link> negative (Murat vacuous-pass guard); added "Google CTA NOT rendered when token empty/whitespace" + the ?c= ribbon trio (happy/fallback/sanitization-reject) + focus + aria-live tests. AC6 added Winston `invited > oauth-error` priority-escalation collision test (?invited=true&error=invite_email_mismatch → invited wins + oauth-error param wiped post-URL-clear); added Murat BLOCKER routes.test.tsx index-loader query-forward test at /?invited=true → /login?invited=true as NEW file separate from LoginPage.test.tsx since the loader is owned by routes.tsx not LoginPage. AC7 story count 14→16: added DefaultWithCenterRibbon + LocaleViWithCenterRibbon (Sally `?c=` happy-path en + vi) + Mobile390EmailFormOpen (Sally — happy-path mobile fold unverified; wireframe only covered easier Expired state). Dev Notes new sections: Architectural Debt Acknowledged (Winston — LoginPage banner is transitional flash-of-200ms; Story 2-1 will move toast to dashboard via APP_POST_LOGIN_URL=/dashboard?invited=true; stale sibling-tab on ?invited=true relies on next silent-refresh tick to hydrate useAuth — same assumption as the verified-banner branch from 1-9a), 1-9d BannerKey gate (Winston — 5th variant forces <Banner variant> discriminated-union refactor pre-merge; 4 branches is a smell, 5 is a defect). Task 8.5 chunk-size CI script path documented at classlite-web/scripts/check-chunk-size.mjs (Winston enforcement-seam catch — discipline rules need enforcement seams or they decay). Out-of-Scope filed follow-ups (NOT 1-9c work): (1) codegen-drift CI gate → DevOps/Winston P2 2 sprints (closes the 3-story-old client.ts drift class, not just this incident); (2) traceability-matrix-epic-1c.md → Murat pre-1-9d-merge (cross-epic matrix bleed obscures audit); (3) nfr-assessment-epic-1c.md → Murat pre-1-9d-merge; (4) Story 2-1 ?invited=true toast move to dashboard; (5) 1-9d <Banner variant> refactor mandate. Ducdo confirmed defaults: invite token alphabet base64url; Murat owns traceability + NFR Epic 1C matrices; codegen-drift CI P2 2 sprints DevOps owner. Net story file delta: +~90 lines (3 ATDD specimens + 5 test contracts + 2 i18n keys + 4 task subtasks + 2 Storybook entries + Architectural Debt section + Filed Follow-ups + amended reframe + sanitization helper task) − ~10 lines dropped tests. Total: 647 lines — EXCEEDS the 600-line bmad-story-conventions.md ceiling by 7.8%; flagged for code-review reviewer attention. Addition density is load-bearing per party-mode rulings (ATDD specimens / a11y contracts / architectural debt acknowledgment are NOT prunable). If contested at code review the prunable candidates are Filed Follow-ups bullets (move to separate doc) or Architectural Debt section (move to project-context). Sibling completion-notes file deferred to first dev pickup per bmad-story-conventions.md. Hand-off to Amelia (dev) for /bmad-dev-story 1-9c.)
# previous_update_-2: 2026-06-26 (Story 1-9c-invite-acceptance-ui transitioned backlog → ready-for-dev. John's pre-dev context engine pass against baseline 3824af5 (1-9b done). 8 ACs map to UX-DR5/DR7/DR10/DR15/DR16/DR17 with THREE backend-reality reframes pinned inline against Epic 1C's wireframe-driven AC: (1) no GET /api/auth/invites/{token}/preview endpoint exists — InviteCard center-logo + inviter-heading + role-badge surface deferred (Epic AC amendment filed; preview endpoint owned by Epic 7 = real staff-invite delivery); (2) the new-user / existing-user-logged-in / existing-not-logged-in / already-accepted branch distinction at mount time requires the preview endpoint — single unified form covers all branches via backend-side branch inference; (3) email field omitted entirely (not "hidden + locked" per Epic AC line 337) — backend authoritatively uses invite.email from token row, anti-enumeration blocks pre-acceptance echo. Risk score ≥6 check: NONE owned (R6 owned by Story 1-6 ATDD suite; R38 i18n parity inherits from 1-7c CI gate). WF-8 ATDD red phase NOT required. R38 discharged via STORY_1_9C_KEYS block (27 new keys = 26 auth.invite.* + 1 auth.login.banner.invited) in i18n-parity-coverage.test.ts. Inheritance from 1-8/1-9a/1-9b: reuses AuthLayout / AuthCard / CollapsibleEmailForm / PasswordInput / GoogleOAuthButton (its pre-staged searchParams prop scaffolded at GoogleOAuthButton.tsx:29-46 JSDoc "Story 1.9c will pass {inviteToken}") / useResendCountdown verbatim; extends authKeys with acceptInviteMutation per the 1-9b mutation-key split convention; consumes regenerated openapi types from a pre-flight Task 0 scripts/codegen.sh re-run that catches a 3-story-old client.ts drift (1-6 endpoints + AcceptInviteRequest/Result/InviteCenter/InviteExpiredDetails/InviteAlreadyAcceptedDetails/InviteEmailMismatchDetails schemas are missing from current client.ts even though api.yaml defines them lines 833-930); adds the first NEW default MSW handler since 1-9b (catalog at msw-handler-catalog-auth.md explicitly lists 1-9c as a target_story but no POST /api/auth/accept-invite section exists yet); MSW_ACCEPT_INVITE_DEFAULT constant extracted with `as const satisfies AcceptInviteResult` typecheck (1-9b code-review Murat STRONG pattern). LoginPage extension at LoginPage.tsx:73-78 extends BannerKey discriminated union with 'invited' (priority chain becomes `invited > reset > verified > oauth-error` — invite beats reset because UX-DR10 names the invite path as the highest-value conversion node) + extends URL-clear effect at LoginPage.tsx:199-210 to drop ?invited=true (additive one-liner). Index-loader at routes.tsx:67-70 amended to forward url.search so Story 1-6 OAuth-success redirect to APP_POST_LOGIN_URL?invited=true (= http://localhost:5173/?invited=true per config.go:68) survives the bounce to /login?invited=true (otherwise query is silently flattened by the existing `redirect('/login')` loader). Backend reality fully reproduced inline: POST /api/auth/accept-invite returns 200 with {accessToken, user, center: {id, name}, role}; errors are 400 INVALID_INVITE_TOKEN / 404 INVITE_NOT_FOUND / 409 INVITE_ALREADY_ACCEPTED (details: centerName) / 410 INVITE_EXPIRED (details: centerName, inviterEmail) / 409 INVITE_EMAIL_MISMATCH (details: invitedEmail, oauthEmail — DO NOT echo per privacy mirror of auth_handler.go:590-597 SEC-11) / 409 PASSWORD_NOT_ALLOWED_FOR_OAUTH_USER / 409 EMAIL_ALREADY_REGISTERED / 429 RATE_LIMIT_EXCEEDED / 422 VALIDATION_ERROR / 5xx. Eight distinct data-testid terminal regions + TEST-FE-6 negative-assertion ratchets (every terminal-region test asserts the OTHER seven terminal regions are absent — Murat 1-9b BLOCKER P-fix pattern carried forward). 14 InviteAcceptancePage stories + 1 LoginPage InvitedBanner variant. Path-param routing (/invite/:token via useParams) chosen over query-param to match Epic 1C AC line 330 + AUTH-05 wireframe at ux-design-directions.html:1691 ("my.classlite.app/invite/abc123"). Out-of-scope deferrals each owned by a specific later story: polished OAuth email-mismatch dedicated screen → 1-9d AC2 (UX-DR20); expired-state contact-inviter polished screen → 1-9d; already-accepted page-mount auto-redirect → Epic 7 (needs preview endpoint); 1-click "Join as [role]" flow for existing-user-logged-in → Epic 7; center logo/lettermark + role badge above form → Epic 7; namespace-coverage i18n-parity extension → 1-9d. Story file 557 lines (under bmad-story-conventions.md 600 cap). Sibling completion-notes file deferred to first dev pickup per docs/bmad-story-conventions.md. Hand-off to Amelia (dev) for /bmad-dev-story 1-9c.)
# previous_update_-1: 2026-06-26 (Story 1-9b-password-reset-ui transitioned review → done after three-layer code review on Opus 4.7 1M (fresh context). 62 raw findings → 51 unique → 13 patches landed (10 from triage + 3 from decision resolutions) + 5 decisions resolved + 27 deferred + 9 dismissed. Patches: (P1) ResetPasswordPage resets errorState/formError/mutation-cache on token change via useEffect; (P2) ForgotPasswordPage.test resend test now drives fake timers + clicks resend + asserts 1→2 + deep-equal second body — Murat's contract finally enforced; (P3) LoginPage cache-wipe moved out of useState lazy initializer into useEffect with wipedRef latch (Concurrent-React anti-pattern resolved); (P4) REVERTED on test-revealed contradiction — the `!isAuthenticated` guard broke the design-intent stale-sibling-tab scenario; (P5) ResetPasswordPage strips ?email= URL param on mount + ratchet test extended to assert URL excludes email AND token survives; (P6) LoginPage.test UrlProbe emits reset/verified/error testids separately + both URL-clear tests fixed — vacuous-pass closed; (P7) rate-limit alert interpolates countdown.remaining so seconds tick down; (P8) Retry-After clamped to MIN_RATE_LIMIT_SECONDS=5; (P9) forgotPassword schema trims email; (P10) new consumedForgotCta i18n key (+1 key both locales = 329 total) replaces misleading "Request a new reset link" footer. Decisions: D1 kept <h1> + amended spec line 144 per [[feedback_pragmatic_interpretation_of_spec_absolutes]]; D2 strengthened passwordNotBlank to v.trim().length >= 8; D3 patched bannerKey escalation now (`if (next === bannerKey) return` precedes the null-stickiness guard); D4 kept banner hide-on-open; D5 inlined displayedError alias. Full CI matrix green: lint, lint:css, tsc -b, vitest 445/445, i18n-parity 328 keys, playwright route-bundle-boundaries 5/5. 27 deferred items logged to deferred-work.md under code review of story-1-9b (2026-06-26). 9 dismissed as noise.)
# previous_update_0: 2026-06-26 (Story 1-9b-password-reset-ui transitioned in-progress → review. All 9 ACs satisfied, all tasks + subtasks marked [x]. Full CI matrix green (lint / lint:css / tsc -b / vitest 444 + lint-fixtures 12 / playwright 5 / build / storybook:build). Chunk-size budget green (ForgotPasswordPage 1.68 KB gzipped, ResetPasswordPage 1.88 KB gzipped — under the 8 KB ceiling). Dev Agent Record + File List authored at sibling 1-9b-password-reset-ui-completion-notes.md per bmad-story-conventions. Pragmatic deviations flagged: (1) `asChild`-less shadcn Button → Link styled with buttonVariants; (2) jest-dom matchers absent → `.toBeTruthy()` / `.toBeNull()` per project convention; (3) banner re-derivation made additive-only so URL-clear doesn't clobber initial paint; (4) Epic 1c "pre-fill email on expired CTA" AC rejected on anti-enum + email-leak grounds — pending Epic AC amendment from John. Ready for /code-review on fresh-context, different-LLM model.)
# previous_update_1: 2026-06-26 (Story 1-9b-password-reset-ui transitioned ready-for-dev → in-progress. Amelia picked up via /bmad-dev-story 1-9b. Baseline commit preserved at 0e1484f.)
# previous_update_2: 2026-06-26 (Story 1-9b-password-reset-ui transitioned backlog → ready-for-dev. John's pre-dev context engine pass against baseline 0e1484f (1-9a done). 8 ACs map to UX-DR5/DR8/DR15/DR16/DR17 with three backend-reality reframes pinned inline: (1) reset URL is `{base}?token={raw}` query-param form per auth_reset.go:102 + config.go:62 — extract via useSearchParams().get('token'); (2) backend returns FOUR distinct error codes (404 RESET_TOKEN_INVALID / 409 RESET_TOKEN_CONSUMED / 410 RESET_TOKEN_EXPIRED / 422 VALIDATION_ERROR) — each owns a distinct inline state per UX-DR16; (3) successful reset invalidates all refresh tokens server-side, so the LoginPage `?reset=1` banner copy explicitly states 'all other devices have been signed out' to set the right user expectations. Risk score ≥6 check: NONE owned; WF-8 ATDD not required. R38 discharged via STORY_1_9B_KEYS block (27 new keys) in i18n-parity-coverage.test.ts. Inheritance from 1-8/1-9a: reuses AuthLayout / AuthCard / PasswordInput / PasswordStrengthBar / useResendCountdown verbatim; extends authKeys with forgotPasswordMutation + resetPasswordMutation per the P5 split; consumes shipped MSW default handlers for both endpoints (no new catalog sections); LoginPage banner amendment adds `?reset=1` as the third query-param branch alongside `?error=` / `?verified=1`. Pragmatic-scope deviation acknowledged: epic AC for 'pre-fill email on expired-CTA' rejected on security grounds (GET /reset-token/{token}/email expands the anti-enum attack surface; embedding email in URL leaks via email-client preview tooling); default — no pre-fill, user re-enters email — flagged for reviewer sign-off in PR description. Out-of-scope deferrals each owned by a specific later story (1.9d for polished error screens / lockout integration / auth-route guards). Sibling completion-notes file authored at first dev pickup per bmad-story-conventions.md. Hand-off to Amelia (dev) for /bmad-dev-story 1-9b.)
# previous_update_2: 2026-06-15 (Story 1d-1-storybook-foundation transitioned backlog → ready-for-dev after Murat's pre-dev TD gate refresh closed. /bmad-tea TD surgical refresh layered Epic 1D onto the 2026-06-04 system-level baseline (in-place, no fork) — test-design-architecture.md + test-design-qa.md + classlite_new-handoff.md all carry new "Epic 1D Refresh (2026-06-15)" sections. Risk decisions: R38 (i18n parity, score 6) discharge-mapped to 1d-1 AC4 (assertI18nParity helper + i18n:parity CI step + WF-8 ATDD red phase MANDATORY before AC4 implementation begins — pinned test: assertI18nParity.test.ts::TestAssertI18nParity_RaisesOnViOnlyKey); R39 (Vite/Rolldown plugin) promoted MONITOR (4-5) → MITIGATE (6) for Epic 1D scope — mitigation = three-tier ladder per 1d-1 AC1 (Tier A Rolldown preferred / Tier B dual-builder fallback / Tier C deferral), Tier C kill-switch requires PM (John) + user (Ducdo) re-scope approval; R45 (CF cache wrong origin) confirmed unchanged at MONITOR (3). Three new Epic-1D-only risks added: R51 (axe baseline drift across 34 primitives + 18 domain components, MONITOR 4) mitigated by storybook:test gate + axe.allowlist.json governance carried from 1-7c; R52 (three-state variant explosion across ~200 stories, MITIGATE 6) mitigated by 1d-1 AC3 @storybook/test-runner postRender hook with missing-empty-export.stories.tsx negative-fixture proof — error on merge from day 1; R53 (designer token churn, MONITOR 4) mitigated by Pattern 1 CSS-variable token discipline in 1d-2 AC7 + // CL-THEME-SWAP: comment governance. Coverage matrix: 25 P0 + 114 P1 + 32 P2 + 10 P3 Epic 1D scenarios across 52 component files / 24 matrix rows (1d-2 grouped by AC category × 6; 1d-3 individually × 10; 1d-4 individually × 8; 1d-1 foundation rows × 5). CI storybook job has 8-min soft cap (shard-by-pattern documented if runtime trends > cap after 100 stories). WF-8 ATDD applicability summary: 1d-1 AC4 is the ONLY ≥6-risk AC across Epic 1D — 1d-2/1d-3/1d-4 have no per-story red-phase ceremony, coverage enforced mechanically via 1d-1's CI gates. TEST-FE-1 mock-seam inheritance (MSW at HTTP boundary, NEVER mock useQuery — Empty stories driven by HttpResponse.json({data:[]})) propagated to all 1d-N stories via storybook-conventions.md § 6 reference. epic-1d transitioned backlog → in-progress (first story in epic 1D promoted per create-story workflow convention). 1d-1 pre-dev-gate paragraph in the story file updated CLOSED 2026-06-15 with the gate outcome summary. Hand-off to Amelia (dev) for /bmad-dev-story 1d-1 — Tier A spike first (1 working day timebox), Tier B if needed (1 day), escalate to Tier C only with explicit John+Ducdo approval. POST-TRANSITION AMENDMENT 2026-06-15 (Murat — /bmad-tea AT pre-flight discovery): AC4 amended in-place after AT pre-flight found that Story 1-7c (shipped 2026-06-12) already delivered the R38 four-layer mitigation (assertI18nParity Vitest helper at src/lib/test/i18n-parity.ts + helper tests + ATDD red specimen at i18n-parity-coverage.test.ts + npm run i18n-parity CI step at ci-web.yml:69-77). AC4 re-scoped from "generate new helper + CI step + failing-fixture infrastructure" to inheritance contract pointing at the existing 1-7c artifacts — no new code from 1d-1. R38 discharge mapping in test-design-architecture.md updated; WF-8 ATDD applicability table corrected (no Epic 1D story requires a separate ATDD red phase, since R38 was discharged at 1-7c and R52 is foundation-CI-lint mitigation that needs no ATDD); test-design-qa.md 1D-P0-001/002/003 reframed from "generate" to "verify 1-7c artifacts still green"; classlite_new-handoff.md story 1d-1 AC pattern + R38 risk-to-story row corrected. Net effect: Amelia's 1d-1 implementation is materially smaller than the original story implied — AC4 collapses from a multi-file build to a doc-inheritance contract. Per-story 1d-2/1d-3/1d-4 dev work picks up a tiny inline `describe('Story 1d-N i18n parity (R38)', ...)` block in i18n-parity-coverage.test.ts alongside their component tests, not a separate red-phase ceremony.)
# previous_update_-1: 2026-06-12 (Story 1-7c-shared-layout-components-and-i18n transitioned review → done after code review: 5 decisions resolved (D1 tertiary CTA shipped + 2 new locale keys, D2 sidebar nav got distinct aria-label + 1 new key, D3 mobile hamburger toggle wired in TopBar, D4 BroadcastChannel cross-tab language sync added, D5 root `errorElement` added to routes.tsx); 19 patches applied across en+vi locales (+4 keys → 41 total), PermissionDenied (tertiary CTA + no-op JSDoc), Sidebar (class dedup + distinct nav aria), TopBar (hamburger + i18n breadcrumb), LanguageToggle (fieldset → role=group), useLanguageInit (BroadcastChannel + Sentry-catch on changeLanguage), usePolling (intervalMs guard + Sentry-catch on rejection), routes.tsx (root errorElement + corrected ranking comment), i18n-parity.mjs (empty-value check), ci-web.yml (i18n-parity ahead of Test for fast-fail), vitest-setup.ts (explicit toHaveNoViolations named import), sprint-status comment renumber, and tests (i18n.t resolution everywhere, TEST-FE-3 `.reset()` usage, Tab-focus contract on AppLayout, BroadcastChannel sync test on useLanguageInit). Vitest 25/25 files, 153/153 tests green (lint-fixtures suite needs higher testTimeout; 4/4 pass at 30s timeout). 7 deferred items (sectionName prop → 2-6, Sidebar placeholder nav text → 1d-3, signOut key → 1-8, cookie migration duplicate, UserPill empty-displayName guard, vitest-setup cookie reset, Playwright clearCookies catch). 7 dismissed (Sidebar collapse-on-desktop is intentional per AC2, routes.tsx inline Component matches spec literal, parity-coverage app.errorFallback-removed guard intentional, App.tsx LSP false positive, usePolling lint suppression justified, Test 5 covered in LanguageToggle.test, bilingual smoke regex matches spec literal). Story closes Epic 1C foundation triple: 1-7a (design system) + 1-7b (app shell) + 1-7c (layout chrome + i18n) all done.)
# previous_update_-2: 2026-06-12 (Story 1-7c-shared-layout-components-and-i18n transitioned in-progress → review. All 11 tasks + 50 subtasks complete. Final test matrix: Vitest 149/149 across 25 files (1-7b baseline 100/100, +49 new); Playwright design-system 23/23 (+10 bilingual smoke); cross-subdomain 6/6 (+2 dashboard-boots-in-vi). `tsc -b`, `lint`, `lint:css`, `i18n-parity` (37 keys both en+vi), `build` (7 lazy chunks: AuthLayout, LoginPagePlaceholder, StudentDashboard, TeacherDashboard, AppLayout, PermissionDenied, NotFound) all clean. Dev-route grep gates exit 1. R38 (i18n parity score 6) mitigated via 4-layer guard: parity-coverage Vitest test, `assertI18nParity` in every component test, CI `npm run i18n-parity` step gating merge, Playwright bilingual smoke walking /login + /permission-denied + catch-all + /dashboard in en+vi. Out-of-spec micro-additions documented: (a) LoginPagePlaceholder H1 swap from app.welcome → auth.login.title, (b) routes.tsx wraps /dashboard + /student in <AppLayout> via pathless lazy layout route so skip-link is reachable on /dashboard. Tech findings flagged for review: vitest-axe 0.1.0 ships empty extend-expect.js (worked around with manual expect.extend + custom TS augmentation); RTL auto-cleanup doesn't fire with Vitest globals:false (added explicit afterEach(cleanup)); usePolling refactored to assign fnRef in separate useEffect (react-hooks/refs lint). Vietnamese translation review for app.errorBoundary.* / app.permissionDenied.* / app.notFound.* flagged in PR description. Task 11.12 manual DevTools cross-domain observation deferred to reviewer — Playwright covers headlessly.)
# previous_update_-3: 2026-06-11 (Story 1-7c-shared-layout-components-and-i18n scaffolded backlog → ready-for-dev: comprehensive context engine closing the Epic 1C foundation triple. OWNS R38 (i18n parity, score 6) — WF-8 ATDD red phase MANDATORY via four-layer mitigation (assertI18nParity helper in every component test + i18n-parity-coverage.test.ts as the ATDD red specimen against ~35 new keys + `npm run i18n-parity` CI step wired into `.github/workflows/ci-web.yml` + Playwright bilingual-smoke spec walking /login /permission-denied /not-found /dashboard in BOTH locales with @axe-core/playwright full-page audits). Scope: AppLayout + Sidebar + TopBar + UserPill placeholders in components/shared/ (non-role-variant — Epic 1D Story 1d-3 will refactor); polished ErrorBoundary REPLACING 1-7b's minimal RootErrorBoundary with Sentry event ID + retry CTA + UX-DR16 three-part recovery; PermissionDenied with two body-copy variants (Owner+Admin / Owner-only) per UX-3 + UX spec §6.4; NotFound + `path: '*'` catch-all closing 1-7b's W1 deferral; LanguageToggle + `lib/language-cookie.ts` + `hooks/useLanguageInit.ts` cookie-driven UX-DR17 cross-subdomain handoff (dashboard half only — landing half ships with Story 1.10); ~35 new i18n keys with parity-enforced en + vi; vitest-axe + axe-core + @axe-core/playwright devDeps + axe.allowlist.json governance stub; four stub hooks (useAuth/useCurrentCenter/useRole stubs returning no-session shapes + usePolling real implementation for Story 1-9a verify poller). Scaffold reality: 1-7b RootErrorBoundary + `app.errorFallback` key DELETED; App.tsx rewires wrapper + adds useLanguageInit(); routes.tsx extends with /permission-denied + path:* catch-all; lib/i18n.ts rewires `lng` seed from cookie. Risk-score ≥6 check: ONE owned (R38). Out of scope (explicit deferrals): role-variant Sidebar / TopBar / UserPill / SearchPalette / Mobile Tab Bar → 1d-3; real useAuth / useCurrentCenter / useRole behavior → Stories 1-8 / 2-2 / 2-6; Astro landing + full classlite.app → my.classlite.app E2E navigation → Story 1.10; EmptyState → Epic 10 Story 10-3/10-4 (Path B); additional shadcn primitives → 1d-2.)
# previous_update_-4: 2026-06-10 (Story 1-7b transitioned review → done: code-review applied 16 patches across api-fetch / auth-refresh / query-client / sentry / location-stub / multi-tab bait + extended useQuery test coverage. Patch highlights: isAuthError used in QueryCache/MutationCache (was AuthExpiredError-only); double-401-after-refresh now throws AuthExpiredError instead of escaping as ApiError(401); onAuthFailure latched against double-fire; next= param compares pathname only (no self-redirect loop); parseEnvelope JSON.parse wrapped (captive portal HTML 200 now surfaces INVALID_RESPONSE); localStorage read/write guarded for QuotaExceededError + private mode; BroadcastChannel listener detach+reattach on test reset; navigator.locks.request rejection falls back to in-process refresh; BroadcastChannel message type guard; Sentry httpClientIntegration dropped (was double-emitting breadcrumbs alongside apiFetch); stubLocation returns restore() handle; route-bundle dev-route audit FAILS instead of skips when dist/ missing; MultiTabTestPage safe error handling; subtasks 11.9/11.10 unchecked with deferred-to-reviewer annotation; project-context TEST-FE-3 updated to sanction reset() action pattern. Vitest 100/100 (added useQuery → QueryCache.onError test), Playwright design-system 13/13, lint + build + grep gates all clean. One defer: NotFound catch-all route → Story 1-7c.)
# previous_update_-5: 2026-06-09 (Story 1-7b transitioned in-progress → review)
# previous_update_-6: 2026-06-09 (Story 1-7b transitioned ready-for-dev → in-progress)
# previous_update_-7: 2026-06-09 (Story 1-7b-app-shell-routing-and-state-management scaffolded backlog → ready-for-dev: comprehensive context engine covering React Router v7 library mode with three explicit lazy chunk boundaries (auth/student/teacher) + TanStack Query v5 cache-level onError 401 silent-refresh contract + UX-DR19 multi-tab navigator.locks + BroadcastChannel coordinator with `lastRefreshedAt` debounce inside lock callback (prevents refresh-token-reuse race that the lock alone does NOT close) + `apiFetch` envelope-unwrap helper with request_id propagation + @sentry/react init with request_id breadcrumbs + minimal `RootErrorBoundary` top-level class component + three Zustand stores (UI/editor/language) with exported initialState per TEST-FE-3 + ESLint guard banning raw fetch/axios in features/hooks; explicit migration of 1-7a `/__theme-resolution` dev route into router as DEV-gated lazy child preserving the existing Playwright design-system project; CQ-1 cleanup of Google Fonts CDN block in index.html. Risk-score ≥6 check: NONE owned (R38→1-7c, R39→1d-1 with 1-7a-style early-signal escalation, R45→1-7c, R46→DevOps); ATDD red phase skipped per WF-8 with inline Vitest+Playwright executable contracts covering AC2-AC8. Scope deferrals explicit: AppLayout/Sidebar/TopBar/PermissionDenied/NotFound/polished ErrorBoundary/language toggle+cookie/assertI18nParity helper/axe-core CI/cross-domain Playwright project — all 1-7c or later.)
# previous_update_-1: 2026-06-08 (Story 1-7a transitioned in-progress → review: design system + shadcn theme bridge + Fraunces/Geist Mono fontsource + tokens.css sync + stylelint/ESLint raw-hex enforcement + components/ui governance complete. Vitest 67/67 green, Playwright 8/8 green on design-system project, lint + tsc + build clean on both surfaces.)
# previous_update_0: 2026-06-08 (Story 1-7a transitioned ready-for-dev → in-progress)
# previous_update_1: 2026-06-08 (Story 1-7a-design-system-and-component-library scaffolded ready-for-dev + party-mode revision pass: Sally/Winston/Amelia/Murat reviewed, John ruled — AC3 corrected to Tailwind v4 import order with full :root mapping including all *-foreground pairings + .dark block retained per Winston's CQ-1 reread + Playwright 4-primitive theme-resolution matrix (Button/Input/Card/Dialog) replacing jsdom; AC2 swapped SHA-256 for sync + git diff --exit-code + automated counter-fixture; AC4 prescriptive @fontsource-variable Fraunces both surfaces + Vietnamese subset + typography-resolution spec + R39 early-signal note; AC5 integration-level lint-script sandbox test + 3-layer fixture exclusion; AC6 trip-wire escalation + [shadcn] commit-tag allowlist + components/ui/README role/state taxonomy with UX-DR2/UX-DR22 anti-patterns; AC7 --cl-ink-dot token retiring stylelint-disable + docs/lint-exceptions.md governance stub; epic-1c transitioned backlog → in-progress)
# previous_update_2: 2026-06-07 (Epic 1D re-scoped to Path B after party-mode review: 1d-1 foundation rewritten with 3-tier Rolldown kill-switch + preview-side deps + i18n parity CI step (R38 mitigation) + error-on-merge three-state lint; 1d-2 adds Toggle/ToggleGroup primitives + shape-semantic skeleton split; 1d-3 adds full TS Props interfaces; 1d-4 RENAMED from visual-status to phase4-visual-bridge (Sally+Mary synthesis — static visual shells of WriteDocSurface/WritingGradingSurface/SpeakingGradingSurface/AnchoredQuestionCard/MobileWritingSurface/InboxListShell/AnalyticsHomeShell, behavior defers to feature epics); 1d-5/1d-6/1d-7/1d-8 deferred to feature epics 2/3/8/9/10; pre-1d-1 gate: /bmad-tea TD re-run required per Murat)
# previous_update_3: 2026-06-07 (Epic 1D Component Library Buildout added: 8 stories 1d-1..1d-8 covering Storybook foundation + shadcn primitives + domain components for Phases 1-3 from 93-screen mockup set; Phase 4 behavior-heavy components deferred to feature epics 5/6/7; designer parallelization initiative — launch slip accepted; all 8 stories status=backlog)
# previous_update_4: 2026-06-07 (story 1-6 → review: Google OAuth init/callback + invite acceptance + force-logout implemented; all ATDD red-phase tests green; full regression + migrate round-trip clean)
# project: classlite_new
# project_key: NOKEY
# tracking_system: file-system
# story_location: "{project-root}/_bmad-output/implementation-artifacts"

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - review-blocked: Implementation shipped but a party-mode / peer review surfaced
#     block-merge items that must land before a code-review pass. Project-specific
#     extension introduced 2026-06-21 (Story 1d-3 party-mode review). Re-promotes
#     to `review` once block-merge tasks are merged.
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)
#
# RESTRUCTURING NOTE (2026-05-29):
# =================================
# Epic 1 split into 1A/1B/1C. Stories 1.2, 1.7, 1.9, 2.3 decomposed into sub-stories.
# New gap stories added: 1.2d-f, 1.3b, 2.7, 4.5, 8.5.
# Epic files now in _bmad-output/planning-artifacts/epics/ (sharded).
# See epics/index.md for full structure.

generated: 2026-05-28
project: classlite_new
project_key: NOKEY
tracking_system: file-system
story_location: "{project-root}/_bmad-output/implementation-artifacts"

development_status:
  # Epic 1A: Project Foundation
  epic-1a: in-progress
  1-1-monorepo-scaffold-and-infrastructure-setup: done
  1-2a-go-api-skeleton-and-middleware-chain: done
  1-2b-database-connection-health-endpoint-and-tenant-context: done
  1-2c-error-handling-and-config-system: done
  1-2d-email-service-abstraction: done
  1-2e-presigned-upload-infrastructure: done
  1-2f-event-tracking-foundation: done
  1-3-auth-database-schema-rls-and-security-testing: done
  1-3b-audit-logging-infrastructure: done
  epic-1a-retrospective: optional

  # Epic 1B: Authentication
  epic-1b: in-progress
  1-4-email-password-registration-and-email-verification-api: done
  1-5-login-session-management-and-password-reset-api: done
  1-6-google-oauth-and-invite-acceptance-api: done
  epic-1b-retrospective: optional

  # Epic 1C: Frontend Foundation & Landing Page
  epic-1c: in-progress
  1-7a-design-system-and-component-library: done
  1-7b-app-shell-routing-and-state-management: done
  1-7c-shared-layout-components-and-i18n: done
  1-8-auth-ui-registration-and-login-screens: done
  1-9a-email-verification-ui: done
  1-9b-password-reset-ui: done
  1-9c-invite-acceptance-ui: done
  1-9d-auth-error-and-recovery-states: done
  1-10-astro-landing-page: done
  epic-1c-retrospective: optional

  # Epic 1D: Component Library Buildout (Path B — trim + Phase 4 visual bridge)
  # Pre-dev gate: CLOSED 2026-06-15 (Murat — TD surgical refresh complete; R38 → 1d-1 AC4 discharge mapping confirmed in handoff; R39 promoted MONITOR → MITIGATE for Epic 1D scope; 3 new risks added R51/R52/R53; coverage matrix appended in-place to test-design-qa.md as "Epic 1D Refresh (2026-06-15)")
  epic-1d: in-progress
  1d-1-storybook-foundation: done
  1d-2-shadcn-primitive-coverage: done
  1d-3-app-shell-stack: done
  1d-4-phase4-visual-bridge: done
  # Story 1d-3 follow-ups (party-mode review 2026-06-21):
  1d-followup-codeowners-and-shell-allowlist-rule: backlog
  # Story 1d-4 follow-ups (code review 2026-06-24):
  1d-followup-tokenizer-migration: backlog # → replace classlite-web/scripts/lib/strip-comments-and-strings.mjs regex stripper with acorn or @babel/parser. Third regex-strip class of bug ate export declarations during 1d-4 (templates with `I'm`/`author's`, JSX attrs with `couldn't`); pass-order reorder is an interim fix.
  # Legacy stories (deferred to feature epics per Path B — files retained as input artifacts):
  1d-4-visual-status-domain: deferred-to-feature-epic # → epic-8 (dashboards/analytics) + epic-9 (billing)
  1d-5-shells-and-states: deferred-to-feature-epic # → epic-2 (onboarding shells) + epic-10 (empty/error states)
  1d-6-data-list-table: deferred-to-feature-epic # → epic-3 story 3-1 (first consumer) + epic-6 story 6-1 (GradingQueueShell)
  1d-7-drawers-modals-forms: deferred-to-feature-epic # → epic-2 story 2-3a (canonical RHF wrapper)
  1d-8-tabs-shell-and-calendar: deferred-to-feature-epic # → epic-3 stories 3-2/3-4/3-5 (with widened 2-day calendar spike per Winston/Murat)
  epic-1d-retrospective: optional

  # Epic 2: Onboarding, Center Setup & Roles
  epic-2: in-progress
  2-1-onboarding-api-persona-selection-center-setup-and-save-resume: done
  2-2-class-template-and-spawning-api: done
  2-3a-onboarding-ui-persona-selection-and-center-setup: done
  2-3b-onboarding-ui-template-selection-and-class-spawning: backlog
  2-3c-onboarding-ui-completion-and-resume: backlog
  2-4-post-onboarding-checklist-and-first-ai-grade-card: backlog
  2-5-center-settings-and-google-meet-integration: backlog
  2-6-roles-permissions-and-authorization-enforcement: backlog
  2-7-bulk-student-import: backlog
  epic-2-retrospective: optional

  # Epic 3: Class Management & Scheduling
  epic-3: backlog
  3-1-class-crud-lifecycle-and-creation-ui: backlog
  3-2-class-detail-view-with-tabs: backlog
  3-3-class-templates-management: backlog
  3-4-schedule-workspace-and-session-management: backlog
  3-5-session-detail-and-attendance-recording: backlog
  epic-3-retrospective: optional

  # Epic 4: Exercise Authoring, AI Content Generation & Knowledge Hub
  epic-4: backlog
  4-1-exercise-library-and-crud-api: backlog
  4-2-exercise-editor-structure-questions-and-settings: backlog
  4-3-ai-content-generation-pipeline: backlog
  4-4-knowledge-hub-and-file-management: backlog
  4-5-jsonb-schema-migration-strategy: backlog
  epic-4-retrospective: optional

  # Epic 5: Assignments, Student Attempts & Submissions
  epic-5: backlog
  5-1-assignment-creation-and-submission-lifecycle-api: backlog
  5-2-quiz-attempt-interface-reading-listening-vocabulary: backlog
  5-3-writing-attempt-interface: backlog
  5-4-speaking-attempt-interface: backlog
  5-5-submission-result-view: backlog
  epic-5-retrospective: optional

  # Epic 6: Grading & AI-Assisted Grading
  epic-6: backlog
  6-1-writing-grading-with-anchored-comments: backlog
  6-2-ai-assisted-writing-grading: backlog
  6-3-speaking-grading-and-ai-assisted-speaking-grading: backlog
  6-4-auto-grading-reading-listening-vocabulary: backlog
  epic-6-retrospective: optional

  # Epic 7: People Management, Enrollment & Anchored Q&A
  epic-7: backlog
  7-1-staff-management-and-invitation: backlog
  7-2-student-lists-and-student-detail-view: backlog
  7-3-enrollment-management: backlog
  7-4-anchored-qa-system: backlog
  epic-7-retrospective: optional

  # Epic 8: Analytics, Dashboards & Search
  epic-8: backlog
  8-1-role-specific-dashboards: backlog
  8-2-analytics-home-and-class-performance: backlog
  8-3-student-performance-views: backlog
  8-4-global-search-cmd-k: backlog
  8-5-analytics-seed-data-script: backlog
  epic-8-retrospective: optional

  # Epic 9: Billing, Plans & Account Management
  epic-9: backlog
  9-1-plan-tiers-and-limit-enforcement: backlog
  9-2-upgrade-downgrade-and-ai-credit-add-ons: backlog
  9-3-payment-failure-grace-period-and-invoices: backlog
  9-4-user-profile-management: backlog
  epic-9-retrospective: optional

  # Epic 10: Inbox, Notifications, Archive & Polish
  epic-10: backlog
  10-1-role-scoped-inbox-and-notifications: backlog
  10-2-archive: backlog
  10-3-empty-states: backlog
  10-4-error-states: backlog
  epic-10-retrospective: optional
