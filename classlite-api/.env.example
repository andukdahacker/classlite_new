PORT=8080
DATABASE_URL=postgres://classlite:classlite_dev_password@localhost:5432/classlite_dev?sslmode=disable
JWT_SECRET=dev-jwt-secret-change-in-production
COOKIE_DOMAIN=localhost
CORS_ORIGINS=http://localhost:5173,http://localhost:4321
SENTRY_DSN=

# Story 6.1 — grade-release notification (student email is gated; grade releases regardless).
# Default false until 5.5b renders the grade block for the student.
GRADE_RELEASE_EMAIL_ENABLED=false
# App origin the release email deep-links into ({base}/assignments/{id}/submission).
APP_RESULT_URL_BASE=http://localhost:5173
